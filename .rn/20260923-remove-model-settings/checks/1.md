# 1 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| repo の `claude/settings.json` に `model` と `effortLevel` のキーが無く、それ以外のキーと値は変更前と同一で、JSON として読める | OK | `jq 'has("model"),has("effortLevel")'` が false/false。変更前ファイルから2キーを `jq del` したものと変更後を `jq -S` で比較して差分なし(jq で読めることも同時に確認)。git diff は2行削除のみで、残りのキー順・インデントは不変 | OK | 差分と `jq -S` 比較を取り直し一致 |
| `./setup.sh` 後の `~/.claude/settings.json` に `model` と `effortLevel` が無く、`./setup.sh` が失敗を報告せずに終わる | OK | 1回目: 終了コード 0、最終行 `Done.`、失敗一覧なし。配備後の `~/.claude/settings.json` で model/effortLevel とも null。setup.sh 自身が足す `hooks`(herdr 連携)と `enabledPlugins`(rn@ccpm)を除けば repo と完全一致(`jq -S` 比較)。2回目も `Done.` で同じ結果 | OK | 実機ファイルで2キーと `modelSettings`・`env` が null。バックアップ2回分で配備完了を裏付け |
| `./setup.sh` 後に起動した Claude Code が設定エラーを出さず、使うモデルと effort がそのモデルの既定値になっている(Opus 5.5 なら effort は medium) | OK | 親セッションから継いだ `CLAUDE_EFFORT` 等の環境変数を外して新規起動。`claude -p --output-format stream-json --verbose` の init で `model: claude-opus-5-5[1m]`、modelUsage も同じ、エラーなし。pty 上で対話起動した画面ヘッダに `Opus 5.5 (1M context)` と `◐ medium · /effort` を確認。docs(model-config)でも既定モデルは Opus 5.5、Opus 5.5 の既定 effort は medium、トップレベル `effortLevel` は Opus 5.5 には効かないと記載あり | OK | 設定を固定しうる層(local/managed settings、`~/.claude.json`、シェル設定、launchctl、SessionStart フック)をすべて確認し固定なし。親由来の `CLAUDE_EFFORT` を外した起動は妥当 |
| statusline がモデル名と effort を表示し、jq エラー等で表示が欠けない | OK | 実機の対話起動で、`--settings` で statusLine だけを「入力を保存してから配備済み statusline.sh に渡す」ラッパーに差し替えて取得(ライブ設定は編集せず)。Claude Code が渡した実 JSON は `effort: {level: "medium"}`、`model.display_name: "Opus 5.5 (1M context)"`。出力は `C:0k/1000k \| Opus5.5/m \| scratchpad`、stderr は空。画面上の表示も同じ文字列 | OK | statusline.sh は Claude Code が渡す値だけを読む。effort あり/なし/null の入力で実行し表示欠けなし |
| `/effort` で書き込まれた値が、次の `./setup.sh` 後の `~/.claude/settings.json` に残っていない | OK | `claude -p "/effort high"` は「this session only」で書き込まれなかったため、expect で対話セッションを動かし `/effort high` を入力。Claude Code 自身が `~/.claude/settings.json` に `modelSettings.claude-opus-5-5.effortLevel: "high"` を書いた(トップレベル `effortLevel` ではない。docs の記載どおり)。その後 `./setup.sh` を流すと `modelSettings`・`effortLevel`・`model` はすべて null(旧ファイルはバックアップへ) | OK | `.bak`(141158)に `/effort` が書いた `modelSettings` があり、配備後は無い |
| repo 内(過去セッションの `.rn/` を除く)に、モデルや effort を固定していると読める記述が無い | OK | `git grep -iE 'effortLevel\|"model"\|opus\|sonnet\|haiku\|effort' -- ':!.rn'` の該当は `claude/settings.json` の2キー(削除済み)と `claude/scripts/statusline.sh` の表示処理のコメント・コードのみ。後者は受け取った値を表示するだけで固定ではない。README・CLAUDE.md・setup.sh・lib/ に該当なし | OK | `.rn` を除く git grep で statusline.sh の表示処理のみ |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | 親セッションの `CLAUDE_EFFORT` を外した起動、`/effort` の実際の書き込み先(`modelSettings`)の特定など、目的に沿った確認。範囲外の補足: statusline は effort を先頭1文字で出すため medium と max が同じ「m」になる(既存の挙動) |

## Expert Reviews (axes the task needs)

### Craft Expert (coding)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | JSON として読め、他のキーと値は不変。既定に従うならキーを置かないのが正しい形。repo 内に2キーを前提とする処理なし |
| Consistency with existing style | OK | 2スペースインデント・キー順・カンマ位置は元のまま |

### Verification Expert (test)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | OK | 差分・配備後ファイル・バックアップ2つ・docs を自ら確認 |
| Coverage (edge cases / claims / steps) | OK | 他の設定層と環境変数(`ANTHROPIC_MODEL` 等)に固定なし |

## Overall Verdict

- Self-check: OK
- QA: OK
- Design expert: N/A
- Craft expert: OK
- Verification expert: OK
- Ready to check off: Yes
