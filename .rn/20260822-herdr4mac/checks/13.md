# #13 Completion Check

## Completion Criteria

| 完了基準 | 判定 | 根拠 |
|---|---|---|
| `setup.sh` に埋め込み python が残っておらず、`lib/merge.py` が単体で読める python ファイルとして存在する | OK | `setup.sh` 1360 → **193 行**、`lib/merge.py` 1196 行。`grep PYTHON_MERGE setup.sh` はヒットなし。`lib/merge.py` は shebang 付き実行可能で、引数なしで叩くと `usage: merge.py {json\|toml} SRC DST` / rc=1 |
| `setup.sh` をどのディレクトリから起動しても `lib/merge.py` を見つける | OK | 既存の `DOTFILES_DIR="$(cd "$(dirname "$0")" && pwd)"` をそのまま使い `"$DOTFILES_DIR/lib/merge.py"` を呼ぶ。`/tmp` から絶対パス指定で叩いて解決を確認 |
| `tests/` から `suite.py` が repo だけで走り、`PASS=127 FAIL=0` | OK | `cd tests && python3.12 suite.py` → `PASS=127 FAIL=0`。ハードコードされていた `/Users/kiyo/.../herdr4mac` は `h.py` / `suite.py` / `fuzzmut.py` / `realdata.sh` の4か所とも `__file__` / `$HERE` からの相対解決に置換済み(`grep` で残存 0) |
| 切り出しによる挙動の変化がない | OK | `suite.py` `PASS=127 FAIL=0`(切り出し前と同数)、`fuzzrand.py` `unexpected rc: 0 / invalid_out: 0 / nonidem: 0 / lost: 0`、`realdata.sh` は `config: ok` 前後 rc=0 / `IDEMPOTENT: yes` / `WT IDEMPOTENT: yes` / 実ファイル md5・mtime 不変 |

## 切り出しで必要になった追随

- `suite.py` の文言検査(`8-*`)は setup.sh 1 ファイルを読んでいたので、`setup.sh` + `lib/merge.py`
  の連結を読むように変えた。呼ぶ側の文言と呼ばれる側の文言は元は同じファイルにあったため
- `h.py` の `MERGE` 既定を `<repo>/lib/merge.py` に。`extract.sh`(setup.sh からヒアドキュメントを
  切り出す道具)は不要になったので持ち込んでいない
- `.gitignore` に `tests/work/` `tests/real/` `__pycache__/` を追加(テストの作業ディレクトリ)

## 3.9 / 3.12 の両対応

`ast.parse` が両方で通ることを確認。`realdata.sh` と `fuzz*.py` は既定でマージ本体を
`/usr/bin/python3`(3.9.6)に投げ、オラクルだけ 3.12 で回す構成を維持。

## カバーできていない領域

- Ubuntu / WSL 実機での実行(mac + Homebrew python 3.12.13 での代替検証まで)
- `shellcheck` 未インストール(`bash -n` は OK)
- `tests/` を CI で回す仕組みは無い。手で走らせる前提(`tests/README.md` に手順)
