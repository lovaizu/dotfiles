# 3 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| `settings.json` is valid JSON and the only immediate-switch keybindings are the 3 `ctrl+alt+[/]/u` entries | OK | `python3 -m json.tool windows-terminal/settings.json` exits 0 (no BOM: file starts with bytes `7b 0a`). Remaining keybindings referencing switch actions: `ctrl+alt+[`→5976FD22, `ctrl+alt+]`→6B80DC5A, `ctrl+alt+u`→E1F939F — exactly 3. All 6 alias entries (`ctrl+shift+[`, `ctrl+shift+]`, `ctrl+shift+u`, `alt+shift+[`, `alt+shift+]`, `alt+shift+u`) are gone. | OK | QA が独立に json.load・全 keybinding 列挙で確認: 即時切替は ctrl+alt 系3件のみ、バイト列 0x14 0x5b/0x5d/0x75 も README と一致 |
| `⇧Enter`→`\n`, `ctrl+t` unbind, `ctrl+c`/`ctrl+v`, `ctrl+shift+f`, `alt+shift+d`, appearance (Gruvbox Dark / HackGen 13), and profile definitions unchanged | OK | Remaining 9 keybindings enumerated: 3× ctrl+alt above + `shift+enter`→DFCDAF06 (`\n`) + `ctrl+t`→`null` + `ctrl+c`/`ctrl+v`/`ctrl+shift+f`/`alt+shift+d` — matches README キー対応表 Win column + WT-specific keys. `git diff` (24 deletions, 0 insertions) touches only the `keybindings` array; `actions`, `profiles` (incl. defaults `Gruvbox Dark` / `HackGen Console NF` 13 and the Ubuntu overrides), `schemes` are byte-identical. | OK | QA が numstat(0 insertions / 24 deletions・keybindings 配列内のみ)と defaults 外観・profiles・schemes の不変を確認。コミット後の追加変更なし(HEAD と同一) |
| No action definitions left unused | OK | Vacuously satisfied — no action became unused, so nothing was removed. All 4 `actions` ids remain referenced: 5976FD22/6B80DC5A/E1F939F by the `ctrl+alt` bindings, DFCDAF06 by `shift+enter`. Programmatic set-difference `action_ids - referenced_ids` = NONE. | OK | QA がクロスリファレンス再実行: orphaned actions = 空、dangling references = 空 |

### Method applied

- **JSON validity**: `python3 -m json.tool windows-terminal/settings.json` → exit 0 ("json.tool OK"). File contains no `//` comments; `head -c 3 | xxd` shows `7b 0a 20` (plain `{`, no BOM), so json.tool is an appropriate check.
- **Keybinding enumeration**: Python `json.load` listed all 9 remaining entries with their action ids (see criterion 2 evidence) and cross-checked against README.md キー対応表 (Win column: `Ctrl+Alt+[` / `Ctrl+Alt+]` / `Ctrl+Alt+U` / `Shift+Enter` / `ctrl+t` 無効化) plus the WT-specific keys named in the criteria.
- **Unused-action check**: set difference between declared `actions[].id` and ids referenced from `keybindings[].id` (null excluded) → empty set.
- **Diff minimality**: `git diff --stat` = 1 file, 24 deletions, 0 insertions; hunks fall only inside the `keybindings` array; no reformatting or reordering of surviving entries.

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | 検証が「残存バインドの実値 × README 対応表」「action 参照整合」「diff 純度」を対象にしており目的に適合。QA の破壊試行(隠れ編集・残存別名・参照切れ・バイト列ドリフト)で欠陥ゼロ |

## Expert Reviews (axes the task needs)

### Craft Expert (coding)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | 純削除24行・挿入0、whitespace churn なし(--ignore-all-space 同値)、括弧/カンマ破損なし、コミットメッセージも逐一正確 |
| Consistency with existing style | OK | WT 生成の 4-space・brace 形式と元の並び順を維持。100755 の実行ビットは既存(本コミット由来でない) |

### Verification Expert (test/static-verification)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked | OK | 宣言された検証を全件再現(JSON/BOM・列挙照合・参照整合・diff 純度)、加えて parent→commit のプログラム的 set-diff で削除6件・追加0件・非 keybindings キーの byte 同値を確認 |
| Coverage (edge cases) | OK | 静的にカバー可能な範囲は網羅。WT 実機ロード(id:null unbind 形式含む)は #5 の次回 Win 同期確認に妥当に繰延 |

## Overall Verdict

- Self-check: OK
- QA: OK
- Design expert: N/A
- Craft expert: OK
- Verification expert: OK
- Ready to check off: Yes(3レビュー PASS、指摘は Info/Low の既存事項のみ。Ubuntu プロファイルは別途エスカレーション中で本タスクの criteria 外)
