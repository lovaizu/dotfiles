# #1 レビュー

| 指摘 | 対応要否 | 対応内容 |
|---|---|---|
| テーマの帰属が曖昧 — 端末の Gruvbox Dark と herdr UI の `gruvbox-light` が区別できない | 要 | README で層を分けて記述(`2836b23`) |
| herdr の GitHub URL が実在を確認できない | 要 | リンクを外してプレーン表記に |
| WT の Ubuntu プロファイルが `colorScheme: Dracula` / `size: 10` で `profiles.defaults` を上書きしている | #1 の範囲外(README の課題ではない) | ユーザーへエスカレーション。後に該当キーを削除 |

**この検証結果は暫定**: README は #8 で「値を持たない」形に書き直すため、値の照合を通った事実は
その時点で意味を失う。#8 の完了基準が新しい判定になる。
