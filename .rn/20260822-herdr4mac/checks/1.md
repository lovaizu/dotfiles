# 1 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| README に対応表があり、即時切替3操作・⇧Enter・⌃T パススルーについて Win / Mac 双方のキーと送信内容(herdr 列)が読み取れる | OK | README「キー対応表」の 5 行の表に previous/next workspace・next agent(`^T [` / `^T ]` / `^T u` + バイト値)、改行送信(`\n` 0x0a)、`⌃T` パススルー(Win: WT 既定無効化 / Mac: 設定不要)の各行があり、Win キー・Mac キー・送信内容の 3 列が揃っている | OK | 5 行すべてのキー・バイト値を config.toml / settings.json と照合し一致(0x14 0x5b/0x5d/0x75, 0x0a, ctrl+t unbind `"id": null`) |
| テーマ(Gruvbox Dark)・フォント(HackGen Console NF 13)と、両OSのセットアップ手順(フォントの入手方法含む)が記載されている | OK | 「テーマ・フォント」節に Gruvbox Dark(端末のカラースキームである旨を明記)と HackGen Console NF 13(Win/Mac のフォント名表記込み)。herdr 自体の UI テーマは `herdr/config.toml` の `[theme]`(`gruvbox-light`)で別管理と付記。「セットアップ」節に WSL/Mac 双方の `./setup.sh` 手順、Win は HackGen リリースページからの手動インストール、Mac は `font-hackgen-nerd` cask(なければ手動)を記載 | OK | WT defaults(Gruvbox Dark / HackGen Console NF 13)と一致。HackGen releases の zip 内容(`HackGenConsoleNF-*.ttf`)・cask 名を fact-check が実物確認。テーマの帰属(端末 vs herdr `[theme]` gruvbox-light)は修正ラウンド(2836b23)で明確化済み |
| 記載内容が steering の Acceptance criteria の対応表と矛盾しない | OK | Win `Ctrl+Alt+[`/`]`/`U` → `0x14 0x5b`/`0x5d`/`0x75`、Mac `⌃⌘[`/`]`/`U`、`⇧Enter`→`0x0a`、`⌃T` パススルー(Win 無効化・Mac 設定不要)が unified spec と一致。Win の別名 chord(ctrl+shift 等)は記載していない | OK | steering の対応表と全項目一致(即時切替は ctrl+alt のみ記載)。settings.json に残る別名 chord は #3 で削除予定、WT Ubuntu プロファイルの Dracula/10pt 上書きはユーザーへエスカレーション中(README の criteria には非抵触) |

### Method applied (claims checked against sources)

- herdr prefix `ctrl+t`、`prefix+[`=previous_workspace、`prefix+]`=next_workspace、`prefix+u`=next_agent、`prefix+y`=copy_mode → `herdr/config.toml` 15–20 行で確認
- 送信バイト列 `[` / `]` / `u` / `\n`、`shift+enter`、`ctrl+t` の unbind(`"id": null`)→ `windows-terminal/settings.json` actions/keybindings で確認
- テーマ Gruvbox Dark・フォント HackGen Console NF size 13 → `windows-terminal/settings.json` profiles.defaults で確認
- `./setup.sh` が herdr config を `~/.config/herdr/config.toml` に配置し WT settings.json を LocalState に配置(既存はバックアップ)→ `setup.sh` で確認
- Mac 側(iTerm2 Dynamic Profile、cask、デフォルトプロファイル手動)は未実装の将来仕様であり、タスク指示の unified spec に準拠(仕様として記載)
- herdr の GitHub URL は検証不能のためリンクを削除しプレーン表記に変更

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | QA が全キー・バイト値・テーマ・フォント・setup 手順を実ソース(config.toml / settings.json / setup.sh)と突合。初回レビューで Ubuntu プロファイル上書き(Medium)とテーマ帰属(Minor)を検出 → 後者は修正ラウンドで解消、前者はエスカレーション。再レビュー PASS |

## Expert Reviews (axes the task needs)

### Craft Expert (writing)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | 見出し階層・表・コードフェンスとも valid。構成(目的→方針→対応表→外観→セットアップ)が読者に適う。初回指摘 4 件(テーマ帰属・⌃T 表記・方針文の位置・表導入文)は修正ラウンドで解消を確認 |
| Consistency with existing style | OK | Win/Mac/OS 中立のキー表記が規律どおり使い分け。用語(herdr・プレフィックス・即時切替)一貫。残ニット(改行送信の表記ゆれ)は修正不要と判定 |

### Verification Expert (fact-check)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (claims verified) | OK | 全 claim をソースと突合(バイト値は ASCII 計算、HackGen zip は実ダウンロード、cask は formulae.brew.sh、DynamicProfiles パスは公式 doc)。初回 NG(テーマ矛盾)は 2836b23 で解消を確認し再レビュー PASS |
| Coverage (claims) | OK | 変更 claim 全件+既存バイト値のスポットチェックを網羅。Mac 側は未実装の将来仕様として区別(#2・#4 で実装、実機確認は #5) |

## Overall Verdict

- Self-check: OK
- QA: OK
- Design expert: N/A
- Craft expert: OK
- Verification expert: OK
- Ready to check off: Yes(全レビュー PASS。WT Ubuntu プロファイルの Dracula 上書きは #1 の criteria 外としてエスカレーション中 — 判断結果は #3 またはREADME追記で反映)
