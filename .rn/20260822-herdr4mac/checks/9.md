# #9 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| `merge_json` が、配置先にしか無いキーを保持したまま dotfiles 側のキーを反映し、結果が有効な JSON である。ネストしたオブジェクト(`hooks` / `profiles.defaults` など)でも同様に動く | OK | `scratchpad/fix9c/suite.py`(116 アサーション)の `R-json-nested` が PASS: `model` は src の `opus` に、dst 固有の `localOnly` は残り、`hooks.Stop`(dst 固有)と `hooks.SessionStart`(src)が両方入る。`R-jsonc` は `//` コメント + 末尾カンマ入りの dst を受理して `{"a": 1, "b": 3}` を出し、警告文が **`comments and trailing commas`** に直っていることも固定(⑧)。実データ: `scratchpad/fix9c/realdata.sh` が実物の `windows-terminal/settings.json` を src、別マシン想定 dst(`//` コメント + 末尾カンマ + WSL プロファイル + `launchMode` + `opacity`)にマージ → `valid JSON: yes` / `dotfiles defaults applied: {'face': 'HackGen Console NF', 'size': 13}` / `dst-only launchMode kept: maximized` / `dst-only defaults.opacity kept: 90`、2 回流して `WT IDEMPOTENT: yes`。JSON 経路は 3.9.6 と 3.12.13 で出力バイト一致(`cmp_py.py`: `identical on both interpreters: 16 / 16`) | | |
| `merge_json` が `profiles.list` を `guid` 単位で突き合わせ、dotfiles に無い guid のプロファイルを残す | OK | `suite.py` の `R-json-guid` が PASS: dst `{aaaa}`/`{bbbb}`、src `{AAAA}`/`{cccc}` → 3 件、`{AAAA}` は case-fold で `{aaaa}` に一致して**再帰マージ**(dst 固有の `startingDirectory` と `font.size` が残り、src の `font.face` が入る)、`{bbbb}` は温存、`{cccc}` は追加。実データ: `realdata.txt` の `profiles: 7` / `dst-only WSL profile kept: True` / `its startingDirectory: //wsl$/Ubuntu-22.04/home/kiyo` | | |
| `merge_toml` が、配置先にしか無いキー・コメント・行順を保ったまま dotfiles 側のキーを反映し、結果が herdr に読める TOML である(`herdr config check` が通る) | OK | `suite.py` の `R-toml-basic`(先頭コメント・dst 固有キー `mine`・**手書きの行末コメント** `# hand set` が残り、値だけ `gruvbox` になり、新規キーが入る。2 回目でバイト一致)、`R-multiline`(複数行文字列の値だけ差し替え)、`1-crlf-*`(CRLF 保持)、`2fp-*` 6 件(②の偽陽性回帰 = `[ui.toast]`→`[ui]` の合法順、dotted key と table の共存、別テーブルの同名キー、`a."b.c".d`、`a.b = 1` → `[a.c]`、`[a.b]`→`[a]`)、`7-comment-block`(⑦)が PASS。**実データ**: `realdata.sh` が実物 `~/.config/herdr/config.toml` の**コピー**にマージ → `herdr config check` が前後とも `config: ok` / rc=0。手書き行末コメント付き `dark_name = "nord" # hand set, keep` は値だけ `gruvbox` に戻って**コメントが残り**(`12:dark_name = "gruvbox" # hand set, keep`)、機械固有セクション `[machine]`(直上コメント付き)も残る。負のコントロール(壊れた TOML を同じ scratch に置く)で `config: issues found` / `TOML parse error` になり `XDG_CONFIG_HOME` が効いていることを確認。2 回流して `IDEMPOTENT: yes`。実ファイルは md5 `74ce2a97feb635ff6d008ae89e0de9e2` / mtime `1787533047`、`~/.claude/settings.json` は md5 `09a62cf2bde201d0da4ea31ee259474f` / mtime `1787534935` が前後で不変。**ファズ**: 実 config の 1 バイト変異 **8487 通**(`fuzzmut.py`、22 種の置換 + 1 バイト削除。うち UTF-8 として読める 8118 件を実行)で「rc=0 なのに出力が無効」**0 件**、「rc=0 なのに dst が tomllib 不正」**0 件**(`mut_after.txt`)。修正前は同じ入力で 211 件 / 212 件(`mut_before.txt`)。ランダム valid TOML 690 対(`fuzzrand.py`)で `invalid_out: 0` / `nonidem: 0` / `lost: 0`(dst 固有キーの消失なし) | | |
| 配置先ファイルが存在しない場合と、内容が壊れている場合のどちらでもエラー終了せず、dotfiles 側の内容で復旧する | OK | 不在: `R-toml-fresh` / `R-toml-empty-dst` が PASS(rc=5、`Installed` と出し、空ファイルは「無い」扱いで警告なし)。破損: `3-unterminated` / `3-junk-line` / `3-no-value` / `3-dup-key` / `3-dup-table` / `3-key-and-table` / `3-unbalanced` / `3-control-char` / `2-broken-dst` の 9 件が rc=3 で src の内容に復旧(③の修正後も**本当に壊れた dst は従来どおり復旧する**ことの回帰)。逆に「自前パーサが読めなかっただけ」の `3-leap-second` / `3-escape-e` / `3-time-no-seconds` / `3-array-of-tables` は rc=4 で **dst をバイト単位で無変更**のまま続行(③)。エラー終了しないことは `shell.sh` が `set -euo pipefail` 下で実測: dst 破損 × 書き込み不可(④)、dst も親ディレクトリも無し(⑤)のいずれも `REACHED_END` に到達し、呼び出し元は exit 0。④は WARNING が**1 個だけ**で「置き換えました」と言わない(修正前は矛盾する 2 個)、⑤は「could not be written / no file there to fall back on / this machine has no settings for it at all」+ 結果行 `Could not install ...`(修正前は「was left as it is」「it still holds exactly what it held before」)。⑥は `backup_then_copy` で cp が失敗すると `cp: ... is a directory (not copied).` が **stderr に出てから**停止する(修正前は無言)。`setup.sh` 全体を `HOME` 差し替えで通し実行 → exit 0、2 回目も exit 0(`sandbox.sh`) | | |
| マージ処理が `jq` / `tomllib` / その他の外部依存を必要とせず、mac の python3 3.9 と Ubuntu の python3 の双方で動く | OK | 埋め込み python の import は `datetime json os re stat sys tempfile` のみ(すべて標準ライブラリ)。`tomllib` / `subprocess` / `os.system` / `.popen` / `jq` の出現数 0(`grep -c` = 0)。`/usr/bin/python3`(3.9.6)で `py_compile` が通り、116 アサーションが `PASS=116 FAIL=0`。Ubuntu の代わりに Homebrew python 3.12.13 を `MERGE_PY` に据えて同じ 116 件を実行 → `PASS=116 FAIL=0`、さらに `cmp_py.py` が 16 ケース(TOML 10 / JSON 6、正常系・破損・不在・CRLF・複数行文字列・タブインデント・うるう秒・`[[...]]`)で rc・stdout・ディスク上のバイトが 3.9.6 と 3.12.13 で**完全一致**。`bash -n` OK。**`shellcheck` は未インストールのため実行できず**(`command -v shellcheck` が空)、手動レビューのみ。**未カバー**: 実際の Ubuntu / WSL 上での実行(3.12 での代替検証まで) | | |
| 書き込みが原子的で、途中で中断しても配置先が半端な内容にならない | OK | 今回の変更は書き込み経路の**順序**だけ(`report_replaced` を `write_atomically` の後ろへ)で、`write_atomically` 自体は無変更。前回の W 群 6 件(内容置換 / mode 継承 / temp が同一ディレクトリ / temp が残らない / rename 直前の `KeyboardInterrupt` で旧内容のまま / その際も temp なし)と `scratchpad/fix9b/atomic.py` の SIGKILL 40 回(`half-written or invalid dst: 0`)がそのまま有効。今回は加えて④の経路(書き込みが `Permission denied` で失敗)で dst が**バイト単位で無変更**であること、かつ「置き換えた」と報告しないことを `shell.sh` で実測。保証の射程は docstring に明記(プロセスの中断に対してであって電源断ではない — ディレクトリの fsync はしていない) | | |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | 3巡すべてで独立にハーネスを組み直し、実装者のスイートを流用せず検証。オラクルに 3.12 の `tomllib` と実 herdr 0.7.4 を使い、負のコントロールで `XDG_CONFIG_HOME` が効いていることを毎回実証。最終巡は構造パターン総当たり 12,863 件で偽陽性・偽陰性ゼロ、ファズ 26,082 件(実 config の1バイト変異 11,808 + ランダム valid TOML 1,500 + settings.json 変異 5,151 + src 側変異 7,623)で「rc=0 なのに出力が無効」「rc=0 なのに dst が不正のまま」「rc=3 が valid な dst を破棄」がいずれも 0 件、rc=0 の 6,513 件全部で冪等。検証は目的(設定を失わせない)に照らして意味があり、形骸化していない。総合 conditional pass — ブロッカーは深いネストでの `RecursionError` 未捕捉 1 件。 |

## Expert Reviews (axes the task needs)

### Design Expert (tasks that produce or revise structure/approach)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | NG | 3原則(src が壊れている→中断 / dst が壊れている→復旧 / マージできない→触らず続行)自体は導出可能で、`merge_config` が 0/3/4/5/6 を吸収して 0 を返すため呼び出し側の責務は増えていない。ただし `Unsupported` と `ValueError` の線が「壊れているか」ではなく「自前パーサが読めるか」で引かれており、`2020-13-45` / `0123` / `1_` / `hello` のようにどの TOML でも不正な値が rc=4 に落ちて dst が壊れたまま永久に温存される(復旧されない)。また rc=4 / no-op でもバックアップを取るため「Backed up」と「Nothing was written」が同時に出て、再実行のたびに増殖する。規模は setup.sh 1342行のうち埋め込み python が 1165行(87%)、TOML 節が 729行 — 対象は herdr の12キーの config.toml で、値検査層(約600行)は上記のとおり健全でも完全でもない。 |
| System-wide integrity (interfaces, cross-doc consistency) | NG | シグネチャは `backup_then_copy` と同形で #7 / #10 は呼び出し名の差し替えだけで済む(OK)。ただし design.md §4.7 の「結末は3つ…終了コードはこの3つを5通りに割ったもの」が内部矛盾(成功が3結末に無く、rc=1 も5通りに無い。実際は4系統6コード)。§4.7 の自己検査の主張が実装の既知の非検査(サロゲート、64bit 超、コメント内の制御文字)と食い違い、python の docstring では自認しているのに設計文書に上がっていない。`DATE_RE` などの `\d` が Unicode 数字を受理して無効な TOML を rc=0 で通す。rc=4 の文面が理由別に分かれておらず、未対応構文が理由のときに「対処して再実行せよ」は誤案内(永久に変わらない)。 |

### Craft Expert (coding / writing / visual — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | NG | bash 側(`set -euo pipefail` 下の errexit 抑止、`local`、クォーティング、終了コードの出し分け)と原子書き込み・mode 継承・symlink 追従・3.9 互換はいずれも健全で、`bash -n` も通る(`shellcheck` は未導入)。NG の根拠は `toml_atom_end` が空トークンや不正な数値・日付まで `Unsupported` に倒すこと — `toml_value_end` 自身の docstring が「A missing value is a ValueError」と約束しているのに実装が守っておらず、`b =` は rc=3 なのに `b = # x` は rc=4 と、同じ「値が無い」がコメントの有無で分岐する。あわせて `RecursionError` が未捕捉で深いネストの dst が setup 全体を落とす。`cp "$src" "$dst" 2>/dev/null` が残っており警告が理由を示さない(`backup_file` 側だけ撤去された)。 |
| Consistency with existing style | NG | インデント・関数直上コメント・`WARNING:` + 2スペース継続行・`echo` の語調はいずれも既存 setup.sh に揃っており、`installed()` への集約で重複も1点解消(ここは OK)。NG は 実装とコメント/docstring の食い違いが4ラウンド連続で新規に出ていること: モジュール docstring が「警告のあとに置換」のままで今回の修正④自体を取り残している、`toml_set_value` の「src supplies the value and nothing else」は偽(`=` 後の空白も src が供給)、`Unsupported` を実際に投げる唯一の関数 `toml_atom_end` に docstring が無い、`toml_parse` の「re-joining them reproduces the input byte for byte」は `ensure_newline` 後の文字列にしか当てはまらない。コメント密度は bash 45%→35%、python 25%→24% と既存部(約15%)に近づいたが、なお重い。 |

### Verification Expert (test / fact-check / dry-run — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | OK | 3巡すべてで、埋め込み python を切り出した `merge.py` と関数定義だけの `lib.sh`、`set -euo pipefail` 下で末尾に到達マーカーを出す `drv.sh` の3層ハーネスを自作。`tempfile.mkstemp` / `os.replace` を差し替えて原子性を内側から観測、30MB 書き込み中の SIGKILL で dst が旧内容のままであることを実測。実 herdr バイナリと 3.12 `tomllib` を独立オラクルとして使い、負のコントロールを毎回取得。保護対象2ファイルの md5 と mtime を各スイート末尾で再確認し、前後で完全一致。主張と実測の食い違いは各巡で具体的に提示されている。 |
| Coverage (edge cases / claims / steps) | NG | カバレッジ自体は非常に広く(GWT で 350 件超 + ファズ 5,500〜26,000 件、3.9.6 と 3.12.13 で 333 ケースのバイト一致、rc 0/3/4/5/6 の全経路を `set -e` 下で実測、symlink の相対・dangling・ループ、JSONC の文字列内 `//` `/*`、複数行文字列内の擬似キー行まで)、前巡の欠陥①②は完全に解消しデータ損失方向はゼロ。NG の根拠はカバーの穴が実害として残っていること: `toml_check_body` が文字列の中身だけを見てコメント行を無検査で通すため、コメント内の生の制御文字(`\x0b` `\x0c` `\x01` `\x7f` `\x00`、裸の `\r` の6形)を含む dst が rc=0「Merged」で通り、実 herdr が `config parse error ... ; using defaults` で拒否する。ファズ 5,500 件の違反21件はすべてこのクラス。加えて⑦の修正が root セクションに届いておらず、top-level 新規キーが次セクションのコメントを跨ぐ(`herdr/config.toml` の `onboarding` が top-level なので実際に踏む経路)。 |

## Overall Verdict

- Self-check: OK
- QA: OK (conditional — unresolved 1)
- Design expert: NG
- Craft expert: NG
- Verification expert: NG (conditional pass — unresolved 2)
- Ready to check off: No — 修正3巡の上限に到達。未解決の Valid 指摘をユーザーに escalate 済み

---

## 検証の記録(Method の適用)— 3 巡目

検証先行。`scratchpad/fix9c/` に、setup.sh からヒアドキュメントの中身をそのまま切り出す
`extract.py` と、setup.sh と同じ引数で呼ぶ `h.py` を置き、①〜⑧それぞれについて**修正前に落ちる**
テストを先に書いてから実装に手を入れた。`suite.py` は 3.12 で走り(オラクルの `tomllib` のため)、
マージ本体は既定で `/usr/bin/python3`(3.9.6)に投げる。

| ファイル | 内容 | 修正前 | 修正後 |
|---|---|---|---|
| `fix9c/suite.py` | ①〜⑧の回帰 + 既存の正常系。116 アサーション | `PASS=86 FAIL=38`(`before.txt`。`baseline.sh` が `merge_before.py` = `git show HEAD:setup.sh` から切り出したものに対して実行) | `PASS=116 FAIL=0` |
| 同上(python 3.12.13) | `MERGE_PY=/opt/homebrew/bin/python3.12` | — | `PASS=116 FAIL=0` |
| `fix9c/shell.sh` | ④⑤⑥ を `set -euo pipefail` 下のドライバで実測(WARNING の個数・文面・dst のバイト・stderr) | `PASS=6 FAIL=7` | `PASS=13 FAIL=0` |
| `fix9c/fuzzmut.py` | 実 `~/.config/herdr/config.toml` の**コピー**の 1 バイト変異 **8487 通**(22 種の置換 + 削除、うち UTF-8 として読める 8118 件)を dst に。オラクルは 3.12 の `tomllib` | 「rc=0 なのに出力が無効」211 件 / 「rc=0 なのに dst が不正」212 件、終了コード `{0: 2463, 3: 5638, 4: 17}` | **どちらも 0 件**、終了コード `{0: 2251, 3: 5246, 4: 621}`。rc=0 の件数 2251 は valid な dst の件数 2251 と**一致**する |
| `fix9c/fuzzrand.py` | ランダムに組み立てた valid TOML 690 対。出力の妥当性・冪等・dst 固有キーの生存を検査 | — | `invalid_out: 0` / `nonidem: 0` / `lost: 0` |
| `fix9c/cmp_py.py` | 16 ケースを 3.9.6 と 3.12.13 の両方に流し、rc・stdout・ディスク上のバイトを比較 | — | `identical on both interpreters: 16 / 16` |
| `fix9c/realdata.sh` | 実 herdr config のコピー × `herdr config check`(負のコントロール付き)、実 WT settings.json × 別マシン想定 dst。2 回流す | — | `config: ok` 前後とも rc=0 / `IDEMPOTENT: yes` / `WT IDEMPOTENT: yes`。実ファイルは md5・mtime とも前後で不変 |
| `fix9c/sandbox.sh` | `HOME` を scratch に向けた setup.sh の通し実行(呼び出し側は無変更なので非退行チェック) | — | exit 0、2 回目も exit 0、配置物は従来どおり |
| `fix9c/density.py` | コメント / docstring 密度 | python 25%(248 行)/ bash 48%(58 行) | python **24%**(250 行。コードは 739 → 776 行)/ bash **38%**(39 行) |

`bash -n` OK。`shellcheck` は未インストールのため実行できず。`# herdr (common)` 以降(呼び出し側)は
HEAD と**バイト一致**であることを `cmp` で確認済み。

### 3 巡目の指摘 ①〜⑩ の対応と、それを裏づけるテスト

| # | 対応 | 修正前に落ちたテスト |
|---|---|---|
| ① | `toml_trim()` を新設し、`toml_key_path` / `toml_header_path`(本体と `rest`)/ `toml_split_value` の `rest` / `toml_parse` の行判定の `strip()` をすべて置き換えた。行末の `\r\n` または `\n` を 1 つだけ落とし、あとは `" \t"` だけを剥がす。あわせて「CRLF の途中で終わっているファイル」(末尾が裸の `\r`)を `toml_parse` の入口で ValueError にした — `ensure_newline` が LF を足して健全に見せてしまうため | `1-cr-header` / `1-cr-dotted` / `1-ff-key` / `1-vt-key` / `1-nbsp-key` / `1-ff-after-value` / `1-ff-line` / `1-nbsp-line` / `1-cr-eof` / `1-cr-eof-crlf`(各 2 アサーション)。回帰として `1-crlf-ok` / `1-crlf-blank` / `1-no-final-newline` |
| ② | `toml_check` に `tables & implied` の突き合わせを追加(ヘッダが先でも dotted key が先でも同じ規則になる)。`toml_can_append` は src のキーが暗黙に作る親テーブルが dst の宣言済みセクションにあれば追記をやめる。`toml_add_key` の探索順を `parents → sections[path[:-1]] → sections[src の path] → implied` に入れ替え、空の `[a.b]` があればそこへ入れる | `2-broken-dst`(rc=3 になること)/ `2-empty-table` / `2-deep` / `2-no-a` / `2-toast`。**偽陽性の回帰** `2fp-order` / `2fp-dotted-table` / `2fp-same-name` / `2fp-quoted-dot` / `2fp-dotted-then-table` / `2fp-header-then-parent` は修正前後とも PASS |
| ③ | 「値として読めない」を `Unsupported` に倒した(`toml_atom_end` の不明トークン、`toml_unescape` の未知エスケープ 3 箇所)。`ValueError` は「文書そのものが不正」に限る。判断基準は `Unsupported` と `toml_value_end` の docstring に明記。`TIME_RE` / `DATETIME_RE` は**緩めなかった** — tomllib は `07:32` もうるう秒も拒むので、受理して書き戻すと「rc=0 なのに出力が無効」を自分で作ることになる。読めないものは触らない側(rc=4)に倒すことで、dst 破棄の偽陽性だけが消える | `3-leap-second` / `3-escape-e` / `3-time-no-seconds`(各 2 アサーション)。**結末2/4 の分布**: 8118 件のファズで exit 3 が 5638 → 5246、exit 4 が 17 → 621。exit 4 の 604 件増加分がそのまま「壊れていないのに破棄されていた dst」+「①で新たに検出できるようになった構文」 |
| ④ | `merge_text` が報告を出さず理由を返し、`merge_file` が `write_atomically` の**後**に `report_replaced` を呼ぶ。バイナリ dst の経路も同じ順に | `shell.sh` の「4: exactly one WARNING」「4: does not claim the file was replaced」 |
| ⑤ | `DST_KEPT` を `merge_file` が立て、`report_unmerged` が dst 不在なら別の文面と新しい終了コード `MISSING = 6` を返す。setup.sh 側は `6) Could not install ... -- see the warning above` | `shell.sh` の「5: does not talk about a file that is not there」「5: does not claim the old contents survive」「5: the result line does not say 'Left ... as it is'」 |
| ⑥ | `cp "$dst" "$bak" 2>/dev/null` から `2>/dev/null` を外し、コメントを実態(`merge_config` は拾って続行、`backup_then_copy` は拾わないので `set -e` で止まる)に合わせた | `shell.sh` の「6: and says why on stderr」「6: the reason comes from cp」 |
| ⑦ | `toml_add_key` の挿入位置の後退条件に「コメント行」を追加。セクションが空行とコメントだけのときは末尾に戻す(先頭コメントの上に押し込まないため) | `7-comment-block [above the comment]`。回帰 `7-only-comments` |
| ⑧ | 「3つ」の記述を削除(終了コードの列挙は「five things」、`case` の直上は「One line per exit code」で数を持たない)。`or a date that exists` を撤回し、うるう秒を見ていないことを `toml_temporal_ok` に明記。stdout の主張に「壊れた src の中断だけは stderr」を追記。JSONC の警告を `comments and trailing commas` に。`read_text` に BOM が書き戻しで落ちる旨を追記 | `8-outcome-count-consistent` / `8-outcome-count-stated` / `8-no-date-overclaim` / `8-stderr-noted` / `8-trailing-commas` / `8-backup-cp-loud` / `8-bom-noted` / `R-jsonc [warns about both]` |
| ⑨ | `design.md` §4.7 に「2と3の境界」(文書が不正 / 自前パーサの語彙の外)と「報告は書き込みの後」を追記し、「値の構文エラーがあれば書かない」がマージ**出力**の話であることを明示。**「マージには削除の概念が無い」**と、JSON 側が `json.dumps` 形に正規化されて初回だけ diff が全面化する点を追記。Alternatives の「理解できる形だけを扱い、外れたら書かずに警告して続ける」に、外れる範囲が `[[...]]` だけでないことと、dst を捨てるのは「どう読んでも不正」のときだけであることを追記 | (文書。`design.md` の diff) |
| ⑩ | `index["keys"]` を「パス → item」に変え `for target in found:` を削除。`toml_index` をループの外で 1 回だけ組み、`toml_index_item` で差分更新する(`toml_append_section` も index を受け取る)。`toml_combine` の `dst_text` の注記を 1 行に。`read_text` に BOM の一言。`Installed $dst` を `installed()` に集約(3 箇所)。**コメントを削った**: bash 48% → 38%(58 → 39 行)、python 25% → 24%(248 → 250 行だがコードは 739 → 776 行)。`merge_json` / `merge_toml` 直上は 21 行 → 12 行 | `density.py` の実測、`8-backup-cp-loud` ほか |

### カバーできていない領域

- 実際の Ubuntu / WSL 上での実行(Homebrew python 3.12.13 での代替検証まで)、WT の実機配置(#10 の範囲)
- `shellcheck` が未インストールのため実行できず(`bash -n` は OK)
- `[[array of tables]]` は非対応のまま(src にあれば中断、dst にあれば触らず続行)
- 値の検査は構文まで。64bit に収まらない整数、サロゲート半分を指す `\u` エスケープは見ていない。
  うるう秒と TOML 1.1 の値は「読めない」扱いで dst を触らずに続行する(受理はしない)
- `hooks.<Event>` 配列や WT の `actions` / `schemes` の id 単位マージ、python の `lib/merge.py` 切り出し、
  値検査を対応構文の明示列挙に作り変えることは、ユーザー確認中の未決事項なので着手していない
- 呼び出し側の差し替え(#7 / #10)は範囲外。`# herdr (common)` 以降が HEAD とバイト一致であることを確認済み

---

## 未解決の Valid 指摘(修正3巡の上限に到達。coordinator が記録、ユーザーに escalate)

3巡すべてで4軸(QA / Design / Craft / Verification)を回し、各巡で挙がった Valid 指摘は
修正した。3巡目の再レビューで新たに/なお残ったものを、修正せずここに記録する。

**データを失う方向の欠陥はゼロ**(ファズ 26,082 件で「rc=0 なのに出力が無効」「rc=3 が
valid な dst を破棄」ともに 0 件、dst 固有キーの喪失 0 件、冪等)。残りは「壊れた dst を
見逃す」「壊れた dst を復旧しない」「文書と実装の食い違い」の3系統。

| # | 指摘 | 指摘した軸 | 完了基準 | 規模 |
|---|---|---|---|---|
| U1 | コメント行が無検査。コメント内の生の制御文字(`\x0b` `\x0c` `\x01` `\x7f` `\x00`、裸の `\r`)を含む dst が rc=0「Merged」で通り、実 herdr が `config parse error ... ; using defaults` で拒否する。`toml_check_body` は文字列の中身しか見ていない。ファズ 5,500 件の違反 21 件はすべてこのクラス | Verification | 3 / 4 | 1 箇所 |
| U2 | `RecursionError` が未捕捉。3000 段ネストの dst で traceback + rc=1 → `set -e` で setup 全体が中止。かつコード上「listed 外の終了コード = 壊れた src = dotfiles のバグ」と規定しているため原因の帰属が誤る | QA | 4 | 数行 |
| U3 | `Unsupported` / `ValueError` の線が「壊れているか」ではなく「自前パーサが読めるか」で引かれている。`2020-13-45` / `25:00:00` / `0123` / `1_` / `hello` はどの TOML でも不正だが rc=4 に落ち、dst が壊れたまま永久に温存される(復旧しない)。`toml_value_end` の docstring が「A missing value is a ValueError」と約束しているのに `b = # x` は rc=4(`b =` は rc=3)| Craft / Design(Valid)、QA / Verification(改善提案)| 4 | 数行 |
| U4 | `DATE_RE` / `TIME_RE` / `DATETIME_RE` の `\d` が Unicode 数字を受理。`when = ١٩٩٩-٠١-٠١` が rc=0 で通り無効な TOML になる | Design | 3 | `[0-9]` に置換 |
| U5 | ⑦の修正が root セクションに届いていない。top-level 新規キーが次セクションのコメントを跨ぐ。`herdr/config.toml` の `onboarding` が top-level なので実際に踏む(出力は valid、表示上のみ)| Verification | 3 | 1 分岐 |
| U6 | rc=4 / no-op でもバックアップを取る。「Backed up ...」と「Nothing was written」が同時に出る(2巡目に直した矛盾と同型の再発)。解決しない dst に対して再実行のたびに同一内容の `.bak` が増える | Design / QA | — | 呼び出し位置 |
| U7 | rc=4 の文面が理由別に分かれていない。「Deal with the reason above and re-run ./setup.sh」は OSError 起因では正しいが、`Unsupported` 起因では永久に変わらないので誤案内 | Design / Craft | — | 文面 |
| U8 | 実装とコメント/docstring の食い違いが4ラウンド連続で新規に出る。今回: モジュール docstring が「警告のあとに置換」のまま(修正④自体を取り残し)、`toml_set_value` の「src supplies the value and nothing else」が偽(`=` 後の空白も src 由来)、`toml_atom_end` に docstring なし、`toml_parse` の「byte for byte」が不正確 | Craft | — | 文言 |
| U9 | `cp "$src" "$dst" 2>/dev/null` が残存(`backup_file` 側だけ撤去された)。直後の `WARNING: $dst could not be written.` が理由を示さない | Craft | — | 1 行 |
| U10 | design.md §4.7 の「結末は3つ…5通りに割ったもの」が内部矛盾(成功が3結末に無く rc=1 も5通りに無い。実際は4系統6コード)。§4.7 の自己検査の主張が実装の既知の非検査(サロゲート、64bit 超、コメント内制御文字 = U1)と食い違う。python の docstring では自認しているのに設計文書に上がっていない | Design | — | 文書 |

### 現行 dotfiles で発火するか

- **発火する**: U5(`onboarding` が top-level)、U6(毎回)、U7 / U8 / U9 / U10(文言・文書)
- **dst 次第で発火する**: U1(アプリや手編集がコメントに制御文字を入れたとき)、U3(dst の値が壊れたとき)
- **発火しない(潜在)**: U2(病的入力)、U4(Unicode 数字を書いたとき)

### 却下した指摘(Invalid / スコープ外)

- 「TOML 1.0 では時刻の秒が省略可」— 誤り。省略可になったのは TOML 1.1(Craft 初回)
- 「`herdr config check` は引数を無視し常に `~/.config/herdr/config.toml` を読む」— QA と Verification が
  負のコントロール付きで反証(Craft 初回)
- 混在改行のファイルで書き換えた行だけ多数派側に寄る / JSON のレイアウトが rc=3・rc=5 の次回に 1 回だけ
  揺れる(2 回目で収束、内容は同一)— 病的入力または表示上のみ
- 64bit を超える整数、サロゲート半分を指す `\u` — コード側で明記済みの割り切り
- symlink ループで通常ファイルに置換される — 壊れたリンクで実害なし
- ディレクトリの fsync / 電源断耐性 — 完了基準は「途中で中断」まで
- `BACKUP_DIR` の世代管理と容量、同時書き込み(read-modify-write の窓)— 設計上の非目標として宣言済み
- Ubuntu / WSL 実機、python 3.8、Windows Terminal 実機、iTerm2 が `.` 始まりを無視するか、shellcheck —
  環境制約。steering の Assumptions が実機確認をユーザー側としている
- 呼び出し側の差し替え、`MERGE_BY_ID` の範囲、python の `lib/merge.py` 切り出し、値検査をやめて
  対応構文の明示列挙に作り変える案 — #7 / #10 の範囲、またはユーザー確認中
