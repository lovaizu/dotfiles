# #14 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| 両OSの端末設定でフォントサイズが 14 になっている。README は数字を持たない(#8 の規則) | OK | `iterm2/herdr.json` の `Normal Font` を `HackGenConsoleNF-Regular 14` に、`windows-terminal/settings.json` の `profiles.defaults.font.size` を 14 に変更(diff は各1行のみ、書式・キー順は不変)。両ファイルとも `python3 -m json.tool` で valid JSON を確認。WT の settings.json にコメントは無く(`//` `/*` の grep ヒットは `$help` / `$schema` の URL のみ)純 JSON。README 33行目のフォント行からサイズを削除し、値 = フォント名、設定箇所 = `Win: profiles.defaults の font / Mac: iterm2/herdr.json の Normal Font` という参照だけを残した。README 全体を `13` / `14` / `pt` / `size` で grep し、残るヒットはキーコード `0x14` のみでサイズの数字は無い。README のフォント名・参照先は実ファイル(WT は `profiles.defaults.font.face = "HackGen Console NF"`、iTerm2 は `Normal Font`)と照合済み。 | | |
| iTerm2 で実際に 14pt で表示されることをユーザーが確認している | NG | 配置までは完了。`./setup.sh` が exit 0 で終了し、`~/Library/Application Support/iTerm2/DynamicProfiles/herdr.json` の `Normal Font` が `HackGenConsoleNF-Regular 14` になっていること(および配置後ファイルが valid JSON であること)を確認済み。実際の見た目が 14pt であることはユーザーの実機確認が必要なため、**ユーザー確認待ち**。 | | |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | | |

## Expert Reviews (axes the task needs)

### Craft Expert (writing)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | | |
| Consistency with existing style | | |

### Verification Expert (fact-check)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | | |
| Coverage (edge cases / claims / steps) | | |

## Overall Verdict

- Self-check: OK(ただし2つ目の基準はユーザーの実機確認待ち)
- QA:
- Design expert: N/A
- Craft expert:
- Verification expert:
- Ready to check off:
