# #14 Completion Check

`iterm2/herdr.json` の `Normal Font` と `windows-terminal/settings.json` の
`profiles.defaults.font.size` を 14 にした(`b91462a`)。README はサイズの数字を持たない。

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| 両OSの端末設定でフォントサイズが 14 になっている。README は数字を持たない(#8 の規則) | OK | `iterm2/herdr.json` の `Normal Font` = `HackGenConsoleNF-Regular 14`、`windows-terminal/settings.json` の `profiles.defaults.font` = `{"face": "HackGen Console NF", "size": 14}`。README にサイズの数字は無い(フォント名と設定箇所の参照のみ。名前の削除は #8 の担当) | | |
| iTerm2 で実際に 14pt で表示されることをユーザーが確認している | 未 | 配置は済んでいる — `./setup.sh` が exit 0 で `herdr.json` を配置し(退避は `~/.local/state/dotfiles-backups/herdr.json.20260905205855.bak`)、`com.googlecode.iterm2.plist` の `New Bookmarks` の `herdr` が `Normal Font` = `HackGenConsoleNF-Regular 14` を保持していることを確認した。**見え方の判断はユーザー本人にしかできないので確認待ち** | | |

## コーディネータの独立確認

専門家レビューは回していない(`steering.md` Rules)。

| 確認したこと | 結果 |
|---|---|
| 2ファイルのサイズ | どちらも 14 |
| README に数字が残っていないか | 残っていない |
| 配置後に iTerm2 が読み込んだか | `New Bookmarks` の `herdr` が 14 を保持。同じ配置で #11 の4キーも数秒で反映された(再起動なし) |

## Overall Verdict

- Self-check: 完了基準1 OK / 完了基準2 はユーザー確認待ち
- QA: N/A(Rules により専門家レビューを回していない)
- Craft expert: N/A(同上)
- Verification expert: N/A(同上)
- コーディネータの独立確認: OK(基準1)
- Ready to check off: No — ユーザーが 14pt の見え方を確認するまで
