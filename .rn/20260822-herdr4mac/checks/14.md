# #14 レビュー

`iterm2/herdr.json` の `Normal Font` と `windows-terminal/settings.json` の
`profiles.defaults.font.size` を 14 にした(`b91462a`)。両ファイルとも diff は各1行、valid JSON。
README からはサイズの数字を落とし、フォント名と設定箇所の参照だけを残した。

**ユーザーの実機確認待ち** — iTerm2 で新規ウィンドウを開き、14pt の見え方を判断してもらう。
レビュー3件はそのあと。
