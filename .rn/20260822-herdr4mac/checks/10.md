# #10 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| `lib/merge.py` が存在せず、`setup.sh` にマージ経路が残っていない。管理対象のファイルはすべて同じ1つの方式(丸ごと上書き)で配置される | OK | `git ls-files` に `lib/` も `tests/` も無く、`merge` / `python3` の grep も0件。配置経路は `deploy` 1本で、herdr config / Claude settings / statusline.sh / iTerm2 プロファイル / WT settings の5つがすべてこれを通る。**本 round では方式の統一を退避側にも広げた**: 退避も配置と同じく「一時名で書いて rename」になり、一時ファイルの掃除(`sweep_tmp_files`)も配置先ディレクトリと BACKUP_DIR で同じ1つの関数を使う。失敗の扱いは従来どおり `record_failure` 1種類(WARNING → 続行 → 末尾で列挙 → 非0)。setup.sh は 328行 → 373行。`bash -n` 通過、`shellcheck 0.11.0` 指摘0件 |  |  |
| mac 上で `setup.sh` が exit 0 で完了し、配置された4ファイルが dotfiles の内容と一致する。2回流してもバイト列が変わらない | OK | すべて `env -i HOME=<隔離先> PATH=…` の隔離ホームで `/bin/bash`(3.2.57、shebang と同じ)により実測(実ホームには一度も実行していない)。**A1(ブロッカー)**: `LAST_BACKUP` グローバルを廃し、退避先は `backup_file` の stdout から `deploy` のローカル変数で受ける。指摘の3ケースを再現し、いずれも約束された `.bak` が run 後も残ることを確認 — (i) config.toml に元設定 + `~/.claude` 500 → config.toml の `.bak` が残り中身は元設定、rc=1 / (ii) settings.json に手元設定 + `~/.claude/scripts` 500 → settings.json の `.bak` が残る、rc=1 / (iii) settings.json だけ差分・statusline.sh は `Up to date`・DynamicProfiles 500 → `.bak` が残る、rc=1。同条件で HEAD(`f03f513`)は (iii) で BACKUP_DIR が空になる(約束された退避が消える)ことを対照実測。**A2**: `mv` → `mv -f`。python の pty で読み取り専用 dst に実行 → プロンプト無し・実際に上書き・一時ファイル0・rc=0。HEAD は同条件で `override …? (y/n [n]) not overwritten` を出したうえ `Installed` と表示し、dst は旧内容・一時ファイル1本・rc=0(偽の成功)。**A3**: 一時ファイルは配置の前に配置先ディレクトリを掃く。iTerm2 の配置中に SIGKILL → `.herdr.json.dotfiles-tmp.<pid>` が残り、次の run で消える(HEAD は次の run 後も残ったまま)。配置先が既に一致している run でも掃くことを確認。**A4**: 退避も一時名で書いて rename する方式に変更。pty でプロセスグループに SIGINT(実 Ctrl-C 相当)を送ると rc=130 で、BACKUP_DIR に残るのは `.settings.json.dotfiles-tmp.<pid>` だけで `.bak` は0本、次の run がそれを掃く。HEAD は同条件で 1,000,000 バイトの部分的な `.bak` を残した。**A5/A6/A7**: シンボリックリンクのコメントを実測に合わせ(内容一致→リンクのまま `Up to date`、不一致→実体に置換・リンク先は無傷、退避にはリンク先の内容)、失敗サマリを `Everything else was deployed or skipped as noted.` に、herdr 不在時の行を `herdr not found. Its config is managed here all the same:` に変更(直後が `Up to date` の run で「Installing」と読めない)。**その他の実測**: 初回 rc=0 で4ファイルが `cmp` 一致・`.bak` 0・一時ファイル0、再実行3回とも rc=0 で `Up to date` 4行、内容が違う状態は上書き+`.bak` に旧内容、`HOME` にスペース rc=0、`umask 077` で statusline.sh は 700(文書どおり)、内容一致で 644 の statusline.sh は `chmod +x` で 755 に戻る(chmod を残す理由の実測)。失敗経路: `~/.config` 500(mkdir 不可)/ `~/.claude` 500 / `~/.claude` がファイル / 配置先がディレクトリ(裸の `cmp:` stderr 0件)/ 配置先のディレクトリが読み取り専用(dst 無傷)/ BACKUP_DIR が作れない(`~/.local/state` をファイル化 → 原本と違う2ファイルが配置されず rc=1)— いずれも他の配置は続行し末尾で列挙して rc=1。`brew install` 失敗 → 警告のみ rc=0、`jq` 不在 → 警告 rc=0、`herdr` 不在 → 1行 rc=0、hook 不在 → 警告、hook を置くと消える。444 の dst は上書き後 644(既知・対象外) |  |  |
| `herdr config check` が配置後の config.toml に対して通る | OK | 隔離ホームへ配置したあと `env -i HOME=<隔離先> herdr config check`(herdr 0.7.4)→ `config: ok` / rc=0。同じ隔離ホームの config.toml を壊すと `config parse error` / rc=1 になり、次の run で配り直すと再び `config: ok` / rc=0 に戻ることまで確認(herdr が実ホームではなくこのファイルを読んでいる裏取り) |  |  |
| WSL でない Linux で `wslpath` 不在により setup が落ちない | OK | `PATH` 先頭のスタブで5通りを再実測。(a) 非 WSL Linux: rc=0 / `Not WSL (no wslpath / cmd.exe). Skipping Windows Terminal.` → `Done.`。(b) WSL 相当 + WT 導入済み: rc=0、WT の settings.json が原本と一致、machine-only キーは消え、2回目も rc=0。(c) WSL だが WT 未導入: rc=0 / `Windows Terminal not installed (…). Skipping.`。(d) `cmd.exe` が答えない: 別メッセージで失敗に数え rc=1、cmd.exe の stderr も残る。(e) `wslpath` が失敗: rc=1 で wslpath の stderr が残る。あわせて「スキップと失敗が同じ run に出る」場合(非 WSL Linux + `~/.claude` がファイル)を実測し、新しいサマリ文が実態と合うことを確認 |  |  |
| `design.md` と `steering.md` と `setup.sh` が同じことを言っている(項目マージへの言及が残らない) | OK | `design.md` を本 round の指摘に沿って改訂 — §4.7: 保証を「**その run が配る先を持っていた**管理対象は原本とバイト一致」に限定、境界に第3カテゴリ(管理対象だが配る先が無い=スキップ・exit 0)を新設、部分適用の積極的な正当化(冪等な再試行)と、部分適用が成果物の組み合わせを崩す実測例(`~/.claude/scripts` が読み取り専用 → 新しい settings.json が古い statusline.sh を指す)を追加し、setup.sh 冒頭の主張を「スクリプトの実行順の話」に限定、退避の失敗が配置をブロックすること(実測)と「約束した退避は run 後も残る/捨てるのはその配置自身のものだけ」を保証として明記、`mv -f` と tty プロンプトの実測を原子性に追加、一時ファイルが配置先ディレクトリに現れることと掃除方式を境界に追加、`chmod +x` を残す理由を「一致していた配置先のモードは書き換わらない(644→755 の実測)」に差し替え。§5.1: 「守る対象が実在しない」を「**現時点では**実在しない(Windows 1台の前提の下での話。2台目では GUID を守る利点が実在し、§4.7 の境界ごと再検討)」に訂正し、捨てた選択肢の唯一の利点を認めた。setup.sh 側のコメントも同じ内容に揃えた。**残る未検証**: iTerm2 がドット始まりのファイルを読み飛ばすか(設計はこれに依存せず掃除で解決)、WT が起動時に WSL プロファイルを作り直すか、Windows / WSL 実機への配置、iTerm2 / WT の実キー送信、`cmd.exe` がリテラル `%LOCALAPPDATA%` を返す経路、`defaults read` が隔離 `HOME` でも実ユーザーの設定を読むこと(検証環境の制約)。`steering.md` はコーディネーター担当のため未編集 |  |  |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | | |

## Expert Reviews (axes the task needs)

### Design Expert

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | | |
| System-wide integrity (interfaces, cross-doc consistency) | | |

### Craft Expert (coding)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | | |
| Consistency with existing style | | |

### Verification Expert (test)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | | |
| Coverage (edge cases / claims / steps) | | |

## Overall Verdict

- Self-check: OK
- QA:
- Design expert:
- Craft expert:
- Verification expert:
- Ready to check off:
