# #10 レビュー

配置方式を丸ごと上書きに統一し、項目マージ機構(`lib/merge.py` 1,230行 + 専用テストスイート)を
撤去した。実装は `3ae7535` `f03f513` `a873819`。以降の指摘対応は、隔離ホーム
(`env -i HOME=…`)で `/bin/bash` 3.2.57 により再現・確認している。

## 前ラウンドの指摘と対応

| 指摘 | 対応要否 | 対応内容 |
|---|---|---|
| **退避先をグローバル変数で持っており、あとの配置の失敗が、前の配置がユーザーに約束した退避を消す**(データ消失) | 要(ブロッカー) | `backup_file` の stdout から `deploy` のローカル変数で受ける形に変更。3ケースで約束された `.bak` が run 後も残ることを確認 |
| **読み取り専用の配置先に対し、tty があると `mv` が確認プロンプトを出して既定 "n" で何もせず exit 0 を返す** — スクリプトは `Installed` と表示し、一時ファイルも残る(偽の成功) | 要 | `mv -f` にした。pty 越しの実測でプロンプトなし・実際に上書き・一時ファイル0 |
| SIGKILL で残った一時ファイルを誰も消さない | 要 | `deploy` が配置の前に配置先ディレクトリの同じパターンを掃く。配置先が既に原本と一致している run でも掃く |
| Ctrl-C で死んだ退避が「完全な退避」を名乗る名前(`.bak`)で残る | 要 | 退避も一時名で書いてから rename する方式に変更。SIGINT で `.bak` は0本 |
| シンボリックリンクに関するコメントが実挙動と違う | 要 | 実測に合わせて修正(内容一致→リンクのまま、不一致→実体に置換、リンク先は無傷) |
| 失敗サマリが「他はすべて配置した」と言うが、配る先が無くてスキップしたものがある | 要 | `Everything else was deployed or skipped as noted.` に変更。design.md §4.6 の境界に第3カテゴリを新設 |
| herdr 不在時のメッセージが、直後が `Up to date` の run で「これからインストールする」と読める | 要 | `herdr not found. Its config is managed here all the same:` に変更 |

## 積み残していた指摘と対応

| 指摘 | 対応要否 | 対応内容 |
|---|---|---|
| **一時ファイルの先頭ドットの理由がコメントで逆に書かれている** — 「iTerm2 は DynamicProfiles の全ファイルを読む」は偽 | 要 | 自分でも実測(`strings /Applications/iTerm.app/Contents/MacOS/iTerm2` に `Skipping it because of leading dot` / `Skipping it because of trailing tilde (GNU-style backup file)` が `reallyReloadDynamicProfiles` の近傍にある)。`BACKUP_DIR` と `tmp_prefix_for` の2箇所を書き直し、「ドットで始まるから読み飛ばされる = 一時ファイルが安全」「`.bak` はどちらの規則にも当たらないから隣に置けない」という向きに揃えた |
| **`backup_file` の一時ファイルが trap にも sweep にも入っていない** | 要 | 一時ファイル名を `deploy` 側で決めて `backup_file` に渡す形にし、trap(`remove_pending_tmp`)が配置用と退避用の両方を消すようにした。合わせて `deploy` が `BACKUP_DIR` 側のパターンも毎回掃く。実測: 退避コピーの途中でプロセスグループへ SIGINT — 修正前は 40 バイトの `.config.toml.dotfiles-tmp.<pid>` が残り、収束後の3回の run を生き延びた。修正後は中断直後に BACKUP_DIR が空、SIGKILL で残した場合も次の1回で消える |
| 「the next run sweeps it」が偽 | 要 | 上の変更で実際に真になった(`deploy` が毎回掃くので、収束済みの run でも掃く)。コメントも「trap が消す/殺された run のぶんは deploy の sweep が拾う」と書き直した |
| `$$` の理由が自己矛盾 | 要 | `tmp_for` 側に一本化。「pid があるから2つの run が同じ一時ファイルへ書いて混ざったものを rename することはない。同時実行はそもそも支援しないが、負けた run は自分の rename で失敗する」— sweep 側のコメントと矛盾しない書き方にした |
| 一時ファイル名の形式が2箇所に手書き | 要 | `tmp_prefix_for` を新設し、`tmp_for`(pid を付ける)と `sweep_tmp_files`(`*` を付ける)の両方がそれを使う。形式を組み立てるのは1箇所だけ |
| WARNING の整形が `record_failure` と手書きに散っている | 要 | 整形だけを `warn` に切り出し、`record_failure` は `FAILURES` に積んでから `warn` を呼ぶ。iTerm2 既定プロファイル警告と brew 失敗警告は `warn` を直接呼ぶので、見た目は同じまま失敗には数えない。実測: スタブ `defaults` / `brew` で両方を出させ、`record_failure` と同じ体裁・exit 0 |
| `windows-terminal/settings.json` が index で 100755 | 要 | `git update-index --chmod=-x` と `chmod 644`。実測: WSL スタブ経路で配置すると `-rw-r--r--` で落ちる |
| `${#FAILURES[@]}` ガードに理由が書かれていない | 要(コメント追加) | 自分で実測してから記載(`/bin/bash -c 'set -u; arr=(); echo "${arr[@]}"'` は `arr[@]: unbound variable` で exit 127)。「体裁ではない」と明記した |
| 失敗時の `Fix:` 文が権限の話しかしない | 要 | リポジトリ側の原本欠落を先に分岐させた。「`No such file or directory` が `$DOTFILES_DIR` 配下を指しているならクローンが不完全」「それ以外はこのマシン側」。実測: 原本を消したコピーで踏ませて文面を確認 |
| **配置に失敗した run が空のディレクトリを残す** | 要 | **消さず、メッセージを直す**方を採った。`mkdir -p` は「この run が作ったか」を返さないので、消すなら元からあったディレクトリを消す覚悟が要る。空ディレクトリは無害で、次に成功する run が埋める。失敗メッセージから「leaves nothing to undo」の断定を外し、「空のディレクトリが残ることがある」と述べる。design.md §4.6 に境界として追記 |
| **`./setup.sh \| head -2` が SIGPIPE で死ぬ** | 要(判断と明記) | **挙動は変えない**。耐えるには `set -e` の下で file 内の全 `echo` に失敗処理を付けるか、報告を溜めて最後に一度に書くしかなく、後者は「失敗したコマンドの stderr のすぐ下に、それを説明する WARNING が出る」現在の並びを壊す。run は非0で終わり、配置は冪等なので次の run が収束させる。design.md §4.6 の境界と `FAILURES` のコメントに理由つきで明記した(実測: `PIPESTATUS` は `141 0`) |

## 完了基準

| 基準 | 自己チェック | 根拠 | QA | QA 根拠 |
|---|---|---|---|---|
| `lib/merge.py` が存在せず、`setup.sh` にマージ経路が残っていない。管理対象のファイルはすべて同じ1つの方式(丸ごと上書き)で配置される | OK | `git ls-files \| grep -i merge` は空、`lib/` は存在しない。`grep -n -iE 'merge\|python3\|lib/' setup.sh` も空。`grep -n 'deploy "' setup.sh` は3行(config.toml / herdr.json / settings.json)で、配置は `deploy` 以外に無い | | |
| mac 上で `setup.sh` が exit 0 で完了し、配置されたファイルが dotfiles の内容と一致する。2回流してもバイト列が変わらない | OK | 隔離ホームで `/bin/bash` 3.2.57(`bash -n setup.sh` も OK)。1回目 `Installed` ×2 で exit 0、2回目 `Up to date` ×2 で exit 0。`find -type f -exec shasum` の結果が1回目と2回目で `cmp` 一致。`diff herdr/config.toml ~/.config/herdr/config.toml` と `diff iterm2/herdr.json ~/Library/…/DynamicProfiles/herdr.json` はいずれも差分なし | | |
| `herdr config check` が配置後の config.toml に対して通る | OK | `env -i PATH=/opt/homebrew/bin:… HOME=<隔離ホーム> XDG_CONFIG_HOME=<隔離ホーム>/.config /opt/homebrew/bin/herdr config check` → `config: ok` / exit 0 | | |
| WSL でない Linux で `wslpath` 不在により setup が落ちない | OK | `uname -s` が `Linux` を返すスタブのみを PATH に置き `wslpath` / `cmd.exe` 無しで実行 → `Not WSL (no wslpath / cmd.exe). Skipping Windows Terminal.` → `Done.` / exit 0。逆に `uname` / `cmd.exe` / `wslpath` の3スタブを置いた WSL 経路では settings.json が配置され exit 0、`stat -f '%Sp'` は `-rw-r--r--`、2回目は `Up to date` | | |
| `design.md` と `steering.md` と `setup.sh` が同じことを言っている | OK | §4.6 を実装に合わせて更新: 先頭ドットの理由(既に修正済み)に加えて、(a) rename の代償にリポジトリ側モードがそのまま配置先モードになることと 644 で持つこと、(b) 掃除と trap が配置先と BACKUP_DIR の両方に効くこと・退避側の一時名を `deploy` が持つ理由・形式を1箇所に置く理由、(c) 失敗した run が空ディレクトリを残す境界と消さない理由、(d) SIGPIPE の境界と直さない理由。steering.md の完了基準・原則(丸ごと上書き、部分適用+非0終了)と矛盾する記述は無い。失敗経路の実測: 配置先を2つともディレクトリにした run で `WARNING:` 2本 → 続行 → `Finished with 2 failure(s):` と2行の列挙 → exit 1、既存の `.bak` は消えていない | | |

## 総合判定

- 自己チェック: OK
- QA:
- Design expert:
- Craft expert:
- Verification expert:
- チェックオフ可否:
