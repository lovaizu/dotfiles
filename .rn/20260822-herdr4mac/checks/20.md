# #20 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| 残ったコメントがすべて「消して素直な書き方に直すと、再現に失敗したまま成功として終わる」箇所に付いている | OK | 残った 21 塊を1件ずつ「消したらどう壊れるか」で判定(下表)。全件が silent-success — 配置していないのに exit 0、読まれない場所へ配置して exit 0、約束した退避が消える、警告が意味を失う、のいずれか。実行時メッセージの言い換え・関数の要約・出力ストリームの選択理由は0件 | | |
| コードが変わっていない: 非コメント・非空行の並びが変更前と完全一致 | OK | `diff <(grep -vE '^\s*#' setup.sh.orig \| grep -vE '^\s*$') <(grep -vE '^\s*#' setup.sh \| grep -vE '^\s*$')` → **出力なし**(261行)。空行も 22 → 22 で不変 | | |
| `bash -n` と `shellcheck -s bash` が通る | OK | `bash -n setup.sh` → exit 0。`shellcheck -s bash setup.sh` → 出力なし / exit 0 | | |
| 隔離ホームで2回流して exit 0 / 0、2回目は `Up to date` | OK | 空ホームに `env -i HOME=$T PATH=/usr/bin:/bin:/usr/sbin:/sbin bash setup.sh` を2回。exit 0 / 0。2回目は `Up to date` 2行(`grep -c` = 2) | | |
| 配置されたファイルが dotfiles と `diff` 一致 | OK | `config.toml` / `herdr.json` とも差分なし | | |
| 出力が変更前と同一 | OK | 変更前後を**別々の隔離ホームで各2回**流し、ホームのパスを `HOME` に正規化して `diff` → run1 / run2 とも差分なし | | |
| 配置失敗を作った run が 警告 → 続行 → 末尾で列挙 → exit 1 のまま | OK | `DynamicProfiles` を `chmod 500` にした run で **exit 1**、`WARNING: … was not deployed.` → herdr config の配置は続行 → `Finished with 1 failure(s):` + 該当パス1件。dangling symlink の run は exit 0 で退避を取らず、リンクが通常ファイルに置き換わる(`dotfiles-backups` は作られない) | | |

## 行数

| | #18 後 | #20 後 |
|---|---|---|
| 総行数 | 524 | 367 |
| コード | 261 | 261 |
| コメント | 240 | 83 |
| コメント塊 | 66 | 21 |

コメント : コード比は 0.92 : 1 から **0.32 : 1** へ。

## 残した 21 塊と、消したときに起きる silent failure

| 場所 | 守っている行 | 消して素直に直すと |
|---|---|---|
| L4-6 | `: "${HOME:?…}"` が trap より前 | trap の後に移すと `set -u` 致命が exit 0 で返る。herdr の config だけ配って iTerm2 を配らず、失敗も列挙せず成功(実測) |
| L11-14 | `case "$value" in /*)` と `xdg_base` で返すこと | 相対 XDG 値をそのまま使うと配置先が cwd 次第になり、毎回違う場所へ配って成功(実測)。stdout で返すとサブシェルで `XDG_IGNORED` が消え、警告そのものが出なくなる |
| L31-33 | `set_xdg_base XDG_CONFIG_HOME …` | `$HOME/.config` 直書きだと herdr が読まないファイルを配って成功(実測) |
| L37-40 | `BACKUP_DIR` が配置先の隣でないこと | `.bak` を DynamicProfiles に置くと同じ Guid の二重プロファイルとして読まれ、環境が壊れたまま run は成功 |
| L46-49 | `tmp_prefix_for` の先頭ドット | ドットが無いと配置中のファイルを iTerm2 が profile として読む |
| L58-59 | `backup_path_for` の basename 単独 | basename の衝突する管理対象を足すと、退避が無言で上書きし合う |
| L75-82 | `trap … INT/TERM` の `exit` と `REACHED_END` | return だけの trap は Ctrl-C で次の配置へ進む。番兵が無いと `set -u` 致命が exit 0 で通る(実測、bash 3.2.57)。`$st` の検査が無いと SIGTERM の 143 が潰れる |
| L96-98 | `warn` 末尾の `return 0` | errexit 下の WSL 分岐で `record_failure` の戻り値が最後の echo 次第になり、run が診断の前に終わる |
| L116-122 | `while [ -e "$bak" ]` の採番 / 退避先を stdout で返すこと | 秒単位なので同じ秒の2本目が deploy の約束したパスを上書きする(実測)。グローバルだと後の配置の後始末が、前の配置が約束済みの退避を消す(実測) |
| L125-126 | `[ -e "$dst" ] \|\| return 0` | dangling symlink で退避を取ろうとする。実体が無いので「退避した」と言えないまま置き換わる境界(§4.6) |
| L135-137 | `cp … && mv -f "$tmp" "$bak"` の一時名経由 | 丸ごと `cp` すると Ctrl-C で部分的な `.bak` が「完全な退避」の名前で残る(実測) |
| L145-149 | `deploy` が常に 0 を返す契約 | 戻り値を返すと呼び出し側の `\|\| true` が関数内部の errexit まで落とし、裸の行が無言で失敗して exit 0(実測) |
| L166-169 | `mv -f` の `-f` | tty があると mv が確認を出し、既定 "n" で何も置き換えず exit 0(実測、pty 越し)= 「配置した」と嘘をつく |
| L231-234 | `iterm_dir` を `deploy` に渡す(= `mkdir -p` させる)こと | WSL 側と揃えて「無ければスキップ」に直すと、iTerm2 未導入の Mac に profile が落ちず run は成功。投函箱と footprint の違い(§4.5) |
| L238-242 | `iterm_profile_guid` のリテラル | `iterm2/herdr.json` の `"Guid"` との tie。片方だけ変えると既定にしてあるマシンでも毎回警告が出て意味を失い、本当に既定でない機械を見落とす |
| L287-291 | `grep -qi microsoft /proc/sys/kernel/osrelease` | ツールの不在を非 WSL の証拠にすると、interop 無効の実 WSL が「配る先が無い」に誤分類され exit 0(実測) |
| L312-317 | `case "$appdata" in /*)` | 空だけを弾くとリテラル `%LOCALAPPDATA%` が `wt_dir` まで運ばれ、「WT 未導入」として exit 0(実測)。判定の限界は §4.5 |
| L324-327 | `LocalState` を作らずスキップすること | 作ってしまうと誰も読まない settings.json を置いて成功する |
| L348-350 | `[ "${#FAILURES[@]}" -gt 0 ]` のガード | `"${arr[@]}"` は bash 3.2 の `set -u` で空配列を unbound と読み、EXIT trap ありなら exit 0 で静かに死ぬ(実測、3.2.57) |
| L358-359 | `REACHED_END=1` がブロックの末尾 | 先頭で立てると、列挙の途中で死んだ run も「意図した終端」になる |
| L365-366 | 末尾の `REACHED_END=1` | 印の無い exit 0 は意図しない終わり方(trap 側の判定と対) |

## 落とした 45 塊と、落とせる理由

| 落とした | 再現に効かない理由 |
|---|---|
| WSL interop の説明(L403-408 の一部 / L414-416 / L428-435 / L437-445 / L474-481、計 23行) | `record_failure` に渡す文字列が同じ内容をユーザーに喋り、**exit 1 で騒ぐ**。コメントが無くても失敗は誰の目にも見える。判定そのものの理由(osrelease を使うこと / `case /*)`)は残した |
| 関数頭の「何をする関数か」(`tmp_prefix_for` / `sweep_tmp_files` / `record_failure` / `backup_file` / `deploy` / `warn`) | #18 が「読む人が最初に要る情報」として意図的に残したが、**追えることは再現に効かない**。関数名と本文で足りる |
| `sweep_tmp_files` 本体と、それが `cmp` より前にある理由(L86-90 / L208-212) | 消えて困るのは一時ファイルの残留であって再現ではない。ゴミが残っても配置は成功する |
| `BACKUP_TMP` を `deploy` 側で名付ける理由(subshell と trap、L237-241) | 同上。SIGINT で書きかけが残るのは litter |
| `tmp_for` の `$$`(L70-73) | 同時実行はこの repo の使い方に無く(1人・年数回・対話的)、コメント自身が「安全性は買わない」と言っていた |
| `cmp` が 2 を返すこと / `2>/dev/null` の理由(L215-223) | 分かりにくいメッセージが出るだけで、判定は変わらない |
| `was_link` を rename の前に見て後に言う理由(L228-231) | 誤った報告が出るが、配置は正しく終わる |
| 失敗経路で退避を捨てる理由(L278-281 / L283-285) | 同内容の `.bak` が1本増えるだけ |
| `defaults` の2分岐の理由(L351-357) | iTerm2 未導入のマシンに要らない案内が出るだけ。配置は済んでいる |
| brew 分岐が warn で済ませる理由(L377-379) | `font_cost` の文字列が「font is not a managed file, so this run is not counted a failure」と実行時に言っている |
| 出力ストリームと文言の選択理由(L129-133 / L150-151 / L324-326 / L410-411 / L504 / L510-511) | どれも「なぜこの文言/このストリームか」であって、変えても再現は成功も失敗もしない |
| `rm -f ""` が無言であること等の trivia(L95-96 / L100-101 / L178-179 / L243-244 / L246-247 / L254 / L256-257 / L262-264 / L308-309) | 実装を読めば分かる |
| セクション見出し(`# herdr (common)` / `# OS-specific setup`) | 直下のコードが同じことを言っている |
| `FAILURES=()` の説明(L53-55) | 配列名と末尾の列挙で足りる |

## #18 の判断との差分

| | #18 | #20 |
|---|---|---|
| 基準 | 消すと**実測済みの欠陥**が戻る | 消すと**再現が静かに壊れる** |
| litter 系(掃除・subshell の書きかけ) | 残す | **落とす** — 戻るのはゴミであって再現の失敗ではない |
| 報告の質(文言・ストリーム・誤報) | 残す | **落とす** — 読み手には見えるので静かではない |
| 関数頭の要約 | 意図的に残す | **落とす** — 追えることは再現に効かない |
| silent-success 系 | 残す | **残す**(全件) |

## コーディネータの独立確認

専門家レビューは回していない(`steering.md` Rules)。

| 確認したこと | 結果 |
|---|---|
| コードが変わっていないか | 非コメント・非空行 261 行が完全一致。空行も 22 で不変。実行ビットは `-rwxr-xr-x` のまま |
| 構文と lint | `bash -n` → 0、`shellcheck -s bash` → 出力なし / 0 |
| 出力が変更前と同一か | 変更前と変更後を別ホームで各2回実行。パス正規化後の `diff` は run1 / run2 とも空 |
| 失敗経路 | `chmod 500` の run で exit 1 + 列挙1件。dangling symlink の run で退避を取らずに置換 + 警告、exit 0 |
| 残ったコメントの位置 | インデントを直下のコード行に合わせる規則を全ファイルに適用し、**変更前のファイルに対しては差分0**(=元からその規則だった)であることを確認したうえで適用 |
| 残した 21 塊が本当に silent か | 1件ずつ「消して素直に直したときの終了コードと出力」で判定。実行時メッセージが同じことを言っている塊が残っていないことを `record_failure` / `warn` の文字列と突き合わせて確認 |

## Overall Verdict

- Self-check: OK
- QA: N/A(Rules により専門家レビューを回していない)
- Craft expert: N/A(同上)
- Verification expert: N/A(同上)
- コーディネータの独立確認: OK
- Ready to check off: Yes
