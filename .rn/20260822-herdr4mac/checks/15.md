# #15 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| `display_name` が `Opus 5 (1M context)` のとき第2セグメントのモデル部が `Opus5` になり、`Sonnet 5` → `Sonnet5`、`Fable 5` → `Fable5`、`Haiku 4.5` → `Haiku4.5` になる。括弧の注記は表示に残らない | OK | 検証は **statusline.sh 全体に JSON を stdin から流して出力1行を厳密比較**するハーネス(`scratchpad/harness5.sh`、`harness4.sh` に `display_name`/`id` 双方が空・null・不在の8ケースを足したもの。repo には未コミット)で **69 ケース**。**今回は整理ラウンドで挙動を1バイトも変えない**のが要件なので、直前コミット `f740dcb` の版(`scratchpad/base_f740dcb.sh`)と現行版に同一 JSON を流し、**stdout と stderr の両方**をケースごとにダンプして突き合わせた: macOS の `/bin/sh` と `/bin/dash` × `LC_ALL=C` / `en_US.UTF-8` / `ja_JP.UTF-8` の6環境**すべてで全 69 ケースが完全バイト一致**(6環境のダンプは互いにも同一、md5 `2eac7e796795c36482199154ddc7e49b`)。通常モードでも6環境すべて **failures: 0**。`Opus 5 (1M context)` → `C:110k/1000k 5h:12% 7d:49% \| Opus5/h \| work5@testbranch`、`Sonnet 5` → `Sonnet5`、`Fable 5` → `Fable5`、`Haiku 4.5` → `Haiku4.5`、`Claude Sonnet 4.5` → `Sonnet4.5`、`(beta) Claude Opus 5` → `Opus5`、`Claudette 5` → `Claudette5`、`Foo Claude 5` → `FooClaude5`。括弧注記は全ケースで消えている。前ラウンド(`f740dcb`)時点の 61 ケース × 14 環境(mac の sh/dash × 3ロケール、Docker 上の ubuntu:24.04 jq 1.7 と ubuntu:22.04 jq 1.6 の sh/dash × 2ロケール)で failures: 0・全ダンプ同一 md5 だったことは据え置きで、そこからの差分がゼロであることを今回のバイト一致が担保している | | |
| 置換規則が特定の家族名を列挙していない — 新しい家族名が来ても素通りせず同じ形に整う | OK | jq 式に家族名リテラルは無く、置換は「括弧注記の除去」「先頭の `Claude` + 空白の除去」「空白の全除去」の3段だけ。`grep -n 'Opus\|Sonnet\|Haiku\|Fable' claude/scripts/statusline.sh` のヒットはコメントの例示行のみ。存在しない家族名 `Quill 7` → `Quill7` を実測。**今回の4点**: **T1** `[\p{Z}\s]` を `\s` に(3箇所 → `sub("^\\s*Claude\\s";"")` と `gsub("\\s";"")`)。`\p{Z}` は無寄与な上、Unicode プロパティ非対応の Oniguruma ビルドでは式全体が実行時エラーになりシェル側ガードに黙って吸われてモデル名が理由不明で `unknown` に化ける唯一の故障点だったので落とした。落としても出力が動かないことは上記バイト一致(NBSP・全角スペース・タブ・改行・CR・混在の各ケースを含む)で確認。**T2** 「Oniguruma は両ホストで同じビルド」という記述は偽(mac の `/usr/bin/jq` は `otool -L` が `libSystem.B.dylib` のみで Oniguruma を静的同梱の `jq-1.7.1-apple`、ubuntu 24.04 は `ldd` で `libonig.so.5` を動的リンクの `jq-1.7`)だったので、「jq は sed と違い**実測で両ホスト同じ結果**」という事実どおりの表現に書き直した。**T3** モデル名セクションのコメントを **27行 → 7行**(コード9行)に圧縮。残したのは例示・家族名を列挙しない理由(Fable 素通りの再発防止)・注記を落とす理由(コンテキスト量は `C:` セグメントの担当)・空文字 `display_name` があるので jq の `//` に頼れないこと・規則の順序と `Claude ` を先頭でのみ剥がす理由(`Foo Claude 5` を切らない)。削ったのは `\p{Z}`/`\s` の測定細部(T1 でコード自体が消えた)、jq バージョンの列挙(腐る記述)、sed をやめた理由の詳説、「jq が PATH に無い場合もここに落ちる」(jq 不在なら第1・第3セグメントも空で結局壊れており、repo の設計方針と食い違う)。冒頭の `printf` を使う理由のコメントは他5箇所に効いているのでそのまま残した | | |
| 第1・第3セグメントと `/努力度` の付き方は従来どおりで、区切りは `\|` のまま。steering の表示形式(`C:…k/…k 5h:…% 7d:…% \| モデル/努力度 \| ディレクトリ@ブランチ`)から外れない | OK | ハーネスは3セグメント全体を1行として比較しており、macOS の `/bin/sh` と `/bin/dash` × `LC_ALL=C` / `en_US.UTF-8` / `ja_JP.UTF-8` の6環境で `f740dcb` 版と全 69 ケース**バイト一致**なので、第1・第3セグメントも努力度の付き方も区切り `\|` も1文字も動いていない。努力度は `high`→`/h`、`medium`/`max`→`/m`、`low`→`/l`、`effort.level` 不在では付かない。`[ -n "$model_name" ] \|\| model_name=unknown` のガードは残してあり、注記だけ `(1M context)` / 空白だけ / `()` / `Claude ` だけ / 全角スペースだけ / タブだけ / NBSP だけ / 改行だけ の名前はいずれも `unknown`(努力度ありなら `unknown/h`)。第1セグメントの jq 式・`printf '%.0f'`・`effort` の処理・`dirname`/`e` の命名・jq 呼び出しの集約・`set -e` には手を触れていない(`git diff` の変更範囲はモデル名セクションのみ、11 insertions / 31 deletions) | | |
| `sh -n` / `dash -n` が通り、`display_name` 不在(`model.id` フォールバック)や `effort.level` 不在でも壊れない | OK | `sh -n` / `dash -n` ともに無エラー。構文検査だけで済ませず全 69 ケースを `/bin/sh` と `/bin/dash` の両方 × 3ロケールで実行。**T4**: jq 式末尾の `"unknown"` リテラルを外し `first // ""` にして、既定値をシェル側 `\|\| model_name=unknown` の1箇所に集約した。`first` は空配列で `null` を返すため `// ""` が無いと `null \| gsub` でエラーになる — そこを重点確認するために、`display_name` と `id` が **両方空 / 両方 null / 片方空+片方不在 / `model` が空オブジェクト / `model` が null / `model` キーごと不在**、および注記だけの `display_name` + `id` ありの8ケースを追加し、**すべて `unknown`(努力度ありなら `unknown/l`)になり、stderr は空**であることを6環境で確認(stderr が空でないのは意図どおり `jq: parse error` を出す不正 JSON ケースだけで、これは `f740dcb` と同一文字列)。そのほか `display_name` 不在で `id` のみ → `claude-opus-5-20260115`、`display_name` が `null` → `id` へ、`context_window` 欠落 → `C:0k`、`rate_limits` 欠落 → `C:110k/1000k`、`used_percentage` フォールバック → `C:51k/200k`、バックスラッシュを含む JSON(`current_dir` が `C:\Users\x`、`display_name` が `A\\B`)、空入力、不正 JSON、`null`、**`jq` が PATH に無い場合**(`jq` を除いた PATH で実行 → ` \| unknown \| work5@testbranch`、`f740dcb` と同一)まで両シェルで確認。**残る未検証項目**: (1) 実 WSL 上の dash と WSL 配布の jq バイナリ — Docker(aarch64)上の ubuntu 22.04/24.04 で代替しており x86_64 実機は未実測。(2) Oniguruma 抜きでビルドされた jq — 入手できず未実測(T1 でその場合の故障点自体は減らした)。(3) 括弧の入れ子・閉じ忘れ、`printf '%.0f'` のロケール依存は前提の外 | | |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | 判子ではない。ハーネスは式の断片ではなく **statusline.sh 全体に stdin から JSON を流し、出力1行を厳密比較**しており、判定対象が目的(表示される1行)と一致している。回帰の向きも正しく、各ラウンドで「変更前の版が実際に壊れること」を示してから修正後を示している。criterion 2(将来の家族名)を grep だけで済ませず未知の家族名 `Quill 7` / `Zephyr 6` の振る舞いで検証しているのも設計として正しい。QA 担当がハーネスの弁別性も確認(旧期待値で判定すると 11 件 FAIL、jq を潰すと 14 件 FAIL — 常に PASS する作りではない)。最終ラウンドは「挙動を1バイトも変えない」が要件だったため `f740dcb` との全ケース byte 比較で検証しており、要件と検証手段が一致している。**指摘(記録)**: ハーネスは scratchpad にあり repo 非コミットのため、この検証は次回自動では再走しない(repo に恒久テストを置かない方針による設計判断)。QA が「`tests/` に常設せよ」と勧めたが、その `tests/` 自体が #10 で撤去対象のため Invalid とした |

## Expert Reviews (axes the task needs)

### Craft Expert (coding)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | POSIX sh のまま、bash 依存なし。jq 式のクォートがシェルのシングルクォート越しに正しく渡り、`\\(` と jq の文字列補間の衝突も回避できている。`gsub`(注記は複数ありうる)と `sub`(`^` アンカーは1回)の使い分けが正しい。規則の順序(注記除去 → 先頭 `Claude` 剥がし → 空白除去)が load-bearing であることを実測で確認済み。**3ラウンドで是正**: `echo` → `printf '%s\\n'`(7箇所。mac の `/bin/sh` と dash の双方が `echo` でバックスラッシュを展開し JSON を壊すことをコーディネーターも独立に再現)、変数名 `abbrev` → `model_name`、死んでいた ` *` の削除、`unknown` の2重定義 → `first // ""` で1箇所に集約 |
| Consistency with existing style | OK | 既存のコメント密度・変数名・段落構成を踏襲。最終的に sed が消え、mac(BSD)と WSL(GNU)の実装差という問題自体が無くなった。**是正した欠陥**: コメントの「なぜ」に事実誤りが3度出た — (1)「アンカーが `Claudette 5` を守る」(実際は末尾スペースが守っており、差が出るのは `Foo Claude 5`)、(2)「`[[:space:]]` は UTF-8 なら NBSP も詰める」(GNU/glibc では偽。Craft・Verification が独立にコンテナで実測)、(3)「Oniguruma は両ホストで同じビルド」(mac は静的同梱の 1.7.1-apple、ubuntu は `libonig.so.5` の 1.7 で偽)。いずれも repo 方針「コメントは理由を書く」に対し**誤った理由は無いより悪い**として修正。最後にコメント 27 行 → 7 行(コード 9 行)に圧縮し、腐る記述(jq バージョンの列挙・測定手続き)を削除 |

### Verification Expert (test)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | OK | 3人のレビュアーが各自ハーネスを組み直して独立に再現。`dash -n` は構文検査にすぎないとの指摘を受け、以後は **`/bin/dash` で実行**して検証。最終的に mac(`/bin/sh`・`/bin/dash` × `C` / `en_US.UTF-8` / `ja_JP.UTF-8`)と **Linux 実機相当(colima + ubuntu:24.04 / jq 1.7、ubuntu:22.04 / jq 1.6 × 2シェル × 2ロケール)**で stdout が全ケース一致することを実測。移設の主張も裏付けられた — 旧 sed 版は `LC_ALL=C` 組と UTF-8 組で出力ハッシュが割れていたのに対し、jq 版は6環境で1バイトも動かない。コーディネーターも独立に 15 ケース × 2シェル × 2ロケールで `f740dcb` とのバイト一致と `sh -n`/`dash -n` を確認 |
| Coverage (edge cases / claims / steps) | OK | 主要家族名・`Claude` 接頭辞・未知の家族名・`(beta) Claude Opus 5`・`Claudette 5`・`Foo Claude 5`・注記だけ/空白だけの名前・`display_name` の空文字/null/不在・`model` キー不在・`effort.level` の有無・空入力・不正 JSON・**jq 不在**・`context_window`/`rate_limits` 欠落・`used_percentage` フォールバック・バックスラッシュを含む JSON・タブ/NBSP/全角スペース/改行を含む名前を、2シェル × 3ロケールで網羅。**残る未検証**: (1) x86_64 の実 WSL(Docker の aarch64 ubuntu で代替)、(2) Oniguruma 抜きの jq(入手不可。T1 でその故障点自体を削除)。**前提の外として意図的に扱った**: 括弧の入れ子・閉じ忘れ、モデル名に区切り文字や改行を含む形状、`printf '%.0f'` のロケール依存、空入力時に第1セグメントが空になる件 — いずれも Claude Code が送る `Opus 5 (1M context)` 形の ASCII 文字列という前提の外(ユーザー判断で範囲を確定) |

## Overall Verdict

- Self-check: OK
- QA: OK(pass。検証手段は目的と一致。指摘は記録済み)
- Design expert: N/A(spawn せず。sed から jq への方式変更はレビュー指摘を受けてユーザーが直接決めたため、設計の是非は本人の判断で決着している)
- Craft expert: OK(3ラウンドの是正後。最終ラウンドで fail の原因だったコメントの事実誤りと分量を解消)
- Verification expert: OK(pass。mac 6環境 + Linux 8環境で実測)
- Ready to check off: Yes — 完了基準4項目をレビュー3者とコーディネーターが独立に実測。修正は計4ラウンド(`6bab994` `7388c13` `c24f61f` `f740dcb` `d18f0ee`)で、規定の3巡を超えたのは方式変更をユーザーが指示したため
