# #4 レビュー

| 指摘 | 対応要否 | 対応内容 |
|---|---|---|
| 非 WSL Linux では `wslpath` が無いまま呼ばれて setup が停止する | #4 の範囲外(既存挙動で、この変更による後退ではない) | #10 で `wslpath` / `cmd.exe` の不在を先に確かめる形に修正 |

繰延: WSL 実機での配置は次回 Windows 同期時、iTerm2 のホットリロードは #5 の実機確認。
