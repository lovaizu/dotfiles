# #6 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| `claude/settings.json` が有効な JSON で、リテラルのホームパス(`/Users/…` / `/home/…`)を含まず、2つの `command` がどちらも `"$HOME/…"` をダブルクォートで囲んでいる | OK | `python3 -m json.tool claude/settings.json` が無エラーで通過。`grep -rnE '/Users/\|/home/\|__HOME__' claude/` が 0 ヒット(exit 1)で、リテラルのホームパスも旧プレースホルダ `__HOME__` も残っていない。JSON をパースして 2 つの `command` を取り出し、正規表現 `"([^"]*\$HOME[^"]*)"` で `$HOME` がダブルクォート内にあることを機械的に確認: SessionStart フック = `bash "$HOME/.claude/hooks/herdr-agent-state.sh" session`、statusLine = `sh "$HOME/.claude/scripts/statusline.sh"`。さらに (1) 各 `command` をシェルに通して `$HOME` が `/Users/kiyo/...` に展開され、その実ファイルが存在することを確認、(2) 空白入りホーム(`/tmp/ho me`)を模した `set --` 実験で `argc=2` / `arg1=[/tmp/ho me/.claude/hooks/x.sh]` となり、クォートによりパスが 1 引数として保たれることを確認。`command` がシェル経由で実行される点は Claude Code 2.1.241 バイナリの hook スキーマ記述("`command` runs through a shell")と statusLine が同一ランナーを通ることから確認済みのため、プレースホルダ置換なしで成立する。 |  |  |
| 現行 `~/.claude/settings.json` との差分が「2箇所の command のパスの `$HOME` 化(クォート付与を含む)」と「`remoteControlAtStartup: false` の追加」のみで、他のキー・値が変わっていない | OK | (1) 生 diff (`diff ~/.claude/settings.json claude/settings.json`) が 3 hunk のみ: L9 のフック command、L18 の statusLine command、L36 の `agentPushNotifEnabled` へのカンマ追加+`remoteControlAtStartup: false` 行追加。(2) キー順・書式の影響を排除するため、両者をパースしてリーフ値をパス付きにフラット化して比較したところ差分は厳密に 3 点のみ(`/hooks/SessionStart/0/hooks/0/command`、`/statusLine/command`、新規 `/remoteControlAtStartup`)で、他の 10 キーは全一致。(3) トップレベルのキー順は原本と同順で、追加キーのみ末尾(既存の同種 boolean `agentPushNotifEnabled` の隣)に付加。2 スペースインデントと末尾改行(最終バイト `0x0a`)も維持。なお原本はフック側がシングルクォート・statusLine 側が裸というちぐはぐな状態だったが、`$HOME` 化に伴い両方ダブルクォートへ統一しており、これは task で明示された意図した差分。 |  |  |
| `claude/scripts/statusline.sh` が `sh -n` / `dash -n` を通り、現行の `~/.claude/scripts/statusline.sh` とバイト一致する | OK | `cmp claude/scripts/statusline.sh ~/.claude/scripts/statusline.sh` が差分なしで終了(2373 バイト)。今ラウンドでは本ファイルを一切編集しておらず、バイト一致が維持されていることを再確認した。`sh -n` と `dash -n`(/bin/dash)がいずれも無エラー。異常系の smoke test も実施: 空入力 → ` \|  \| herdr4mac@worktree-herdr4mac` exit 0、不正 JSON → jq の parse error を stderr に出しつつ同様の劣化表示で exit 0、jq 不在 PATH → `C:0k \| unknown \| herdr4mac@worktree-herdr4mac` exit 0。これらが exit 0 のまま劣化表示になるのは原本由来の既知挙動であり、本タスクはバイト一致複製が要件のため改変していない。 |  |  |
| `claude/themes/catppuccin-mocha.json` と `claude/themes/catppuccin-latte.json` が有効な JSON で、現行 `~/.claude/themes/` の同名ファイルとバイト一致する | OK | `cp` で複製後、`cmp claude/themes/catppuccin-mocha.json ~/.claude/themes/catppuccin-mocha.json` と latte 版がいずれも差分なしで終了(各 689 バイト)。`python3 -m json.tool` が両ファイルとも無エラーで通過。原本そのままで整形し直していない。パーミッションは要求どおり 644 (`-rw-r--r--`)。これらは `settings.json` の `theme: "custom:catppuccin-mocha"` の実体であり、欠けると他 PC で Claude Code が警告なく組み込み dark にフォールバックする。 |  |  |
| 秘密情報(APIキー・トークン等)が含まれていない | OK | `grep -rniE 'api[-_]?key\|token\|secret\|sk-ant\|password\|bearer' claude/` のヒットは statusline.sh の 8 行のみで、すべてコンテキストウィンドウのトークン数計算に関する記述(`input_tokens`, `cache_read_input_tokens`, `total_output_tokens` 等の jq フィールド名およびコメント)であり資格情報ではない。settings.json とテーマ 2 ファイルはヒット 0。全 4 ファイルを全文目視し、値はプラグイン名 `rn@ccpm`、公開 GitHub リポジトリ `lovaizu/ccpm`、テーマ名・カラーコードのみで機微情報なし。 |  |  |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") |  |  |

## Expert Reviews (axes the task needs)

### Craft Expert (coding / writing / visual — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice |  |  |
| Consistency with existing style |  |  |

### Verification Expert (test / fact-check / dry-run — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) |  |  |
| Coverage (edge cases / claims / steps) |  |  |

## Overall Verdict

- Self-check: OK
- QA:
- Design expert: N/A
- Craft expert:
- Verification expert:
- Ready to check off:
