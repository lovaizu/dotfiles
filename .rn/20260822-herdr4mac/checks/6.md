# #6 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| `claude/settings.json` が有効な JSON で、`__HOME__` 以外にホーム依存の絶対パスを含まない | OK | `python3 -m json.tool claude/settings.json` が無エラーで通過。`grep -rnE '/Users/\|/home/[a-z]' claude/` が 0 ヒット。`__HOME__` の出現は 2 箇所のみ (settings.json:9 の SessionStart フック command、settings.json:18 の statusLine command) で、いずれも意図した箇所。 |  |  |
| 現行 `~/.claude/settings.json` との差分が「2箇所のパスのプレースホルダ化」と「`remoteControlAtStartup: false` の追加」のみで、他のキー・値が変わっていない | OK | (1) 生 diff (`diff ~/.claude/settings.json claude/settings.json`) が 3 hunk のみ: L9 と L18 のパス置換、L36 の `agentPushNotifEnabled` へのカンマ追加+`remoteControlAtStartup: false` 行追加。キー順・2スペースインデントは保持。(2) キー順の影響を排除するため `sort_keys=True, indent=2` で正規化した JSON 同士も diff し、同じ 3 点のみ (パス 2 箇所 + 追加キー 1 行) を確認。(3) 逆方向の round-trip 検証: `sed 's#__HOME__#/Users/kiyo#g' claude/settings.json` を原本と diff すると `remoteControlAtStartup` の追加 1 点だけが残り、パス置換が元の値へ完全に復元可能=情報欠落なしであることを確認。追加キーは既存の同種 boolean フラグ `agentPushNotifEnabled` の隣に配置。 |  |  |
| `claude/scripts/statusline.sh` が `sh -n` を通り、現行の `~/.claude/scripts/statusline.sh` とバイト一致する | OK | `cmp ~/.claude/scripts/statusline.sh claude/scripts/statusline.sh` が差分なしで終了 (両者 2373 バイト)。`sh -n claude/scripts/statusline.sh` が無エラー。`cp -p` で複製したため実行権も原本と同じ `-rwxr-xr-x` (755)。加えて構文チェックだけでは動作を保証できないため、サンプル JSON を標準入力に与えた smoke test を実施し `C:108k/1000k 5h:12% 7d:34% \| O5/h \| herdr4mac@worktree-herdr4mac` と期待どおりの 3 セグメント出力を確認。ホーム依存の記述がないことも `grep -nE '/Users/\|/home/\|\$HOME\|~/'` が 0 ヒットで確認済み。 |  |  |
| 秘密情報(APIキー・トークン等)が含まれていない | OK | `grep -rniE 'api[-_]?key\|token\|secret\|password\|passwd\|credential\|bearer\|sk-ant' claude/` のヒットは statusline.sh の 8 行のみで、すべてコンテキストウィンドウのトークン数計算に関する記述 (`input_tokens`, `cache_read_input_tokens`, `total_output_tokens` 等の jq フィールド名およびコメント) であり資格情報ではない。settings.json 側はヒット 0。両ファイルを全文目視し、値はプラグイン名 `rn@ccpm`、公開 GitHub リポジトリ `lovaizu/ccpm`、テーマ名等のみで機微情報なし。 |  |  |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") |  |  |

## Expert Reviews (axes the task needs)

### Craft Expert (coding / writing / visual — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice |  |  |
| Consistency with existing style |  |  |

### Verification Expert (test / fact-check / dry-run — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) |  |  |
| Coverage (edge cases / claims / steps) |  |  |

## Overall Verdict

- Self-check: OK
- QA:
- Design expert: N/A
- Craft expert:
- Verification expert:
- Ready to check off:
