# #6 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| `claude/settings.json` が有効な JSON で、リテラルのホームパス(`/Users/…` / `/home/…`)を含まず、2つの `command` がどちらも `"$HOME/…"` をダブルクォートで囲んでいる | OK | `python3 -m json.tool claude/settings.json` が無エラーで通過。`grep -rnE '/Users/\|/home/\|__HOME__' claude/` が 0 ヒット(exit 1)で、リテラルのホームパスも旧プレースホルダ `__HOME__` も残っていない。JSON をパースして 2 つの `command` を取り出し、正規表現 `"([^"]*\$HOME[^"]*)"` で `$HOME` がダブルクォート内にあることを機械的に確認: SessionStart フック = `bash "$HOME/.claude/hooks/herdr-agent-state.sh" session`、statusLine = `sh "$HOME/.claude/scripts/statusline.sh"`。さらに (1) 各 `command` をシェルに通して `$HOME` が `/Users/kiyo/...` に展開され、その実ファイルが存在することを確認、(2) 空白入りホーム(`/tmp/ho me`)を模した `set --` 実験で `argc=2` / `arg1=[/tmp/ho me/.claude/hooks/x.sh]` となり、クォートによりパスが 1 引数として保たれることを確認。`command` がシェル経由で実行される点は Claude Code 2.1.241 バイナリの hook スキーマ記述("`command` runs through a shell")と statusLine が同一ランナーを通ることから確認済みのため、プレースホルダ置換なしで成立する。 | OK | `python3 -c 'json.load'` で妥当、`grep -nE '/Users/[A-Za-z0-9_.-]+\|/home/[A-Za-z0-9_.-]+'` 0ヒット。JSON デコード後の実値が `bash "$HOME/.claude/hooks/herdr-agent-state.sh" session` / `sh "$HOME/.claude/scripts/statusline.sh"` であることを確認。さらに `HOME=/Users/testuser` と `HOME=/home/testuser` を与えて実際に展開させ、mac / WSL 双方で正しいパスになることを実行確認(文字列検査に留めていない)。 |
| 現行 `~/.claude/settings.json` との差分が「2箇所の command のパスの `$HOME` 化(クォート付与を含む)」と「`remoteControlAtStartup: false` の追加」のみで、他のキー・値が変わっていない | OK | (1) 生 diff (`diff ~/.claude/settings.json claude/settings.json`) が 3 hunk のみ: L9 のフック command、L18 の statusLine command、L36 の `agentPushNotifEnabled` へのカンマ追加+`remoteControlAtStartup: false` 行追加。(2) キー順・書式の影響を排除するため、両者をパースしてリーフ値をパス付きにフラット化して比較したところ差分は厳密に 3 点のみ(`/hooks/SessionStart/0/hooks/0/command`、`/statusLine/command`、新規 `/remoteControlAtStartup`)で、他の 10 キーは全一致。(3) トップレベルのキー順は原本と同順で、追加キーのみ末尾(既存の同種 boolean `agentPushNotifEnabled` の隣)に付加。2 スペースインデントと末尾改行(最終バイト `0x0a`)も維持。なお原本はフック側がシングルクォート・statusLine 側が裸というちぐはぐな状態だったが、`$HOME` 化に伴い両方ダブルクォートへ統一しており、これは task で明示された意図した差分。 | OK | `diff -u ~/.claude/settings.json claude/settings.json` の実出力が3箇所のみ(フック command、statusLine command、`remoteControlAtStartup: false` 追加)。他10キーは値・順序とも完全一致。原本はフック側シングルクォート・statusLine 側無クォートと不揃いだったものをダブルクォートに統一しており、これは `$HOME` 化に伴う意図した差分。 |
| `claude/scripts/statusline.sh` が `sh -n` / `dash -n` を通り、現行の `~/.claude/scripts/statusline.sh` とバイト一致する | OK | `cmp claude/scripts/statusline.sh ~/.claude/scripts/statusline.sh` が差分なしで終了(2373 バイト)。今ラウンドでは本ファイルを一切編集しておらず、バイト一致が維持されていることを再確認した。`sh -n` と `dash -n`(/bin/dash)がいずれも無エラー。異常系の smoke test も実施: 空入力 → ` \|  \| herdr4mac@worktree-herdr4mac` exit 0、不正 JSON → jq の parse error を stderr に出しつつ同様の劣化表示で exit 0、jq 不在 PATH → `C:0k \| unknown \| herdr4mac@worktree-herdr4mac` exit 0。これらが exit 0 のまま劣化表示になるのは原本由来の既知挙動であり、本タスクはバイト一致複製が要件のため改変していない。 | OK | `sh -n` / `dash -n`(`/bin/dash` 存在確認済み)ともに無エラー。`cmp` で原本と差分なし、sha256 / md5 一致(md5 3baf17ab0bef97e852cbdbedc0c9e352)、パーミッションも `-rwxr-xr-x` で一致。 |
| `claude/themes/catppuccin-mocha.json` と `claude/themes/catppuccin-latte.json` が有効な JSON で、現行 `~/.claude/themes/` の同名ファイルとバイト一致する | OK | `cp` で複製後、`cmp claude/themes/catppuccin-mocha.json ~/.claude/themes/catppuccin-mocha.json` と latte 版がいずれも差分なしで終了(各 689 バイト)。`python3 -m json.tool` が両ファイルとも無エラーで通過。原本そのままで整形し直していない。パーミッションは要求どおり 644 (`-rw-r--r--`)。これらは `settings.json` の `theme: "custom:catppuccin-mocha"` の実体であり、欠けると他 PC で Claude Code が警告なく組み込み dark にフォールバックする。 | OK | 両ファイルとも `python3 -m json.tool` 通過、`cmp` で原本と差分なし、md5 一致(mocha 7649a40c…, latte 9266931f…)。latte の末尾改行なしは原本由来でバイト一致要件上正しい。 |
| 秘密情報(APIキー・トークン等)が含まれていない | OK | `grep -rniE 'api[-_]?key\|token\|secret\|sk-ant\|password\|bearer' claude/` のヒットは statusline.sh の 8 行のみで、すべてコンテキストウィンドウのトークン数計算に関する記述(`input_tokens`, `cache_read_input_tokens`, `total_output_tokens` 等の jq フィールド名およびコメント)であり資格情報ではない。settings.json とテーマ 2 ファイルはヒット 0。全 4 ファイルを全文目視し、値はプラグイン名 `rn@ccpm`、公開 GitHub リポジトリ `lovaizu/ccpm`、テーマ名・カラーコードのみで機微情報なし。 | OK | `grep -inE 'sk-ant\|api[_-]?key\|token\|secret\|password\|bearer\|ghp_\|gho_\|xox[abp]-'` のヒットは statusline.sh のトークン数計算コメント / jq フィールド名のみ。全4ファイルを目視し、値はプラグイン名・公開リポジトリ名・配色コードのみ。正規表現ベースの既知パターン検査に限られる点は指摘あり(entropy スキャナ併用は完了基準外)。 |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | 自己申告を渡さず独立に全 criterion を再実行。表層の「クォートが付いているか」で終わらせず、`HOME` を mac 形 / WSL 形に差し替えて実際に展開させる検証を行っており、目的(両OSでの再現)に対して意味のある確認になっている。指摘2件(原本とのドリフトを継続検知する仕組みが無い / 秘密検査が既知パターンのみ)はいずれも完了基準に含まれずブロッカーではない。総合 PASS。 |

## Expert Reviews (axes the task needs)

### Craft Expert (coding / writing / visual — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | settings.json は有効 JSON・2スペースインデント・末尾改行あり。原本で不揃いだった command のクォートをダブルクォートに統一しており、空白入りホームパス対策として妥当。statusline.sh は `sh -n` / `dash -n` 通過。原本由来の弱点(`jq` の存在確認なし、latte の末尾改行なし)はバイト一致要件上「複製として正しい」と切り分けられており、`jq` ガードは #7 に計画済み。 |
| Consistency with existing style | OK | `claude/` 配下が `~/.claude/` の構造をそのまま写しており design.md 3.2 の方針と一致。`herdr/` / `iterm2/` / `windows-terminal/` がフラット1ファイルなのに対しネスト構造なのは意図した設計判断。パーミッションは settings.json 644(他の設定ファイルと一致)、statusline.sh 755(原本と一致)。setup.sh が `claude/` を未配置なのは #7 として明示的に分離済みでスコープ外。 |

### Verification Expert (test / fact-check / dry-run — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | OK | 全 criterion を独立に実行検証(`python3 -m json.tool` / `diff` / `cmp` / `sh -n` / `dash -n` / `grep`)。主張と実測の食い違いなし。 |
| Coverage (edge cases / claims / steps) | OK | GWT で5ケースを実測: 空入力 → exit 0 / `" \|  \| dir@branch"`、不正 JSON → exit 0・stderr に jq parse error 6行、`jq` 不在 PATH → exit 0・`jq: command not found` 6行で無言劣化、空白入りホームパス → `$#=3` でパスが1引数に保たれる、フック本体不在 → exit 127。未検証として残るのは Claude Code ランタイム上での実挙動(#7 の `/config` 目視確認ステップでカバー予定)と、`~/.claude/hooks/herdr-agent-state.sh` の配置順依存(#7 で不在時警告を出す計画あり)。 |

## Overall Verdict

- Self-check: OK
- QA: OK
- Design expert: N/A
- Craft expert: OK
- Verification expert: OK
- Ready to check off: Yes
