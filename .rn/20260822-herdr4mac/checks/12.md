# #12 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| `herdr/config.toml` の `[theme] name` が `gruvbox-light` | OK | `herdr/config.toml` の `[theme]` を直接確認: `name = "gruvbox-light"` / `auto_switch = false` / `dark_name = "gruvbox"` / `light_name = "gruvbox-light"` | | |
| 端末側(WT の `profiles.defaults.colorScheme`)が Gruvbox Dark | OK | `windows-terminal/settings.json` L92 `"colorScheme": "Gruvbox Dark"`、同ファイル `schemes` に `"name": "Gruvbox Dark"`(L272)が定義され `background` `#282828` / `foreground` `#EBDBB2` | | |
| 端末側(iTerm2 `herdr.json` の色)が Gruvbox Dark | OK | `iterm2/herdr.json` の `Background Color` = R/G/B 0.15686…(= `#282828`)、`Foreground Color` = 0.92157/0.85882/0.69804(= `#EBDBB2`)で WT の Gruvbox Dark と同値 | | |
| README にテーマ2層の対応が書かれている | OK | `README.md` 「テーマ・フォント」節。2層(端末そのものの配色 / その上の herdr UI テーマ)と各層の設定箇所(WT `profiles.defaults.colorScheme` / iTerm2 の色定義 / `herdr/config.toml` の `[theme]`)、暗/明を食い違わせる意図を記載 | | |
| design.md にテーマ2層の対応が書かれている | OK | `.rn/20260822-herdr4mac/design.md` §4.7(新規)。層ごとの設定の持ち主と値、暗/明が意図であること、書き戻しと丸ごと上書きの関係、破れの検出方法。§3.2 の `herdr/config.toml` の行から §4.7 を参照 | | |
| 記載が Acceptance criteria と一貫している | OK | steering.md Acceptance criteria の「`[theme]` は herdr の UI テーマが `gruvbox-light`、端末そのものは Gruvbox Dark」「README は意図と規則だけ / 色の hex・テーマ名を書かない」の両方と突き合わせ。README に `Gruvbox` / `gruvbox` / hex / `auto_switch` の文字列が残っていないことを grep で確認(該当0件) | | |

## 確かめた主張と出典

| 主張 | 当てた出典 |
|---|---|
| herdr の UI テーマは `gruvbox-light`、自動切替は無効 | `herdr/config.toml` `[theme]` |
| WT の既定プロファイルの配色は Gruvbox Dark で、スキーム実体も同ファイルにある | `windows-terminal/settings.json` `profiles.defaults.colorScheme`(L92)と `schemes` の `Gruvbox Dark`(L256-278) |
| iTerm2 側の前景/背景が WT の Gruvbox Dark と同値 | `iterm2/herdr.json` の `Background Color` / `Foreground Color` を 0-255 に換算して WT の hex と比較 |
| README は値を持たない規則がある | `design.md` §3.2 / §4.1、`steering.md` Acceptance criteria 第1項 |
| 端末が暗く herdr UI が明るいのはユーザーの意図(選択状態の判別) | `steering.md` Acceptance criteria の `[theme]` 項、および #12 Purpose |
| フォント行は #8 の担当で #12 では触らない | `steering.md` #8 Steps(「README から具体的な設定値を落とす: フォント名とサイズ…」) |

## コーディネータの独立確認

専門家レビューは回していない(`steering.md` Rules)。実装の主張を実ファイルで測り直した。

| 確認したこと | 結果 |
|---|---|
| WT と iTerm2 の配色が同値か | 背景 `#282828` / 前景 `#EBDBB2` が一致。**ANSI 16 色も1色ずつ突き合わせて不一致0** |
| `profiles.defaults.colorScheme` | `Gruvbox Dark`。`schemes` に同名スキームが実在 |
| `herdr/config.toml` `[theme]` | `name = "gruvbox-light"` / `auto_switch = false` |
| README に値が残っていないか | `gruvbox` / `#282828` / `EBDBB2` / `auto_switch` を grep して**0件** |
| design.md の参照先が実在するか | §3.2(Design overview の構成要素)/ §4.1(README spec の保証)とも実在し、§4.1 の「README は値を持たない」と §4.7 の記述が矛盾しない |

**#8 との順序**: README のテーマ記述は最初から値なしで書かせたので、#8 の書き直しがこの節を取り消す
必要はない。残っているフォント行(値を持つ)は #8 の担当。

## Overall Verdict

- Self-check: OK
- QA: N/A(Rules により専門家レビューを回していない)
- Design expert: N/A(同上)
- Craft expert: N/A(同上)
- Verification expert: N/A(同上)
- コーディネータの独立確認: OK
- Ready to check off: Yes
