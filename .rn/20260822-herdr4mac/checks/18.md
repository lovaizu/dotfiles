# #18 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| 残ったコメントがすべて「その行がなぜそうなのか」を言っている。「何をしているか」の言い換え、design.md にある方針・分類の再掲、レビュー履歴の記録は残っていない | OK | 66 個のコメント塊を1件ずつ (a) 特定の行に付いているか / (b) その行を消すか単純化すると実測済みの欠陥が戻るか で判定。落とした 341 行はすべて design.md §4.5 / §4.6 に実在を確認(下表)。例外として残したのは関数頭の「何をする関数か」1〜2行(`backup_file` / `deploy` / `sweep_tmp_files` / `tmp_prefix_for` / `backup_path_for` / `warn` / `record_failure`)のみ。`§4.6 of design.md carries the same three` を含む 3分類の再掲(52行)、`deploy` ヘッダの方針再掲(66行)、`FAILURES` の部分適用論(26行)、trap の SIG_IGN 経緯(34行)、XDG の方針再掲(28行)は全削除。レビュー経緯・不採用代案の議論は 0 件残存(`grep -in 'review\|alternative\|instead of\|cost more than'` で残るのは `deploy` の "Returning it anyway cost more" = errexit が落ちていた実測1件のみ) | | |
| コードが1バイトも変わっていない: 変更前後で `grep -vE '^\s*#' setup.sh` の結果が完全一致 | OK | `diff <(grep -vE '^\s*#' setup.sh) scratchpad/code.before` → 出力なし(283行、空行22行を含めて同一)。ファイルモードも `-rwxr-xr-x` のまま | | |
| `bash -n` と `shellcheck -s bash` が通る | OK | `bash -n setup.sh` → exit 0。`shellcheck -s bash setup.sh` → 出力なし / exit 0 | | |
| 隔離ホームで2回流して exit 0 / 0、2回目は `Up to date` | OK | `T=$(mktemp -d …)` の空ホームに `env -i HOME=$T PATH=/usr/bin:/bin:/usr/sbin:/sbin bash ./setup.sh` を2回。exit 0 / 0。2回目は `Up to date: HOME/.config/herdr/config.toml` と `Up to date: HOME/Library/Application Support/iTerm2/DynamicProfiles/herdr.json` の2行 + brew 不在の WARNING + `Done.` | | |
| 配置されたファイルが dotfiles と `diff` 一致 | OK | `diff $T/.config/herdr/config.toml herdr/config.toml` → 差分なし。`diff "$T/Library/Application Support/iTerm2/DynamicProfiles/herdr.json" iterm2/herdr.json` → 差分なし | | |
| 出力が変更前と同一 | OK | 両 log のホームのパスを文字列 `HOME` に置換した上で `diff after1.log run1.before` / `diff after2.log run2.before` → いずれも差分なし | | |
| design.md / steering.md と矛盾しない | OK | 落とした内容は §4.5 / §4.6 に実在を1件ずつ確認。design.md に無かった2件(変数名の大小文字規約、半端な TOML / JSON の切り方の実測)は setup.sh から落とす前に design.md §4.6 へ移した。steering.md の #18 Steps / Completion criteria とも一致(コード行は不変、コメントのみ2分して後者を削除) | | |

## 行数

| | 変更前 | 変更後 |
|---|---|---|
| 総行数 | 865 | 524 |
| コード | 261 | 261 |
| コメント | 582 | 241 |

空行22行は両者とも `grep -vE '^\s*#'` 側(283行)に含まれる。コメント : コード比は 2.2 : 1 から 0.92 : 1 へ。

## 残したコメントと、それが守っているもの

66 塊。全件を掲げる(行番号は変更後の setup.sh)。

| 場所 | 守っている行 | 消すと戻る欠陥 |
|---|---|---|
| L4-8 | `: "${HOME:?…}"` が trap より前にあること | trap の後に置くと bash 3.2 が `set -u` 致命を exit 0 で返す。以前は herdr の config だけ配って iTerm2 を配らず、失敗も列挙せず exit 0(実測) |
| L13-21 | `case "$value" in /*)` と、答えを `xdg_base` で返すこと | 相対 XDG 値をそのまま使うと配置先が cwd 次第になる(実測)。stdout で返すとコマンド置換のサブシェルで `XDG_IGNORED` が消える |
| L38-40 | `set_xdg_base XDG_CONFIG_HOME …` を通すこと | `$HOME/.config` を直書きすると herdr が読まないファイルを配って exit 0(実測) |
| L44-49 | `BACKUP_DIR` が配置先の隣でないこと | `.bak` を DynamicProfiles に置くと同じ Guid の二重プロファイルとして読まれる(バイナリ文字列からの推論) |
| L53-55 | `FAILURES=()` | 何をためる配列か(関数頭に準じる1〜3行)+ §4.6 参照 |
| L58-65 | `tmp_prefix_for` の先頭ドットと、形式を1か所で組むこと | ドットが無いと配置中のファイルが iTerm2 に profile として読まれる。形式を2か所に書くと生成と掃除が無言で食い違う |
| L70-73 | `tmp_for` の `$$` | pid が無いと2本の run が1つのファイルに書く。同時実行の安全性は買わないことも併記(掃除が他 run の分も消す、実測) |
| L78-81 | `backup_path_for` の basename 単独 | 同じ basename の管理対象が2つになると `.bak` も掃除の glob も区別できない(規律のまま、§4.6) |
| L86-90 | `sweep_tmp_files` の共有プレフィックス glob | 掃除が無いと SIGKILL で残った一時ファイルを誰も消さない。先頭ドットで `ls` にも出ない(実測) |
| L95-96 | `DEPLOY_TMP=""` / `BACKUP_TMP=""` | 空でよいこと(`rm -f ""` は exit 0 で無言、実測)= 空文字列ガードを足す理由が無い |
| L100-101 | `rm … 2>/dev/null \|\| true` | ディレクトリごと消えた dst では rm が鳴く |
| L106-107 | `trap … INT` / `TERM` が `exit` すること | return だけの trap は bash を次の配置へ進ませ、Ctrl-C が何も止めない |
| L109-115 | `REACHED_END` と `[ "$st" -eq 0 ]` | 番兵が無いと `set -u` 致命が exit 0 で通る(実測、bash 3.2.57、`$?` を読み直しても直らない)。`$st` の検査が無いと SIGTERM の 143 が潰れる |
| L129-133 | `warn` の出力が全部 stderr であること | stdout に出すと説明と cp の complaint が別ストリームに割れ、片方だけ読む呼び出し側が半分しか受け取らない(実測) |
| L135-137 | `warn` 末尾の `return 0` | errexit 下の WSL 分岐で、`record_failure` の戻り値が最後の echo 次第になる |
| L150-151 | `record_failure` | 何をする関数か(2行) |
| L157-158 | `backup_file` | 何をする関数か(2行) |
| L160-164 | `while [ -e "$bak" ]` の採番 | 秒単位のタイムスタンプなので、同じ秒の2本目が deploy の約束したパスを上書きする(実測: herdr のテーマ書き戻しで `-1.bak` が出る) |
| L166-168 | 退避先を stdout で返すこと | グローバルだと後の配置の失敗が、前の配置がユーザーに約束した退避を消す(実測) |
| L170-171 | `tmp` を引数で受けること | 名前を中で決めるとコマンド置換のサブシェルが trap を持たない(deploy 側に理由の本文) |
| L174-176 | `[ -e "$dst" ] \|\| return 0` | dangling symlink では退避を取らずに実体ファイルへ置き換わる境界(§4.6 (c)) |
| L178-179 | `mkdir -p "$BACKUP_DIR" \|\| return 1` | 退避先が作れなければ配置もしない(呼び出し側が退避を約束する直前だから) |
| L187-190 | `cp … && mv -f "$tmp" "$bak"` の一時名経由 | 丸ごと `cp` すると Ctrl-C で部分的な `.bak` が「完全な退避」の名前で残る(実測) |
| L198-199 | `deploy` | 何をする関数か(2行)+ §4.6 参照 |
| L201-205 | `deploy` が常に 0 を返す契約 | 戻り値を返すと呼び出し側の `\|\| true` が関数内部の errexit まで落とし、裸の行が無言で失敗して exit 0(実測) |
| L208-212 | `sweep_tmp_files` 2本が `cmp` より前にあること | 後ろだと収束済みマシンを一度も掃かず、中断で残った一時ファイルが永久に残る(実測: 3回の run を生き延びた) |
| L215-223 | `cmp -s … 2>/dev/null` と「identical だけ読む」こと | cmp は未存在の dst に 2 を返す(初回配置の普通の値)。リダイレクトが無いと管理対象を名指ししない "Is a directory" だけが出る |
| L228-231 | `[ -L "$dst" ]` を rename の前に置き、警告は後に出すこと | 前で警告すると、配置に失敗した run が「リンクは消えた、張り直せ」と言うのにリンクは無傷(実測) |
| L237-241 | `BACKUP_TMP="$(tmp_for …)"` が `deploy` 側にあること | コマンド置換の中で名前を持つとサブシェルが trap を持たず、SIGINT で書きかけが残る(実測) |
| L243-244 | `local … backup=""` | ローカルかつ空なので、下の後始末はその配置自身の退避しか消せない |
| L246-247 | `BACKUP_TMP=""` のリセット | rename 済みの名前を trap が消しにいかないため |
| L249-252 | `mv -f` の `-f` | tty があると mv が確認プロンプトを出し、既定 "n" で何も置き換えず exit 0(実測、pty 越し)= 「配置した」と嘘をつく |
| L254 | `DEPLOY_TMP=""` | rename が持ち去ったので掃除対象が無い |
| L256-257 | `Backed up …` を rename の後に出すこと | 実際に置き換わった内容についてしか退避を約束しない |
| L262-264 | `was_link` の warn(record_failure ではない) | 退避で取り消せない唯一の破壊なので黙らない。管理対象は正しく配置されるので失敗には数えない |
| L278-281 | 失敗経路で `backup` を捨てること | rename に到達していないか rename が失敗したかなので dst は無傷、その退避は同内容の写し。捨てないと失敗のたび1本増える(実測: 4回で4本) |
| L283-285 | `rm -f "$backup" \|\| true` | ここだけ失敗を許す行であること(捨てる側なので報告は変わらない) |
| L308-309 | XDG の warn がこの位置にあること | 値を捨てた場所ではまだ `warn` が存在しない |
| L324-326 | `herdr not found. Its config is managed here all the same:` の文言 | 下の deploy は何も書かないことがあるので、install と読ませない |
| L334-337 | `iterm_dir` を `deploy` に渡す(= `mkdir -p` させる)こと | 投函箱と footprint の違い。WSL 側と逆であることの理由は §4.5 |
| L341-348 | `iterm_profile_guid` のリテラル | `iterm2/herdr.json` の `"Guid"` との tie。JSON にコメントが書けないのでこちら側に置く。片方だけ変えると既定にしてあるマシンでも毎回警告が出て意味を失う |
| L351-357 | `if default_guid="$(defaults …)"; then … else` の2分岐 | 非0(ドメイン無し)を不一致と見なすと、iTerm2 未導入のマシンに「Settings で既定にしろ」と言う(実測) |
| L377-379 | brew 分岐が `warn` で済ませること | フォントは管理対象ではないので失敗に数えない(§4.5) |
| L403-408 | `grep -qi microsoft /proc/sys/kernel/osrelease` | ツールの不在を非 WSL の証拠にすると、interop 無効の実 WSL が「配る先が無い」に誤分類され exit 0(実測) |
| L410-411 | 非 WSL の `echo … >&2` | 配らなかった通知を stdout に残すと、stdout だけ取る呼び出し側に「問題の無い run」に見える |
| L414-416 | ここが `record_failure` であること | 上の分岐と違い、配る先はあるのに配れていない |
| L428-435 | cmd.exe の `\|\| true` と stderr を残すこと | errexit 下で cmd.exe が動かないと、下の case が3経路を言い分ける前に run が終わる。stderr は唯一の手掛かり |
| L437-445 | `if [ -n "$appdata" ]` と `if resolved=…; then appdata="$resolved"` | 無条件代入だと失敗した wslpath が空を返し、「リテラル」と「無回答」が診断上同じ見た目になる(実測、スタブ3種) |
| L451-459 | `case "$appdata" in /*)` | 空だけを弾くとリテラル `%LOCALAPPDATA%` が `wt_dir` まで運ばれ、「WT 未導入」として exit 0(実測)。判定の限界は §4.5 |
| L466-469 | `LocalState` を作らずスキップすること / `>&2` | パッケージ footprint に誰も読まない settings.json を置かない |
| L474-478 | ここが `record_failure` であること | WT 未導入とは別の事象(Windows 側はあるが AppData の場所が分からない) |
| L480-481 | 失敗メッセージが `${appdata:-…}` を引用すること | cmd.exe の答えを残しているので3経路が紙面で分かれる |
| L498-502 | `[ "${#FAILURES[@]}" -gt 0 ]` のガード | `"${arr[@]}"` は bash 3.2 の `set -u` で空配列を unbound と読み、EXIT trap ありなら exit 0 で静かに死ぬ(実測、3.2.57) |
| L504 | 失敗列挙の `>&2` | まとめる対象の WARNING と同じストリームに置く |
| L510-511 | `Everything else was deployed or skipped as noted.` の文言 | 「他はすべて配置した」と言わない(スキップがある) |
| L513-515 | `REACHED_END=1` がブロックの末尾にあること | 先頭で立てると、列挙の途中で死んだ run も「意図した終端」になる |
| L521-523 | 末尾の `REACHED_END=1` | 印の無い exit 0 は意図しない終わり方(trap 側の判定と対) |

このほかに、関数の頭の「何をする関数か」1〜2行のみの塊が残っている(`tmp_prefix_for` / `sweep_tmp_files` / `record_failure` / `backup_file` / `deploy`)。これは (b) を満たさないが、読む人が最初に要る情報として意図的に残した。

## 落としたコメントと、行き先

| 落とした内容 | design.md に実在したか | 対応 |
|---|---|---|
| 通知の3分類(record_failure / warn / 素の echo)の定義と、フォントがブロックでスキップが1行である理由(52行) | あり — §4.6「通知の3形態の使い分け」。コメント自身が `§4.6 of design.md carries the same three` と申告していた | 削除。`warn` には形の1行と stderr の理由(実測)だけ残す |
| `deploy` ヘッダの方針再掲: dotfiles が正 / 一致する dst は書かない / rename の原子性 / rename の代償(モード・ディレクトリ権限)/ symlink 3ケース / 電源断は守らない(66行) | あり — §4.6 の「原子性」「代償」「境界」各項 | 削除。`deploy` ヘッダは 2行の「何をする関数か」+「常に 0 を返す」の実測のみ |
| 半端な JSON / TOML の被害の実測(行境界20通り・バイト境界368通り・`config: ok`) | **無し** | design.md §4.6 の原子性の段落へ移してから削除 |
| `FAILURES` の部分適用・冪等な再試行・stdout/stderr の分け方・`\| head -2` の SIGPIPE(26行) | あり — §4.6「部分適用…」「stdout を途中で読むのをやめる呼び出し方には耐えない」 | 削除。`FAILURES` には3行の要約 + §4.6 参照 |
| XDG の方針再掲: 仕様の引用、2つの cwd からの実測、herdr が相対値を cwd から解決する実測(28行) | あり — §4.6「XDG の値が絶対パスでなければ…」 | 削除。判定の理由5行のみ残す |
| trap の SIG_IGN 経緯(非対話シェルの `&` ジョブが SIGINT を無視のまま継承する最小再現)(34行のうち12行) | あり — §4.6 境界「trap が効かないのは、SIGINT を無視した状態で起動された場合だけ」 | 削除 |
| `BACKUP_DIR` が `XDG_STATE_HOME` を見る理由(スクリプト内で XDG の扱いを非対称にしない) | あり — §4.6「BACKUP_DIR はどのプログラムも読まない」 | 削除 |
| `cmp` が PATH に無い機械(127)を検査しない理由 | あり — §4.6 境界「冪等性は `cmp` が PATH にあることに乗っている」 | 削除 |
| `mkdir -p` が残す空ディレクトリを消さない理由(3経路の実測) | あり — §4.6 境界「配置に失敗した run は空のディレクトリを残すことがある」 | 削除(失敗メッセージ本文が既に述べている) |
| 配置先のモードが収束しない実測(umask 077 / 022) | あり — §4.6 境界「配置先のモードは収束しない」 | 削除 |
| `backup_path_for` の basename 一意性を実行時検査にしない理由(Guid を grep に委ねたのと同じ) | あり — §4.6 境界「管理対象の basename は一意でなければならない」 | 削除。規律の1行 + §4.6 参照のみ |
| `backup_file` / `tmp_for` の同時実行の実測(TOCTOU、`.bak` 1本) | あり — §4.6 境界「同時実行は支援しない」 | 削除 |
| `defaults` が `HOME` を見ない(隔離ホームで完結しない) | あり — §4.5 | 削除 |
| `wslpath` がリテラルを拒むかは未検証、WT がプロファイルを作り直すかは未検証 | あり — §4.5 と §4.6 境界(未検証事項3件) | 削除。判定の末尾に §4.5 参照1行 |
| `iterm_dir` の DynamicProfiles(投函箱)と LocalState(footprint)の対比の全文 | あり — §4.5「配る先のディレクトリをこちらで作ってよいアプリか」 | 削除。両側に2〜4行 + §4.5 参照 |
| `iterm_default_menu` / `font_cost` を1か所に持つ理由(2つ書けばズレる) | — コードが自分で言っていること | 削除(移す先も不要) |
| brew 不在も warn を通す理由(同じ知らせを2ストリームに割らない) | あり — §4.5「両者は同じ知らせ…どちらも同じ警告経路 = stderr」 | 削除 |
| WT が `settings.json` を自分で書くこと / 配布時のディストロ GUID | あり — §4.6 と境界 | 削除 |
| ファイル直下の変数名の大小文字規約(4行) | **無し** | design.md §4.6 の機構の箇条書きの後へ移してから削除 |

## コーディネータの独立確認

専門家レビューは回していない(`steering.md` Rules)。4つの検証をすべて自分で走らせ直した。

| 確認したこと | 結果 |
|---|---|
| コードが1バイトも変わっていないか | `diff <(grep -vE '^\s*#' setup.sh) code.before` → **空**(283行、空行含め完全一致)。実行ビットも `-rwxr-xr-x` のまま |
| 構文と lint | `bash -n` → 0、`shellcheck -s bash` → 出力なし / 0 |
| 出力が変更前と同一か | 新しい隔離ホームで2回実行 → exit 0 / 0。ホームのパスを正規化した出力が `run1.before` / `run2.before` と **`diff` 完全一致** |
| 配置されたファイル | `config.toml` / `herdr.json` とも repo の原本と `diff` 一致 |
| 行数 | 総 865 → **524**、コード 261 → **261**(不変)、コメント 582 → **241**。比 2.2:1 → 0.92:1 |
| 落とした内容が design.md に実在するか | 抜き取りで確認。symlink の3ケースは §4.6 に (a)(b)(c) として実在し、`mv` のあとに警告を出す理由(失敗した run が「リンクは消えた」と嘘をつく実測)まで書かれている。通知3分類も §4.6 に実在 |
| 残ったコメントの質 | 最大の塊は 66行 → **15行**(`backup_file` の頭)。中身は「何をする関数か」2行 + 秒単位タイムスタンプの衝突回避・退避パスをグローバルで持たない理由・一時ファイル名を渡す理由の3つで、いずれも**特定の行を守る実測付きの why**。規則を満たしている |
| design.md へ移した2件 | 変数名の大小文字規約と、半端な TOML/JSON の被害の実測(行境界20通りすべて `config: ok`・うち15通りが `[keys]` 欠落)。後者は **rename を採る根拠が甘い側の読みに乗っていることを言う唯一の記録**で、消さずに移した判断は正しい |

関数頭の「何をする関数か」1〜2行は規則 (b) を満たさないが、意図的に残したという実装の判断に同意する
— 読む人が最初に要る情報で、これが無いと setup.sh 単体でコードを追えなくなる(#18 の制約)。

## Overall Verdict

- Self-check: OK
- QA: N/A(Rules により専門家レビューを回していない)
- Craft expert: N/A(同上)
- Verification expert: N/A(同上)
- コーディネータの独立確認: OK(4つの検証を再実行して同じ結果)
- Ready to check off: Yes
