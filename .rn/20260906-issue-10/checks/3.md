# 3 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| 実行後、配置先の `CLAUDE.md` がリポジトリの原本と `diff` 一致する | OK | `[real-1]` 実 `$HOME` で初回実行。`Installed /Users/kiyo/.claude/CLAUDE.md` を出し、`diff claude/CLAUDE.md "$HOME/.claude/CLAUDE.md"` が無差分 (md5 とも 20b9c5e46a61fc0cbac4ec8a44546a7a)。対照 `[ctl-1]`: 配置先に1行足すと同じ `diff` が差分を出すので、この一致は空振りではない。 |OK | QA・検証の2名が退避 HOME で独立に再現。配置先ディレクトリが存在しない状態から `deploy` の `mkdir -p` で作られ、`diff` 一致・md5 一致。**failing control**: 配置後に1バイト足すと同じ `diff` が差分を報告する。 |
| 2回目の実行が `Up to date` と表示し、バックアップを増やさない | OK | `[real-2]` 続けて2回目を実行。`Up to date: /Users/kiyo/.claude/CLAUDE.md` を出し、バックアップ数は 18 → 18、`CLAUDE.md.*.bak` は0件のまま。対照は `[real-1]` 自身 — 内容が違えば `Up to date` ではなく `Installed` になる。 |OK | 2名とも `Up to date` を再現し、バックアップディレクトリがそもそも作られないことまで確認。**failing control**: 配置先を書き換えると同じ2回目が `Backed up … / Installed` に変わり、バックアップが 0 → 1 になる。無条件に `Up to date` と出るわけではない。 |
| 既存ファイルがある状態での実行がバックアップを残し、その名前が既存3つのバックアップと衝突しない | OK | `[scratch-C]` 退避 HOME に別内容の `CLAUDE.md` を置いて実行 → `CLAUDE.md.20260912120719.bak` ができ、中身は退避前の "pre-existing local notes"。同じ実行で `config.toml.*.bak` `herdr.json.*.bak` も並び、上書きも取りこぼしもない。名前は `backup_path_for` = `$BACKUP_DIR/$(basename dst)` + 秒タイムスタンプなので、衝突条件は basename の一致だけ。管理4ファイルの basename は `CLAUDE.md` / `config.toml` / `herdr.json` / `settings.json` の4種で重複なし `[names]`。`[scratch-F]` 連続2回のバックアップも `-1.bak` の数え上げ以前に別タイムスタンプで2本残り、互いを消さない。 |OK | 3つの配置先すべてに別内容を置いて1回実行し、`CLAUDE.md` / `config.toml` / `herdr.json` の退避が**同一秒**に3本できて衝突も混線もしないことを確認。同一秒の再実行では `-1` `-2` と連番が付き、どの世代も失われない。**failing control**: `config.toml` と同じ basename を持つ5本目の deploy を足した改変版では、無関係な2つが1つの名前空間に押し込まれ連番でしか区別できなくなる。つまり「basename が別」は自明に通る条件ではない。 |
| 配置に失敗させた実行が FAILURES に載り、非ゼロで終わる | OK | `[scratch-D]` 退避 HOME の `~/.claude` を `chmod 500` にして実行 → 終了コード 1、`Finished with 1 failure(s):` に `.../.claude/CLAUDE.md was not deployed.` が載る。配置先の旧内容 "stale" はそのまま、tmp の残骸なし、herdr と iTerm2 の配置は続行済み。対照は `[real-1]`/`[real-2]`/`[scratch-A]` の rc=0 で、失敗を仕込まない限り FAILURES は空。 |OK | 4通りの壊し方(`mkdir -p` 失敗 / `cp` 失敗 / 原本削除 / 退避先が書けない)すべてで終了コード 1 と `Finished with 1 failure(s):` への記載を確認。`deploy` が常に 0 を返す設計でも、`record_failure` が返る前にグローバルの `FAILURES` に積むので伝播する。呼び出しはすべてトップレベルでサブシェルを経由しない。退避を取った後に `mv` が失敗する経路(`chflags uchg`)も確認し、配置先の内容は元のまま・取った退避は巻き戻され・一時ファイルも残らない。失敗しても後続の deploy は続き、実行全体は止まらない。 |
| 既存3ファイルの配置結果が、この変更の前後で変わらない | OK | `[scratch-E]` 同じ内容で種をまいた2つの退避 HOME に、変更前 (`git show HEAD:setup.sh`) と変更後を実行。配置後の `config.toml` と `herdr.json` は md5 一致 (74ce2a97…, f5eb5ea8…)、取られたバックアップ集合もタイムスタンプを伏せると `CLAUDE.md.TS.bak` の増分のみ、stderr は完全一致、stdout も `CLAUDE.md` の行を除けば完全一致。対照 `[ctl-5]`: 配置後の `config.toml` を1行変えると同じ比較が差分を出す。実機側も iTerm2 プロファイルの md5 が実行前後で f5eb5ea8… のまま `[real-state]`。 |OK | 同じ種をまいた退避 HOME に変更前後のスクリプトを流し、終了コード・stdout・stderr・`$HOME` 配下全ファイルの md5 ツリーを比較。既存3ファイルとその退避はすべて一致し、差分は `CLAUDE.md` の2行と退避1本だけ。**重要な訂正**: self-check の `[scratch-E]` は timestamp の正規化が BSD `sed` で効いておらず、かつ2回の実行がたまたま同じ秒に入ったために一致していた。`sed -E` と 1.2 秒の間隔を入れて再実行しても結論は同じ。**failing control**: iTerm2 プロファイルを1つ上の階層に配置する回帰を仕込むと md5・ファイルツリー・stdout の3軸すべてで検出された。なお stdout だけの比較では既存ファイルの内容回帰を**検出できない**ことも実証されたので、この基準は内容ツリーで見る必要がある。 |

```sh
# 作業ディレクトリはすべて worktree ルート。S は退避用スクラッチ。
R=/Users/kiyo/work/lovaizu/dotfiles/.claude/worktrees/issue-10
S=<scratchpad>/t3

# [static] 構文と lint
bash -n setup.sh                       # -> OK (無出力)
shellcheck setup.sh                    # -> 指摘なし

# [scratch-A] まっさらな退避 HOME で初回とその次
env HOME="$S/A" "$R/setup.sh"          # -> Installed x3 (herdr / CLAUDE.md / iTerm2), Done., rc=0
env HOME="$S/A" "$R/setup.sh"          # -> Up to date x3, rc=0, バックアップ 0 件のまま

# [scratch-C] 既存ファイルありでのバックアップと命名
printf 'pre-existing local notes\n' > "$S/C/.claude/CLAUDE.md"   # herdr/iTerm2 にも別内容を置いて
env HOME="$S/C" "$R/setup.sh"
ls -1 "$S/C/.local/state/dotfiles-backups"
# -> CLAUDE.md.20260912120719.bak / config.toml.20260912120719.bak / herdr.json.20260912120719.bak
cat "$S/C/.local/state/dotfiles-backups/"CLAUDE.md.*.bak         # -> pre-existing local notes

# [names] 衝突条件は basename のみ (backup_path_for / backup_file)
for f in claude/CLAUDE.md herdr/config.toml iterm2/herdr.json windows-terminal/settings.json; do basename "$f"; done | sort | uniq -c
# -> 1 CLAUDE.md / 1 config.toml / 1 herdr.json / 1 settings.json  (4種すべて相異なる)

# [scratch-F] 続けて2回バックアップしても互いを消さない
env HOME="$S/F" "$R/setup.sh"   # 1回目: "first local version" を退避
env HOME="$S/F" "$R/setup.sh"   # 2回目: "second local version" を退避
# -> CLAUDE.md.20260912120826.bak / CLAUDE.md.20260912120828.bak の2本、中身も別

# [scratch-D] 失敗経路: 配置先が書けない
chmod 500 "$S/D/.claude"; env HOME="$S/D" "$R/setup.sh"; echo "rc=$?"
# -> rc=1 / "Finished with 1 failure(s):" に CLAUDE.md was not deployed.
# -> 旧内容 "stale" はそのまま、.dotfiles-tmp.* の残骸なし

# [scratch-E] 変更前後の比較 (既存3ファイルの配置結果)
git show HEAD:setup.sh > "$S/old/setup.sh"      # 同じ木構造にシンボリックリンクを張って実行
env HOME="$S/E_old" "$S/old/setup.sh"; env HOME="$S/E_new" "$R/setup.sh"
diff "$S/E_old/.config/herdr/config.toml"                      "$S/E_new/.config/herdr/config.toml"
diff "$S/E_old/Library/.../DynamicProfiles/herdr.json"          "$S/E_new/Library/.../DynamicProfiles/herdr.json"
# -> どちらも無差分。バックアップ集合の差は CLAUDE.md.TS.bak の1本だけ、stderr は一致、
#    stdout も CLAUDE.md の行を落とせば IDENTICAL。

# [real-1] 実機 (実行前に $HOME/.claude/CLAUDE.md が無いことを確認済み)
./setup.sh                                       # -> Installed /Users/kiyo/.claude/CLAUDE.md, Done., rc=0
diff claude/CLAUDE.md "$HOME/.claude/CLAUDE.md"  # -> 無差分

# [real-2] 実機2回目
./setup.sh                                       # -> Up to date: /Users/kiyo/.claude/CLAUDE.md, rc=0
ls -1 "$HOME/.local/state/dotfiles-backups" | wc -l              # -> 18 (1回目の後と同じ)
ls -1 "$HOME/.local/state/dotfiles-backups" | grep -c '^CLAUDE'  # -> 0

# [real-state] 実機の実行前後の md5
# 前: herdr config c62746cc… / iTerm2 f5eb5ea8… / CLAUDE.md 不在
# 後: herdr config c62746cc… / iTerm2 f5eb5ea8… / CLAUDE.md 20b9c5e4… (= リポジトリ原本)
#     ※ 実行は herdr config をリポジトリ版で上書きした (手元で手を入れた内容が入っていたため)。
#        バックアップ config.toml.20260912120840.bak が残っており、中身を実行前に戻してある。

# 対照 (通るべきでないときに通らないことの確認)
# [ctl-1] printf 'x\n' >> "$S/C/.claude/CLAUDE.md"; diff -q claude/CLAUDE.md "$S/C/.claude/CLAUDE.md"  -> 差分あり
# [ctl-5] printf 'tampered\n' >> "$S/E_new/.config/herdr/config.toml"; diff ...  -> 差分あり
```

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | 5基準すべてに failing control が付き、「通った」ではなく「正しいことをした」を見ている。基準5は主張ではなく変更前後の実行比較で確かめている。self-check の弱点2点(同一秒の偶然一致、同一秒連番の未到達)はレビュー側で塞いだ。 |

## Expert Reviews (axes the task needs)

### Craft Expert (coding)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | N/A | 追加は既存の `deploy` 呼び出し1行のみで、直上の herdr の行と同じ形。`bash -n` と `shellcheck` はいずれも clean。専門家を1名立てる規模ではないと coordinator が判断し、代わりに diff を直接読んで確認した。 |
| Consistency with existing style | N/A | 同上。変数を起こさずパスを直書きしたのは、既存の `HERDR_CONFIG`(`set_xdg_base` がグローバルで答えるため)や `iterm_dir`(行が長いため)と違い、変数にする理由が無いから。design.md を引く注釈を付けなかったのは、このセッションが design.md を持たないため。 |

### Verification Expert (test)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | OK | `setup.sh` 全370行と `git show 6bc2142` を読んだうえで、すべての確認が実スクリプト(またはバイト一致の複製)を実行している。 |
| Coverage (edge cases / claims / steps) | OK | 5基準すべてに failing control。加えてシンボリックリンクの配置先、切れたリンク、`HOME` 未設定、`HOME` 空、相対値の `XDG_STATE_HOME`、`HOME` に空白を含む場合、同一秒の連番、退避後の `mv` 失敗からの巻き戻しを確認。唯一の空白は WSL の `settings.json` 経路で、Darwin では実行できないため読みによる確認。追加行は OS 分岐の外にあり、共有する状態は append のみの `FAILURES` と basename の異なる退避先だけなので、この範囲では妥当。 |

## Overall Verdict

- Self-check: OK
- QA: OK
- Craft expert: N/A(1行の追加、規模に見合わないため立てず。coordinator が diff を確認)
- Verification expert: OK
- Ready to check off: Yes

今後に持ち越す指摘(このタスクの欠陥ではない):

- 退避の名前空間は basename だけで決まり、実行時の検査は無い。Issue #9 が `claude/settings.json` を戻すと Windows Terminal の `settings.json` と同じ basename になり、WSL では**同じ実行の中で**両方が配置される。連番で上書きは防げるが、どちらの退避か区別できなくなる。#9 の範囲で扱う。
- 実 `$HOME` への実行は、手編集された `~/.config/herdr/config.toml` を一度上書きした(退避から復元済み)。退避 HOME でも同じ基準を確かめられたので、実 `$HOME` で流す必要は無かった。
- 既存の退避ディレクトリには、いまは配置対象でない `statusline.sh` の退避が3本残っている(`e5f607a` 以前の名残)。`CLAUDE.md` はこれとも衝突しない。
