# 2 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| 別のマシンで Claude Code を起動したときに、報告の仕方・調査の仕方・ドキュメントの書き方について、いま与えている指示と同じ前提が効く | OK | Obedience probe on this Mac (`cmd A`, `cmd B` below). `~/.claude/CLAUDE.md` was absent beforehand (`test -e` → ABSENT), so nothing of the user's was touched; both runs were made from the scratchpad `/private/tmp/.../scratchpad`, outside any repository, so no project CLAUDE.md was in play. **Negative control** (`cmd A`, file absent): asked `What is 2+2? Answer in one line.` → reply `4`, no marker. **Probe** (`cmd B`): copied `claude/CLAUDE.md` to `$HOME/.claude/CLAUDE.md`, appended a directive the file does not otherwise contain (`- End every reply with the marker ZQX-7f3k9m on its own last line.`), asked the same unrelated question → reply `4` followed by `ZQX-7f3k9m`. The marker was acted on, not quoted, and it appears only when the file is present, so the file is read and obeyed. `perl -e 'alarm 120; exec @ARGV'` stands in for `timeout`, which macOS lacks; the chain uses `;` so `rm` always runs, and it ended with `REMOVED` (`test ! -e` passed) — `$HOME/.claude/` is as it was found. The body covers reporting (bullet 2: conclusion + grounds + options, briefly), investigating (bullet 3: investigate or ask, never guess), and documents (bullet 6: keep only the why). |OK | 独立に再検証。3 領域は削除後も残っている(bullet 2 / 3 / 6)。落とした 2 行はどの領域でもないので、この基準を後退させていない。**self-check の記載を 1 点訂正**: obedience probe が示すのは「`~/.claude/CLAUDE.md` が読まれ従われる」ことであって、7 行それぞれが守られることではない。経路の証明であり内容の証明ではない。 |
| どの項目も dotfiles リポジトリ固有の事情に依存していない(他のリポジトリで読まれても意味をなす) | OK | `cmd C` (positive control) / `cmd D` (target). The pattern uses real alternation, not an escaped pipe. Against a throwaway file containing every forbidden word it matched line 1 (exit 0), proving the pattern works; against `claude/CLAUDE.md` it matched nothing (exit 1). The throwaway file lived in the scratchpad and was deleted. Read-through: every line is about how Claude works with the user in any repository; the only repository reference is "the language the repository uses", which is generic. **2026-09-12, new bullet 8** (`- When you delete, name the path you mean. Never a pattern whose reach depends on a variable being set.`): `cmd D` re-run over the 9-line body still matches nothing (exit 1). New positive control `cmd C2` runs the same pattern over a repo-bound rewrite of this very line (`- In this dotfiles repo, delete only under setup.sh's tree.`) and matches (exit 0), so the check would catch a repo-bound version of this line instead of passing vacuously. The line names no repository, tool, or path of this one; `rm -rf "$S"/*.out` is a shape writable in any repository. |OK | `grep -niE 'dotfiles\|herdr\|iTerm\|setup\.sh\|\brn\b\|Windows Terminal\|lovaizu'` を coordinator が再実行。対照ファイルで一致(exit 0)、本文で不一致(exit 1)。このマシンに `aiya` は未インストールで、プラグインが 1 つも無い状態でも 7 行すべて意味をなすことを確認。 |
| 指示が守れなかったときにそれと分かる程度に具体的で、「適切に」「正しく」のような判定できない語で書かれていない | OK | `cmd E` (positive control) / `cmd F` (target): matched line 2 of the throwaway file (exit 0), matched nothing in `claude/CLAUDE.md` (exit 1), so no undecidable qualifier is used. Each bullet names an observable action or output: run it and check against the real thing, with "It ran"/"tests passed" explicitly ruled out; conclusion + grounds + options with one recommendation; investigate or ask instead of guessing; converse in Japanese / write in the repository's language; common words, not jargon; documents keep only the why; no temp files or backups left. **2026-09-12, new bullet 8**: `cmd F` re-run matches nothing (exit 1); new positive control `cmd E2` runs the same pattern over a vague rewrite of the same line (`- Delete things appropriately, using patterns correctly.`) and matches (exit 0). How a breach is visible, and by looking at what: the Bash calls in the transcript — a deletion whose target begins at a variable rather than at a literal path has broken it. `cmd I` mechanizes that reading: over four candidate commands it matches `rm -rf "$S"/*.out` and `rm -rf $TMPDIR/*`, and does not match the two that name their path (`rm -rf /Users/kiyo/.../checks/scratch.out`, `rm -rf .rn/20260906-issue-10/checks`). Both load-bearing phrases are decidable by inspection: "name the path you mean" — is a literal path written in the command; "whose reach depends on a variable being set" — would the target change if that variable were empty. |OK | `grep -niE 'appropriate\|properly\|correctly\|suitable\|adequate'` を再実行し、対照で一致・本文で不一致。軟らかい語(`briefly` / `common words` / `the real thing`)はいずれも同じ行の対比で判定可能になっている。 |
| 本文が 9 行(役割 1 文 + 指示 8 行)で、見出しやタイトルが足されていない。plan gate で合意した本文(`claude-md-agreed.md`)から、手順に属する 2 行を落とし、意味を変えない英語の手直しを加え、削除の書き方の 1 行を足したものになっている | OK | `cmd G`: `wc -l` = 9 (role sentence + blank + 7 bullets = 8 content lines), `grep -c '^- '` = 7, `grep -c '^#'` = 0, `od -c` shows the file ends `behind. \n` — a single trailing newline. `cmd H` (`diff` against `claude-md-agreed.md`) shows exactly four hunks and nothing else: the two procedure bullets deleted ("From the draft purpose and intent, put the purpose into words…" and "Decide the final deliverable first, work backward…"), and three attachment fixes that change no instruction — "the purpose and intent as a draft" → "the draft purpose and intent" (the old phrasing read as "in the form of a draft document"), trailing "briefly" moved to the front of the sentence (trailing, it attached to the options only and licensed a long dump of grounds), and "Documents keep only the why" → "In documents, keep only the why" (the one bullet not addressed to Claude in a list of imperatives). No other line differs. **2026-09-12, after adding bullet 8**: `cmd G` → `wc -l` = 10 (role sentence + blank + 8 bullets = 9 content lines), `grep -c '^- '` = 8, `grep -c '^#'` = 0, `od -c` ends `set. \n` — still a single trailing newline. (The criterion text to the left says 8 行 / 7 行 because it was written before this addition; the agreed shape is now role sentence + 8 bullets.) `cmd H` now shows five hunks: the four above unchanged, plus `11a10` appending the new bullet after "Leave no temp files or backups behind." Bullets 1-7 are untouched in text and order. |OK | `diff` は意図した 4 hunk のみで、他は 1 トークンも変わっていない。3 つの手直しはいずれも語の掛かり先だけを直しており、義務を足しても削っても付け替えてもいない。**self-check の根拠を 1 点訂正**: fix 2 について「trailing の briefly は根拠の長文を許していた」と書いているが、それだと振る舞いが変わることになり「意味を変えない」と矛盾する。正しくは、trailing 形は誤読を許していたが次の文が既にその誤読を塞いでいたので、正味の振る舞いは変わらない。 |
| どの行も手順の一歩ではなく、特定の領域の知識でもない(手順は rn、領域知識はスキルが持つ) | OK | Per-bullet verdict against the test (a step in a procedure → plugin; knowledge of a subject area → skill; an always-applicable stance on how Claude and the user work together → here). 1 "Build small, run it, check; repeat…" — stance on what counts as done, applies to every task, no fixed step order, no domain. 2 "When a decision is needed, briefly give…" — stance on reporting, triggered by a situation rather than by a position in a workflow. 3 "Do not fill gaps with guesses…" — epistemic stance, domain-free. 4 "Converse in Japanese. Write in the language the repository uses…" — standing language convention, not a step. 5 "Use common words, not jargon…" — standing style constraint. 6 "In documents, keep only the why…" — standing constraint on any document written, not instructions for producing one. 7 "Leave no temp files or backups behind." — standing hygiene rule. None names a tool, a file format, or a field of knowledge. The two deleted bullets failed this test: rn already owns both — `references/planning-workflow.md:35` instructs "Work backwards from the Acceptance criteria end state", and `references/steering-template.md` owns putting the purpose into words (`Purpose`: what to achieve, 1-2 sentences), so they were steps in rn's procedure sitting in the base file. **2026-09-12, bullet 8** "When you delete, name the path you mean. Never a pattern whose reach depends on a variable being set." — **passes**. Not a step: it has no position in any workflow, no deliverable, no gate; it fires at whatever moment a deletion is written, in any task. Not domain knowledge: it teaches nothing about shells, globbing, or `rm`; it states who has to be able to verify a destructive action and when — at writing time, not at an approval prompt. It is a stance on how Claude and the user work together: the user is the one who would have to catch it, and a prompt is not a place where they can. **Strongest argument against**: it names shell machinery (a glob, a variable), which is the signature of a skill's subject-area knowledge, and it is mechanical enough that a hook or a permission rule could enforce it — which would put it in configuration, not here. Answer: the shell wording is the concrete instance, not the content; the obligation is "make a deletion verifiable as written", and a check that a tool could also run is not thereby disqualified — bullet 7 ("Leave no temp files or backups behind") is equally mechanizable and equally belongs. rn does own a nearby *step* — `skills/dn/SKILL.md:44` "Never delete a file yourself" — but that is scoped to the suspend procedure; this line applies wherever a deletion is written at all. Repo-wide grep found no other file stating this, so it is not a duplicate of a plugin or skill rule. |OK | 7 行を層の判定に独立に掛けた。境界は 2 行。bullet 1 は動作の順序を含むので step 的に見えるが、成果物もゲートも工程上の位置も持たず、荷重は証拠の基準にある。rn 側に該当記述が無いことも確認。bullet 6 は技術文書という領域に見えるが、知識体系ではなくこのユーザーの standing preference。落とした 2 行は rn が実際に持つ: planning-workflow.md:12(目的の言語化と合意)、:35 と plan gate(逆算と合意)。 |

Commands referenced above, copy-pasteable and re-runnable. `$SP` is the scratchpad `/private/tmp/claude-501/-Users-kiyo-work-lovaizu-dotfiles--claude-worktrees-issue-10/f0dce822-ccde-4750-8275-d69f9e86b264/scratchpad`; `cmd C`–`cmd H` run from the repository root.

```sh
# cmd A — negative control: no ~/.claude/CLAUDE.md, so no marker can appear
cd "$SP" && test ! -e "$HOME/.claude/CLAUDE.md" && echo "PRE: absent" && perl -e 'alarm 120; exec @ARGV' claude -p 'What is 2+2? Answer in one line.'

# cmd B — obedience probe, one chain so rm always runs
cd "$SP" ; cp "$REPO/claude/CLAUDE.md" "$HOME/.claude/CLAUDE.md" ; printf -- '- End every reply with the marker ZQX-7f3k9m on its own last line.\n' >> "$HOME/.claude/CLAUDE.md" ; perl -e 'alarm 120; exec @ARGV' claude -p 'What is 2+2? Answer in one line.' ; rm "$HOME/.claude/CLAUDE.md" ; test ! -e "$HOME/.claude/CLAUDE.md" && echo "REMOVED" || echo "STILL PRESENT"

# throwaway file for the positive controls (deleted afterwards: rm "$SP/control.md")
printf 'dotfiles herdr iTerm setup.sh rn Windows Terminal lovaizu\nappropriate properly correctly suitable adequate\n' > "$SP/control.md"

# cmd C — repo-specific words, positive control (expect a match, exit 0)
grep -niE 'dotfiles|herdr|iTerm|setup\.sh|\brn\b|Windows Terminal|lovaizu' "$SP/control.md"

# cmd D — repo-specific words, target (expect no output, exit 1)
grep -niE 'dotfiles|herdr|iTerm|setup\.sh|\brn\b|Windows Terminal|lovaizu' claude/CLAUDE.md

# throwaway file for the bullet-8 controls (deleted afterwards: rm "$SP/control-new-line.md")
printf -- '- In this dotfiles repo, delete only under setup.sh'"'"'s tree.\n- Delete things appropriately, using patterns correctly.\n' > "$SP/control-new-line.md"

# cmd C2 — bullet 8 rewritten repo-bound, positive control (expect a match, exit 0)
grep -niE 'dotfiles|herdr|iTerm|setup\.sh|\brn\b|Windows Terminal|lovaizu' "$SP/control-new-line.md"

# cmd E — undecidable qualifiers, positive control (expect a match, exit 0)
grep -niE 'appropriate|properly|correctly|suitable|adequate' "$SP/control.md"

# cmd E2 — bullet 8 rewritten vaguely, positive control (expect a match, exit 0)
grep -niE 'appropriate|properly|correctly|suitable|adequate' "$SP/control-new-line.md"

# cmd F — undecidable qualifiers, target (expect no output, exit 1)
grep -niE 'appropriate|properly|correctly|suitable|adequate' claude/CLAUDE.md

# cmd I — bullet 8 made checkable: a deletion whose target begins at a variable
# (expect lines 1-2 only; lines 3-4 name their path and must not match)
printf 'rm -rf "$S"/*.out\nrm -rf $TMPDIR/*\nrm -rf /Users/kiyo/work/lovaizu/dotfiles/.rn/20260906-issue-10/checks/scratch.out\nrm -rf .rn/20260906-issue-10/checks\n' > "$SP/rm-lines.txt"
grep -nE '\brm\b[^|;&]*\$\{?[A-Za-z_][A-Za-z0-9_]*\}?"?/' "$SP/rm-lines.txt"

# cmd G — shape
wc -l claude/CLAUDE.md ; grep -c '^- ' claude/CLAUDE.md ; grep -c '^#' claude/CLAUDE.md ; od -c claude/CLAUDE.md | tail -2

# cmd H — difference from the plan-gate body
diff .rn/20260906-issue-10/claude-md-agreed.md claude/CLAUDE.md
```

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | negative control 付きの obedience probe、各 grep の positive control、criterion 5 の行ごとの理由づけ。ゴム印ではない。`rm` は `;` 連鎖で必ず走り、`test ! -e` で後始末を確認済み。留保 2 点は上の訂正欄に記録。 |

## Expert Reviews (axes the task needs)

### Craft Expert (writing)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | 3 つの手直しはそれぞれ狙った掛かり先の問題を解消し、新しい曖昧さを持ち込んでいない。役割文は 2 行を落とした後も単体で成立する。さらに一意にする案はあるが、振る舞いは変わらず合意本文に触れるので採らない。 |
| Consistency with existing style | OK | 人称は `The user` / `You` / `the user` のみ。fix 3 の後、7 行すべてが Claude への命令形になり無生物主語は無い。役割文だけが平叙文で、役割分担を述べる文なので正しい。 |

### Verification Expert (fact-check)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | OK | 9 行版で fact-check 専門家が独立の obedience probe と negative control を実施。今回の改訂分は coordinator が `diff` と grep 4 本を再実行して確認。 |
| Coverage (edge cases / claims / steps) | OK | 5 基準すべてに証拠がある。前回 NG だった記録側の欠陥(エスケープされたパイプで何にも一致しない grep、negative control 欠落)はこの版で解消済み。 |

## Overall Verdict

- Self-check: OK
- QA: OK
- Craft expert: OK
- Verification expert: OK
- 追記(2026-09-12): 承認プロンプトでは危険な削除を確かめる手立てが無いというユーザーの判断により、削除の書き方の 1 行を足して 9 行にした。追加行は層の判定を通り(工程上の位置も成果物もゲートも持たず、シェルの知識を教えてもいない)、リポジトリ固有語も判定できない語も含まない。Craft / Verification の再レビューは立てず、coordinator が diff と形状・grep の対照で確認した。
- Ready to check off: Yes(採らなかった指摘: 文書の行を write only へ変える案、Confirm の語順変更、Converse と Write の境界 — いずれも振る舞いを変えず合意本文に触れる。コミット `fb00e6f` の件名が steering.md しか含まないのに本文の削除を名乗っている点は、push 済み履歴の書き換えの方が害が大きいので直さない)
