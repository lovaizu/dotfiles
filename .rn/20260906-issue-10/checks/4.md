# 4 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| README だけを読んだ人が、何が管理されどこへ置かれるかと、手で直しても次の実行で戻ることを理解できる | OK | 「設定対象」表に `claude/CLAUDE.md` の行(何を=Claude Code への個人用の指示(OS 共通))。加えて冒頭 1 行の説明を「端末環境と、Claude Code への個人用の指示を、clone して…再現するためのリポジトリ」に改め、端末環境だけを名指していた旧文が管理対象を取りこぼす状態を解消(C1)。置き先は README の既存方針どおり個別に列挙せず、「セットアップ」の「何をどこへ置いたか…は `./setup.sh` が実行時に表示する」が 4 ファイルすべてを覆う(`setup.sh` の `deploy "$DOTFILES_DIR/claude/CLAUDE.md" "$HOME/.claude/CLAUDE.md"` で宛先を確認、他 3 ファイルと同じ `deploy` 経路)。手で直しても戻る点は「直す前に」3 つ目の項の「端末や herdr の UI から変えた設定も、置いた先のファイルを手で直した分も」で明示。 |OK | QA・文章の2名が独立に確認。表の行は herdr の隣、OS 共通の 2 件が並ぶ位置に入っており、`setup.sh` の呼び出し順(227 行と 229 行、どちらも OS 分岐の外)と一致する。配置先を書いていないのは 4 件すべてに共通の作法で、「何をどこへ置いたか…は `./setup.sh` が実行時に表示する」で実行時に委ねている。ただし他の 3 件はアプリ名が配置先を一意に決めるのに対し、`CLAUDE.md` はプロジェクト単位でも読まれうるので、表の「個人用」と本文の「どのリポジトリにも依存しない」がユーザー単位であることを示す役目を負っている。手で直しても戻る点は「直す前に」の書き換えで満たされ、これは一般化ではなく穴埋め(旧文は GUI 経由の変更しか挙げておらず、GUI を持たない `CLAUDE.md` を覆っていなかった)。 |
| README に指示の本文・具体的な文言が引き写されていない | OK | `claude/CLAUDE.md` を文/箇条書き単位に分割(12 文字以上の 17 単位)し、全単位で README への完全一致を検査 → ヒット 0(C2)。同じ検査に対照として先頭文 "The user gives the draft purpose and intent, and makes the decisions." を README のコピーへ挿入すると一致を検出するので、検査自体が取りこぼしていないことを確認済み(C2)。訳語経由の写しも 一時ファイル/バックアップを残さ/日本語/専門用語/推測/小さく/テストが通/結論/根拠/選択肢/削除/パス を grep → すべて 0 件(C3)。本文への最接近だった「誰が決めるか、何を渡すか、どう報告するか。」(ファイルの目次であって規則ではない)は今回削除した(C1)。 |OK | 言語が違うので文字列一致ではなく意味で確認。`claude/CLAUDE.md` を 17 の文・箇条に割ってすべて README に含まれないことを確認し、**control** として先頭の 1 文を README の複製に混ぜると同じ検査が検出することを示した。日本語訳 12 語(一時ファイル / 推測 / 結論 / 根拠 / 削除 / パス など)も 0 件。最も近かった「誰が決めるか、何を渡すか、どう報告するか。」は、この修正で削除済み。 |
| 既存の記述と重複した説明が増えていない | OK | 節内の二重定義を 2 か所削除。(a)基本指針の項から目次的な一文を削除、(b)結びの「どちらでもなく、いつでも当てはまる構えだけが」を「どちらでもなければ」に縮め、消去法が答えを出した後で定義を言い直す形を解消(C1)。残した「プラグインを全部外しても意味が通る。」が所属判定として機能する。既存記述との関係も従来どおり:「直す前に」3 つ目の項は上書きの理屈を増やさず例示を 1 句広げただけ、置き先は「セットアップ」の一文に任せて重ねて書かない。節は 9 行・342 文字(削除前 9 行・362 文字)、README 全体は 57 行・1965 文字で、節が占める割合は 9/57 行(C4)。 |OK | 既存記述との重複は無い。README はこれまで Claude Code にもプラグインにもスキルにも触れていなかった。節の内部にあった二重定義(「基本指針」の定義と、締めの「いつでも当てはまる構えだけが」)は 2 名が一致して指摘し、両方とも削除した。膜となる検査は「プラグインを全部外しても意味が通る」の 1 つに絞った。 |
| README が、この 3 層(CLAUDE.md = 手順にもリポジトリにも依存しない基本指針 / rn などのプラグイン = 手順 / スキル = 領域知識)と、行を足すときの判定を持っている | OK | 「設定対象」直下の `### Claude Code への指示をどこに書くか` に 3 項:CLAUDE.md=基本指針(「どの手順にも、どのリポジトリにも依存しない、Claude との関係そのもの。プラグインを全部外しても意味が通る。」/ 二人称を使わない文体に統一)、プラグイン(rn など)=手順、スキル=ある領域の知識(「その領域で何が定石で、何を避けるか。」— 他 2 項と同じ長さ・語調の 1 句を補い、太字リードだけで終わる項を無くした)。判定は続く一文:「手順の 1 ステップならプラグインへ、ある領域の知識ならスキルへ。どちらでもなければ `claude/CLAUDE.md` に残る。」見出し深さは表の行に接続する説明として `###` のまま(C4)。 |OK | 3 層と消去法の判定がそろっている。判定は「手順の 1 ステップならプラグインへ、ある領域の知識ならスキルへ、どちらでもなければ残る」と順序と行き先を名指ししており、候補の行に実際に当てられる形になっている。 |

検査に使ったコマンド:

```sh
# (C1) 変更内容の確認
git diff README.md
sed -n '1,3p;14,22p' README.md

# (C2) CLAUDE.md の各単位が README に残っていないか + 対照で検査の有効性を確認
python3 - <<'EOF'
import io, re
cm = io.open('claude/CLAUDE.md', encoding='utf-8').read()
rm = io.open('README.md', encoding='utf-8').read()
units = []
for line in cm.splitlines():
    line = re.sub(r'^[-*]\s+', '', line).strip()
    if not line:
        continue
    for s in re.split(r'(?<=[.?!])\s+', line):
        s = s.strip()
        if len(s) >= 12:
            units.append(s)
scan = lambda t: [u for u in units if u in t]
print('units:', len(units))
print('hits in README:', scan(rm))                      # -> []
print('hits in control:', scan(rm + chr(10) + units[0]))  # -> 先頭文を検出
EOF

# (C3) 訳語経由の写し
for w in 一時ファイル バックアップを残さ 日本語 専門用語 推測 小さく テストが通 結論 根拠 選択肢 削除 パス; do
  printf '%-14s %s\n' "$w" "$(grep -c -- "$w" README.md)"
done

# (C4) 節と全体の分量
sed -n '14,22p' README.md | wc -lm   # 9 行 / 342 文字(削除前: 9 行 / 362 文字)
wc -lm < README.md                   # 57 行 / 1965 文字
git show 21c4d8a:README.md | sed -n '14,22p' | wc -lm
```

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | 引き写しの検査に control が付いており、「一致しない」が検査の不備によるものでないことが示されている。基準 1 は README を初見の読者として読み直す形で確かめられている。 |

## Expert Reviews (axes the task needs)

### Craft Expert (writing)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | 修正後。指摘は 3 件とも反映済み(締めの重複削除、読み手を名指しする語の除去、3 つ目の箇条の裸の見出しに 1 句追加)。体言止めと平叙の終止は既存の文体に合っている。 |
| Consistency with existing style | OK | 管理対象 / 設定対象 / 配置 / 退避 と競合する用語を持ち込んでいない。見出しの語と表の語が一致している。 |

### Verification Expert (fact-check)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | N/A | 文章のタスクで、事実確認は Craft と QA の 2 名が担った。配置先は `setup.sh` を読んで確認済み。専門家を 3 人目として立てる規模ではないと coordinator が判断。 |
| Coverage (edge cases / claims / steps) | N/A | 同上。 |

## Overall Verdict

- Self-check: OK
- QA: OK
- Craft expert: OK
- Verification expert: N/A(文章のタスクにつき QA と Craft が事実確認を兼ねた)
- Ready to check off: Yes

採らなかった指摘:

- **見出しを `###` から `##` へ上げる案**。2 名とも「判断の余地」とし、どちらも必須とはしなかった。節の 1 文目が直上の表の行に錨を下ろしているので `###` のままにする。`##` に上げると、概念の補足が 設定対象 / 直す前に / セットアップ と同格になり、実態より重く見える。
- **プラグインとスキルが Claude Code の用語で、この README では未定義**という指摘。herdr も同じく未定義のまま表に出ており、この文書の作法に沿っている。

## 改訂ラウンド(PR #12 レビュー)

指摘「詳細を書くとメンテのたびに書き換えが要る/成果物を見れば分かる/入り口だけ示すべき」を受けた README の書き直し。57 行 → 38 行。

落とした: 「## 設定対象」の表、冒頭の管理対象の列挙、「### Mac 初回のみ: herdr プロファイルをデフォルトにする」の手順 3 ステップ、フォント名の在り処を JSON キー名で指す記述。
残した: 「直す前に」の 3 点、Claude Code への指示の 3 層と消去法の判定、`./setup.sh` 1 コマンドという使い方と実行時表示に任せる一文、フォントの節(入手先リンク + Win 手動 / Mac 自動)。

| # | 確認項目 | Self-check | Evidence |
|---|---|---|---|
| 1 | 目的・使い方・決まりごとの 3 本立て | OK | 3 行目=目的、`## 使い方`(`### フォント` を含む)=使い方、`## 直す前に` と `## Claude Code への指示をどこに書くか`=決まりごと。`##` 見出しは 3 つ。 |
| 2 | 管理対象の一覧が消えている | OK | `config.toml` / `settings.json` / `herdr.json` と表の見出し「何を」は README から消滅(grep でヒット 0)。置き先は 13 行目の「何をどこへ置いたか…は `./setup.sh` が実行時に表示する」に委ねる(`setup.sh` の `echo "Installed $dst"` / `echo "Backed up the previous $dst to $bak"` / 末尾の失敗一覧を確認)。`claude/CLAUDE.md` はファイル名として 32・34・38 行に残るが、これは指示をどこに書くかの決まりごとの対象であって配置先の一覧ではない。 |
| 3 | 3 層と行を足すときの判定が残っている | OK | 32〜38 行。3 層(基本指針 / プラグイン = 手順 / スキル = 領域知識)と消去法の一文を文言そのまま維持。見出しは `###` → `##`(親だった「設定対象」が消えたため)。 |
| 4 | 「直す前に」の 3 点が残っている | OK | 22〜28 行、3 項とも文言を変えていない。 |
| 5 | フォントの節が `setup.sh` の "see README" の指す先として成り立つ | OK | 15〜20 行。入手先 https://github.com/yuru7/HackGen/releases と Win 手動 / Mac Homebrew の別。参照元は `setup.sh` の `brew install --cask font-hackgen-nerd` 失敗時と Homebrew 不在時の 2 つの `warn` で、どちらも「手で入れる」ための入手先を求めている。 |
| 6 | 設定ファイルの内部構造に依存する記述が無い | OK | `profiles.defaults.font` / `face` / `Normal Font` / `Guid` などのキー名は README に無い(grep で 0)。17 行は「名前は端末の設定ファイルにある」とだけ言い、キーを名指ししない。 |
| 7 | iTerm2 のメニュー手順の写しが消えている | OK | `Other Actions` / `Set as Default` / 新しいウィンドウを開く手順は README から消滅。13 行「デフォルトでなければ警告し、直し方もそこで表示する」に置き換え。根拠は `setup.sh` の `iterm_default_menu` を含む 2 つの `warn`(デフォルトでない場合と、iTerm2 の設定がまだ無い場合)。 |
| 8 | 全ての記述を出典に当てた | OK | 下表。 |

各記述の出典:

| README の記述 | 出典 | 確認結果 |
|---|---|---|
| Windows (WSL) と Mac の 2 つが対象 | `setup.sh` の `case "$(uname -s)"`(Darwin / それ以外で `/proc/sys/kernel/osrelease` の microsoft 判定) | 一致 |
| 配色がそろう | `iterm2/herdr.json` の Background 0.15686 → #282828・Foreground 0.92157/0.85882/0.69804 → #EBDBB2 と `windows-terminal/settings.json` の colorScheme "Gruvbox Dark"(background #282828 / foreground #EBDBB2) | 同一の配色 |
| フォントがそろう | `iterm2/herdr.json` の HackGenConsoleNF-Regular 14 と `windows-terminal/settings.json` の HackGen Console NF / size 14 | 同一 |
| キー操作がそろう | `iterm2/herdr.json` のキーマップ 3 つ(修飾子 0x140000)が送る文字列と、`windows-terminal/settings.json` の ctrl+alt+[ / ctrl+alt+] / ctrl+alt+u が送る文字列が同じ。shift+enter と 0xd-0x20000 も改行で一致 | 送出内容が一致 |
| `./setup.sh` 1 コマンドで済む | `setup.sh` に引数処理が無く、全 deploy が一続きで走る | 一致 |
| 置いた先・退避・失敗を実行時に表示 | `setup.sh` の `deploy` の `echo "Installed $dst"` と `echo "Backed up the previous $dst to $bak"`、`record_failure` と末尾の失敗一覧 | 一致 |
| デフォルトプロファイルの警告と直し方 | `setup.sh` の `iterm_default_menu` を含む 2 つの `warn`(いずれも Fix: にメニュー経路を出す) | 一致 |
| フォント入手先 | 旧 README から引き継ぎ。`setup.sh` の 2 つの警告が「install the font by hand (see README)」で参照 | 参照先として成立 |
| Mac は Homebrew があれば自動 | `setup.sh` の `command -v brew` → `brew install --cask font-hackgen-nerd`(導入済みなら skip) | 一致 |
| Windows は必ず手動 | `setup.sh` の WSL 側にフォント処理が無い(deploy するのは `windows-terminal/settings.json` だけ) | 一致。旧 README の「再実行するコマンドは警告がそのまま表示する」は Homebrew 不在時の警告がそのコマンドを出さないため削除した |
| 端末は暗い・herdr の UI は明るい・追随させない | `herdr/config.toml` の [theme] name = "gruvbox-light" / auto_switch = false と上記の端末側 Gruvbox Dark | 一致 |
| Mac のキーが cmd 側 | `iterm2/herdr.json` の修飾子 0x140000(control 0x40000 + command 0x100000)と Win 側の ctrl+alt | 一致 |
| `./setup.sh` が管理対象を丸ごと上書きする | `setup.sh` の `deploy` が `cp` → `mv -f "$DEPLOY_TMP" "$dst"` で置き換え、直前に `backup_file` を取る | 一致 |
| 指示の 3 層と消去法 | 成果物から読めない決まりごと。前ラウンドで受け入れ済みの記述を文言そのまま維持 | 変更なし |

既存の表の基準 1「何が管理されどこへ置かれるか」について: 管理対象の列挙を落とすのはコーディネーターの判断であり、README はこれを `./setup.sh` の実行時表示に委ねる形になった。「手で直しても次の実行で戻る」は「直す前に」3 つ目の項に残っている。

### 冒頭の目的文の修正(コーディネーター指摘)

修正前: 「clone して `./setup.sh` を実行すると、どちらの OS でも端末の配色・フォント・キー操作がそろう。」
修正後: 「Windows (WSL) と Mac で同じ作業環境を使うためのリポジトリ。clone して `./setup.sh` を実行するだけで、どちらの OS でも同じ設定になる。」

| # | 確認項目 | Self-check | Evidence |
|---|---|---|---|
| a | 冒頭が端末に限定されていない | OK | 3 行目に「端末」の語が無い(grep の残りヒットは 17・26・28 行で、いずれもフォントと「直す前に」の個別の話)。主語は「作業環境」で、OS をまたいでそろえるものを種類で絞っていない。 |
| b | 管理対象の列挙が無い | OK | 「配色・フォント・キー操作」の 3 点並列を削除。ファイル名・アプリ名・設定項目名を 1 つも挙げていない。何がそろうかは `./setup.sh` の実行時表示に委ねる(13 行目)。 |
| c | 管理している 4 ファイルのどれも目的文から外れて見えない | OK | `setup.sh` が deploy するのは `herdr/config.toml`(OS 共通)、`claude/CLAUDE.md`(OS 共通)、`iterm2/herdr.json`(Darwin)、`windows-terminal/settings.json`(WSL)の 4 つ。「同じ作業環境」「同じ設定」はこの 4 つを種類で区別せず覆う。とくに `claude/CLAUDE.md` は端末の設定ではないので、旧文では外れて見えていた。 |
