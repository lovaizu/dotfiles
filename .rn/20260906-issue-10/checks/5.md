# #5 Acceptance Criteria Check

2026-09-13 の再実行。新しい clone(`fd7220b`)に `HOME` を隔離ディレクトリへ向けて
`./setup.sh` を 4 回流した結果。

| # | Acceptance criterion | 結果 | 根拠 |
|---|---|---|---|
| 1 | clone して実行すると配置先が原本と `diff` 一致 | OK | run 1 で `Installed .../.claude/CLAUDE.md`、`diff` が無出力 |
| 2 | 配置は OS 分岐の外、Mac と WSL で同じ配置先計算 | OK | `setup.sh:229` は `case "$(uname -s)"` より前。式は `$HOME/.claude/CLAUDE.md` のみで OS 条件を持たない(Mac で実行確認、WSL は同一行を通る) |
| 3 | 配置先は `$HOME/.claude/CLAUDE.md` 固定、`CLAUDE_CONFIG_DIR` を扱わない | OK | `setup.sh:229`。リポジトリ全体に `CLAUDE_CONFIG_DIR` の出現なし |
| 4 | 既存ファイルがあればバックアップ、名前が既存3つと衝突しない | OK | run 3 で `CLAUDE.md.20260913081352.bak` が残り、中身は退避前の内容。同時刻の `config.toml` / `herdr.json` と別名。basename 4 種(`config.toml` / `CLAUDE.md` / `herdr.json` / `settings.json`)は全て異なる |
| 5 | 失敗は FAILURES に載り非ゼロ終了、成功時は配置先と退避が表示される | OK | run 4(配置先ディレクトリを 500)で `Finished with 1 failure(s)` + `EXIT=1`、他の対象は継続。run 1/3 が `Installed` と `Backed up the previous ... to ...` を表示 |
| 6 | リポジトリの `CLAUDE.md` が役割 1 文 + 指示 8 行(英語) | OK | 10 行(役割 1 + 空行 + 箇条書き 8)。8 行の主題が criterion の列挙と一致し、見出し・タイトルなし |
| 7 | README が 3 層と行を足すときの判定を持つ | OK | 「Claude Code への指示をどこに書くか」の節に 3 層と消去法の判定 |
| 8 | README が意図のレベルで説明し、指示の中身を持たない | OK | 表の 1 行と層の説明のみ。8 行の文言は README に現れない。配置先は実行時表示に委ねている |
| 9 | 既存3ファイルの配置挙動が変わらない | OK | `main...HEAD` の `setup.sh` 差分は `deploy` 1 行の追加のみ。run 1/2/3 で herdr と iTerm2 が従来どおり配置・`Up to date`・退避 |

補足: このマシンの `~/.claude/CLAUDE.md` は 8 行目を足した後に `./setup.sh` を流していないため
7 行のまま。成果物の欠陥ではなく、マージ後に実行すれば揃う。
