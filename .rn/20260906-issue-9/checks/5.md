# 5 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| プラグインが何も入っていない `$HOME` で `./setup.sh` を流したあと、repo が宣言したプラグインが有効なものとして使える | OK | 空の隔離 `$HOME`(`mktemp -d`)で `HOME=$H1 bash ./setup.sh` を実行(`claude`/`jq` は実物、marketplace/plugin は未登録・未導入)。出力に `Adding marketplace…✔ Successfully added marketplace: ccpm` → `Added marketplace ccpm (lovaizu/ccpm).` → `Installing plugin "rn@ccpm"...✔ Successfully installed plugin: rn@ccpm (scope: user)` → `Installed plugin rn@ccpm.` → 最後に `Done.`、`exit 0`。実行後 `HOME=$H1 claude plugin list --json` が `{"id":"rn@ccpm",...,"enabled":true}` を返し、`HOME=$H1 claude plugin marketplace list --json` が `{"name":"ccpm","source":"github","repo":"lovaizu/ccpm",...}` を返すことを確認 | OK | Verification が独立に同シナリオを実機再現し同じ結果を確認済み。QA も `enabledPlugins` キー形式(`rn@ccpm`)が `claude plugin list --json` の `.id` および `claude plugin install` の引数形式と一致することを実データで確認 |
| すでに入っている環境で流しても、使える状態と run の終わり方が変わらない | OK | 上記1の直後、同じ `$H1` で `./setup.sh` をもう一度実行。`Marketplace ccpm already configured. Skipping.` → `Plugin rn@ccpm already installed and enabled. Skipping.` → `Done.`、`exit 0`(1回目と同じ終わり方)。実行後も `claude plugin list --json` は `enabled: true` のまま、`$H1/.claude/settings.json` は repo の `claude/settings.json` と `diff` で完全一致(バイト一致)。2回目の実行では `settings.json` 自体は claude 本体が書き戻した差分を repo の内容に戻すため `Backed up the previous .../settings.json to ...` → `Installed .../settings.json` が出たが、これは task #4 で確認済みの deploy() の既存挙動であり、プラグイン側の判定・終わり方には影響していないことを確認 | OK | Verification が同一シナリオを実機再現。`claude` CLI 呼び出し自体が `settings.json` の `model` フィールドを非決定的に書き換えることがあり、それが次回 deploy() を "Up to date" ではなく "Backed up + Installed" にすることを観察(exit code・プラグイン有効状態には無影響、#4 の性質への副次的な観察事項として記録) |
| プラグインの実体を用意できない環境でも、設定そのものの再現は完了する。プラグインまで再現できたかどうかは run の終わり方から読み取れる | OK | `claude`/`jq` を PATH から外した隔離実行: 全実行ファイルを列挙し `claude`/`jq` だけを除いたシンボリックリンク集(`$FAKEBIN`)を作り、`PATH=$FAKEBIN HOME=$H3 "$FAKEBIN/bash" ./setup.sh` を実行(`PATH=$FAKEBIN command -v claude`/`jq` はともに exit 1 で「無い」ことを事前確認)。結果: `Installed .../.claude/settings.json` `Installed .../.claude/scripts/statusline.sh` `Installed .../.claude/CLAUDE.md` は通常どおり配置され、`WARNING: claude and/or jq is missing, so no plugin declared in settings.json was realized.`(record_failure ではなく warn)が出たのち `Done.`、`exit 0`。設定の再現(exit 0 + Done.)とプラグイン未実施(warn の有無)が run の終わり方から見分けられることを確認 | OK | Verification が同一シナリオを実機再現、同じ結果 |
| #4 で満たした状態が壊れていない — 設定の再現結果と run の終わり方が、プラグイン側の成否に引きずられない | OK | プラグイン側をわざと失敗させる実測: `claude/settings.json` の `extraKnownMarketplaces.ccpm.source.repo` を一時的に存在しないリポジトリ名(`lovaizu/nonexistent-repo-xyz-does-not-exist-999`)に書き換え(テスト後に `git checkout -- claude/settings.json` で復元、`git diff --stat` で `setup.sh` のみが変更ファイルであることを確認済み)、新しい隔離 `$HOME` で `./setup.sh` を実行。`Installed .../.claude/settings.json`・`scripts/statusline.sh`・`CLAUDE.md`・iTerm2 プロファイルは通常どおり配置された後、`claude plugin marketplace add` が `✘ Failed to add marketplace: ...`(exit 1)→ `WARNING: Marketplace ccpm (...) was not added.` → `record_failure` により `FAILURES` に追加。続く `claude plugin install rn@ccpm -y` も `✘ Failed to install plugin "rn@ccpm": Plugin "rn" not found in marketplace "ccpm"`(exit 1)→ 同様に record_failure。最終的に `Finished with 2 failure(s): - Marketplace ... - Plugin ...` と表示され `exit 1`。設定ファイル自体の配置(#4 の保証)は正常に完了しており、プラグイン失敗の record_failure だけが run 全体を非0にしていることを確認 | OK | Verification が同一シナリオを実機再現、`claude plugin list/marketplace list --json` が共に `[]` で中途半端な状態が残っていないことも追加で確認 |
| (追記) Craft レビューで指摘された欠陥の修正 — `$claude_settings` の `jq` 解析自体が失敗した場合(手編集で JSON が壊れた場合)も、上記4行目と同じ severity で検知・報告される | OK | 修正前は `done < <(jq ...)` が `set -euo pipefail` 下でも `jq` の exit status を伝播せず、ループが0回実行されて `warn`/`record_failure` なしに `Done.`/`exit 0` に達していた(`echo '{invalid json' \| jq ...` の再現で確認)。修正: 両方の `jq -r ...` 呼び出しを `if var=$(jq ...); then ループ; else record_failure; fi` の形に変更。再検証として `claude/settings.json` を一時的に壊れた JSON(`{invalid json, missing closing`)に書き換え(テスト後に元の内容へ復元、`git status --porcelain -- claude/settings.json` で差分なしを確認済み)、新しい隔離 `$HOME` で `./setup.sh` を実行。結果: `deploy()` による `settings.json`/`CLAUDE.md`/`statusline.sh` の配置は従来どおり `Installed ...` で成功(deploy 自体は JSON を検証しないため無傷)、その後 `jq: parse error: Invalid numeric literal at line 1, column 9` が2回(marketplaces 用・plugins 用それぞれ)出力され、`WARNING: settings.json's extraKnownMarketplaces could not be read.` と `WARNING: settings.json's enabledPlugins could not be read.` が `record_failure` として記録され、最終的に `Finished with 2 failure(s): - settings.json's extraKnownMarketplaces could not be read. - settings.json's enabledPlugins could not be read.` で `exit 1`。修正により沈黙した成功偽装(exit 0)がなくなったことを実測で確認。なお修正の実装過程で `jq` 出力を変数に経由させる際に一度 `printf '%s' "$var"`(末尾改行なし)を使い、宣言が1件だけの場合に `read` がその1件を読み捨てる回帰を作り込んだが、`bash -x` トレースで検出し `printf '%s\n'` に修正して解消(fresh-install 再実行で `Added marketplace ccpm (lovaizu/ccpm).` / `Installed plugin rn@ccpm.` が出ることを確認) | OK | Craft がこの修正自体を `set -euo pipefail`/`/bin/bash 3.2.57` の両方で実機検証(`if var="$(jq ...)"` は `set -e` の対象外であること、`jq` の parse-error は exit 5・空結果は exit 0/0バイトであることを実測で確認、誤ルーティングなし) |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | 実際の `enabledPlugins`/`extraKnownMarketplaces` の形(`rn@ccpm` / github source)に対して、コマンドの exit code だけでなく `claude plugin list --json`/`marketplace list --json` で実際の到達状態を確認する形になっている。指摘事項: 「インストール済みだが無効化されている(enabled=false)」状態は precheck が再インストールへ倒す挙動が未測定(単一ユーザー・低頻度実行のため非ブロッキングと判断) |

## Expert Reviews (axes the task needs)

### Craft Expert (coding)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK(1周目 NG → 修正後 OK) | 1周目: `done < <(jq ...)` が `set -euo pipefail` 下で `jq` の parse error を伝播せず、警告なしに exit 0 で通過する欠陥を検出(実機再現済み)。修正後: `if var="$(jq ...)"; then ... else record_failure; fi` に変更されたことを `set -e` の意味論・`jq` の実際の exit code の両面から実機で再検証し、欠陥が解消されたことを確認 |
| Consistency with existing style | OK | コメントは「なぜ」のみ、`record_failure` メッセージの形(症状・原因参照・影響範囲・具体的な fix 手順)は既存コードと一致。2つのループ間の重複はラッパー部分のみで、既存ファイルの「1〜2箇所限りのロジックはインラインに書く」流儀と整合するため、ヘルパー化しないことを妥当と判断 |

### Verification Expert (test)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | OK | 4シナリオ(新規/再実行/claude・jq無し/実失敗)全てを隔離 `$HOME` で独立に実機再現し、self-check の記載と一致することを確認。加えて修正後の malformed JSON ケースも Craft が独立に実機検証 | 
| Coverage (edge cases / claims / steps) | OK | この repo の実際の宣言形(github source 一形、`enabledPlugins` 常に `{"name@marketplace": true}`)の範囲では網羅。範囲外(空 `enabledPlugins`、`@marketplace` サフィックス無し、github 以外の source)は実在しない設定であり単一ユーザー repo では検証不要と判断。観察事項として `claude` CLI 呼び出しが `settings.json` の `model` を非決定的に書き換えることがある点を記録(コード欠陥ではない) |

## Overall Verdict

- Self-check: OK
- QA: OK
- Craft expert: OK(修正1周)
- Verification expert: OK
- Ready to check off: Yes
