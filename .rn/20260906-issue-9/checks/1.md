# 1 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| `design.md` を読んだ第三者が、Acceptance criteria の各状態をこの設計がどう満たすかを辿れ、その判断が要件の変化に対してまだ正しいかを判断できる。h3 の問いに未回答のものが無い | OK | Round 3 の3件(§4.1 permissions行、§1.4/§4.4 のアンインストール対象外、§4.3 の未検証点明示)はいずれも既存 h3 の中への追記・訂正のみ。h3 見出し自体・回答状況は変更していない。§4.1 の permissions 行は AC「repo が宣言したプラグインが有効なものとして使える状態」と §4.4 の実体化メカニズムへ明示的に結びつけたことで、要件変化時に辿れる理由が増した。Round 4 の4件(§4.4文書名補完、§4.1/§2.1の`~`展開重複解消、§4.3未検証点の§2.1登録、§1.4理由(2)の書き直し)も全て既存h3内の訂正・追記で、新しいh3や未回答のh3は生じていない。§4.4の「§4.6」参照に文書名(herdr4mac design.md)を補ったことで、自セクション参照との混同が無くなり辿りやすさが上がった | OK | Craft/Verification/Design 3軸の再レビュー(round 3・round 4後の最終確認)がいずれも「h3全問回答済み・第三者が辿れる」を確認済み(下記 Expert Reviews 参照) |
| repo が持たないと決めた設定について、持たない理由が書かれている | OK | 今回の3件のうち修正2(§1.4)は「宣言から消えたプラグイン/marketplace のアンインストール・登録解除」を新たに対象外として追加し、持たない理由(頻度・実害の性質・複雑さとの見合い)を明記した。修正1・3は既存の「持たない」判断を変更していない(§4.1 の「マシン側に委ねる設定」「秘密」の理由、§4.3 の herdr-agent-state.sh を持たない理由は無傷)。Round 4 の修正4は、この対象外(アンインストール非対応)とする理由(2)を「残存プラグインは実害が無い」という不正確な主張から、公式ドキュメント(`enabledPlugins`の`defaultEnabled`フォールバック、既定`true`)に基づく正確な限定(ファイル面は無害だが機能面は無効化される保証がない)に書き直した。対象外にする決定自体は変えず、理由の正確性のみ上げた | OK | Design軸の最終確認で、§1.4理由(2)の書き直しが技術的に正確(`enabledPlugins`の`defaultEnabled`フォールバック、`rn`プラグインのmanifest実物確認)であり、対象外とする結論の理由付けも筋が通っていることを確認済み |
| 置き換えたものの判別可能性について、それをどう保証するかと、既存の再現(herdr / iTerm2 / Windows Terminal)に及ぶ影響が書かれている | OK | §4.2(バックアップ命名衝突の解消と既存再現への影響)は今回の3件の対象外で無傷。今回の修正はこの完了基準の対象範囲(バックアップ命名・判別可能性)に触れていない。Round 4 の4件も §4.2 に一切触れていない | OK | Verification軸の再レビューで `backup_path_for()`/`backup_file()` の呼び出し箇所・署名を setup.sh 実物と行単位で照合し、§4.2 の記述が正確であることを確認済み(round 3) |
| 宣言と実体が別のものについて、実体を入れられない環境・すでに入っている環境・入れようとして失敗した環境の3つで run がどう終わるかがそれぞれ決まっている | OK | §4.4 の3環境×severity(warn/echo/record_failure)の割り当ては変更していない。修正2で追加した「宣言からの削除は対象外」は追加方向の3分類とは別の非対称であり、対象外である理由を明記したことでこの3分類自体の完全性を損なっていない。Round 4 は §4.4 内の1箇所(「§4.6」への文書名補完)以外を変更しておらず、3環境の割り当ては無傷 | OK | Design/Verification両軸の再レビューで §4.4 の3分類(warn/echo/record_failure)とその根拠に矛盾が無いことを確認済み |
| 決めていないことを決めたように書いていない — 未検証の仮定は仮定として名指しされている | OK | 修正1は §4.1 の permissions 行から「確認済み」という誤った断定を除き、`additionalDirectories` での `~` 展開が未検証であることを明示し、§2.1 にも同種の未検証事項として1項目追加した。修正3は §4.3 で「フックの呼び出し失敗が Claude Code 自身を壊さないこと」が実機で未検証の前提であることを明示的に名指しした。いずれも断定を弱め、未検証を未検証として名指しする方向の修正。Round 4 でさらに: 修正2は §4.1 permissions 行にあった `~` 展開未検証の説明が §2.1 の同内容の項目と丸ごと重複していたのを、§4.1 側は「(§2.1)」への参照1文に圧縮し、根拠・代替案は §2.1 側にのみ残す形に一本化(二重管理による将来の食い違いリスクを解消)。修正3は §4.3 が名指ししていた未検証点(herdr-agent-state.sh不在時にClaude Code自体が壊れないか)が §2.1 のカタログに未登録だったのを新規登録し、§4.3側は §4.4 と同じ「2.1で名指しした」橋渡し文に揃えた。修正4は §1.4 理由(2)の「残存プラグインは実害を持たない」という不正確な主張(公式ドキュメント`enabledPlugins`の`defaultEnabled`フォールバック=既定`true`、および実機の`rn`プラグインmanifestがこれを上書きしていないことを確認して判明)を、「宣言を消すだけでは無効化される保証はない」という正直な記述に書き直した — 未検証を未検証と名指しするだけでなく、誤っていた断定を裏付けのある記述に置き換えた点でこの基準に強く適合する | OK | Craft/Verification/Design 3軸とも、`~`展開の過大主張(round 3で発覚・修正)を含め、断定と未検証の書き分けに残る問題が無いことを round 4後の最終確認で確認済み |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | 初回(セッション中断前)に実施し PASS(軽微2件、Craft/Verification/Designと重複する内容で別途対応済み)。以降の4ラウンドの修正はいずれも内容の正確性・完了基準への紐付けの訂正であり、self-check の検証手法自体(何を確認したか)は変えていないため、この観点について再実施は不要と判断し据え置いた |

## Expert Reviews (axes the task needs)

### Design Expert (tasks that produce or revise structure/approach)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | OK | round 3で「§3.1の2機構分離・§4.2のバックアップ命名一律化・§4.4の3分類」はいずれも利用状況に対して適切と判定。round 3で指摘した3件(§4.1/§2.1重複、§4.3未検証点の§2.1未登録、§1.4理由(2)の裏付け不足)はround 4で修正し、round 4後の最終確認で解消を確認 |
| System-wide integrity (interfaces, cross-doc consistency) | OK | round 4後の最終確認で、§4.1の圧縮が情報欠落でないこと(詳細は§2.1に一本化)、§2.1新規項目が既存項目と同じ定型で§4.3と整合すること、§1.4理由(2)の書き直しが`enabledPlugins`/`defaultEnabled`の公式ドキュメントおよび実機の`rn`プラグインmanifestと技術的に一致することを確認済み |

### Craft Expert (coding / writing / visual — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | round 3で「文章の明瞭さ・正確さ」OK(軽微1件: §4.4の「§4.6」参照に文書名`herdr4mac design.md`が抜けていた)。round 4で修正、他12箇所以上の同種参照と表記統一を確認 |
| Consistency with existing style | OK | round 3で「用語・声の一貫性」OK。severity語彙(record_failure/warn/echo)・「実体化」「宣言」「配置」「破れの検出」等の用語が全編で一貫していることを確認済み |

### Verification Expert (test / fact-check / dry-run — per the task's medium)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | OK | round 3で、`~/.claude/settings.json`実物(13キー)・`setup.sh`実物(`backup_path_for`/`backup_file`呼び出し箇所)・`~/.claude/hooks/herdr-agent-state.sh`実物・`.rn/20260822-herdr4mac/design.md`・GitHub Issue #9コメント・公式ドキュメント(settings-reference.md/permissions.md)と、design.mdの事実主張を1件ずつ突き合わせて確認(11項目、全OK) |
| Coverage (edge cases / claims / steps) | OK | round 3で「未検証」と自己申告している箇所(`~`展開・WSL実機・コマンド冪等性・precheck出力形式・フック不在時のClaude Code自体の安全性)が文書内で一貫して未検証のまま扱われていることを確認済み |

## Overall Verdict

- Self-check: OK
- QA: OK
- Design expert: OK
- Craft expert: OK
- Verification expert: OK
- Ready to check off: Yes — Craft/Verification/Design 3軸とも最終ラウンドでOK。QAは初回PASSを据え置き(検証手法自体は今回の修正で変わっていないため再実施不要と判断)。5件の完了基準はいずれも満たされている

## Post-review fix pass (review findings triaged as Valid)

Four findings from the QA/Design/Craft/Verification adversarial review were triaged as Valid and fixed in `design.md`. Two other findings (README manual-steps traceability soft gap; bold-vs-plain label cosmetic style) were triaged as not required and were left untouched.

Verification performed against source before writing each fix:

1. **§4.1 key count (11→12)** — re-ran `jq 'keys | length' ~/.claude/settings.json` myself: confirmed 12. Also ran `jq 'keys' ~/.claude/settings.json` and cross-checked the 12 names against the classification table already in §4.1 (`model, hooks, statusLine, enabledPlugins, extraKnownMarketplaces, outputStyle, effortLevel, tui, skipDangerousModePermissionPrompt, theme, remoteControlAtStartup, agentPushNotifEnabled`) — table already listed all 12 correctly, only the heading's stated count was wrong. Changed "11" to "12" in the heading text only; table left untouched.

2. **§4.2 herdr/iTerm2 backup-path impact** — re-read §4.2 in full before editing; confirmed neither "herdr" nor "herdr.json"/"iterm2" appeared anywhere in that section. Added two sentences after the "既存バックアップへの影響" paragraph naming herdr's `config.toml` and iTerm2's `herdr.json` explicitly, stating their backups move from the old flat `$BACKUP_DIR/<basename>.<timestamp>[-N].bak` path to the new nested `$BACKUP_DIR/herdr/config.toml.<timestamp>[-N].bak` / `$BACKUP_DIR/iterm2/herdr.json.<timestamp>[-N].bak` paths, and that only the backup bookkeeping location changes — not deployment destination, idempotency, or failure behavior per herdr4mac design.md §4.6.

3. **§4.2 実装上の含意 (backup_path_for callers)** — re-read `setup.sh` directly: confirmed `backup_path_for()` is called at line 129 (inside `backup_file()`, computing `stem=...`), line 153 and line 163 (both inside `deploy()`). Confirmed `backup_file()`'s signature is `backup_file(dst, tmp)` (local dst="$1" tmp="$2") with no `src` parameter, called from `deploy()` as `backup_file "$dst" "$BACKUP_TMP"`. Rewrote the paragraph to state that `deploy()`'s two call sites already have `src` in scope, but `backup_file()`'s internal call site does not and needs new information passed to it — presenting the two implementation options (add a parameter to `backup_file()`, or have `deploy()` precompute the full nested path and pass it in) as a decision deferred to task #3/#4. Did not modify `setup.sh` itself. Left the surrounding sentences about `mkdir -p`, `sweep_tmp_files`/`tmp_for` needing no changes, and the stale setup.sh comment unchanged, as instructed.

4. **§4.3 misattribution to steering.md** — re-read steering.md's Assumptions section in full (6 bullets: WSL/macOS shell location, settings.json write-back, plugin-install login-free for public marketplaces, outputStyle/theme built-in status, statusline invocation mode, 1-Mac/1-Windows machine count). Confirmed none states anything about herdr's Claude Code integration being used on both machines. Changed "steering.md の Assumptions が置く前提であり" to "本設計が置く前提であり" in §4.3's closing paragraph; rest of the sentence (revisiting the `hooks.SessionStart` stanza decision if the premise breaks, out of scope for this design) left unchanged. Did not add this assumption to steering.md.

Regression check against task #1's 5 completion criteria (from steering.md, restated verbatim):

- "design.md を読んだ第三者が、Acceptance criteria の各状態をこの設計がどう満たすかを辿れ、その判断が要件の変化に対してまだ正しいかを判断できる。h3 の問いに未回答のものが無い" — holds. All edits were additions/corrections within existing answered h3 sections (4.1, 4.2, 4.3); no h3 heading or its answered status was touched.
- "repo が持たないと決めた設定について、持たない理由が書かれている" — holds. §4.1's "マシン側に委ねる設定"/"秘密" paragraphs and §4.3's herdr-agent-state.sh exclusion reasoning are untouched by these edits.
- "置き換えたものの判別可能性について、それをどう保証するかと、既存の再現(herdr / iTerm2 / Windows Terminal)に及ぶ影響が書かれている" — strengthened, not regressed: the herdr/iTerm2 impact on existing backups is now explicitly named in §4.2, closing exactly the gap this criterion covers.
- "宣言と実体が別のものについて、実体を入れられない環境・すでに入っている環境・入れようとして失敗した環境の3つで run がどう終わるかがそれぞれ決まっている" — holds. §4.4 (plugin realization) was not touched by any of these 4 fixes.
- "決めていないことを決めたように書いていない — 未検証の仮定は仮定として名指しされている" — strengthened: fix 4 corrects a case where an assumption was wrongly attributed to steering.md rather than named as this design's own; fix 3 explicitly defers the concrete signature decision to task #3/#4 rather than pretending it's already decided.

No other sections, wording, or formatting were changed. The two excluded findings (README manual-steps traceability soft gap; bold-vs-plain label style) were not touched.

## Post-review fix pass, round 2 (5 coordinator-triaged findings from a separate adversarial-review batch)

Scope: `design.md` only (steering.md / setup.sh / README untouched). Five fixes, each verified against primary sources before writing.

1. **§1.3 wording vs §4.1 table mismatch** — Re-read §1.3 and §4.1 in full. Confirmed "他のプログラムが所有する" never applies to a `settings.json` key in §4.1's table; it only applies to the referenced file (`herdr-agent-state.sh`) excluded in §4.3. Rewrote §1.3's bullet to state the key classification as 3-way ("repo が持つ/マシン側に委ねる/秘密", for §4.1) separately from the referenced-file exclusion criterion ("他のプログラムが所有するか", for §4.3), while keeping the 4-category concept itself (repo が持つ/マシン側に委ねる/他のプログラムが所有する/秘密) unchanged as instructed.

2. **§4.1 key table — add `permissions.additionalDirectories`** — Ran `jq 'keys' ~/.claude/settings.json` and `jq 'keys | length'` myself on the live file: 13 keys, including `permissions` (previously 12, per checks/1.md round-1's own verification). Confirmed via `jq '.permissions'` that its value is `{"additionalDirectories": ["/Users/kiyo/.claude/plugins/cache"]}`. Added a new table row for `permissions.additionalDirectories` classified "repo が持つ", with the reasoning given by the coordinator (worktree + plugin reference-file read friction, `~` expansion per Claude Code docs' "Working directories" section, scope limited to `~/.claude/plugins/cache` and not the broader `~/.claude/plugins`). Changed the table heading from the literal "現物12キー" to "確認時点で13キー" and added a clause noting this count can change again from Claude Code's own write-back, so it is stated as a value observed at verification time, not asserted as permanent.

3. **§1.4 hardcoded values removed** — Re-checked the live `~/.claude/settings.json`: `theme` is now `"auto"` (design.md's text said `light`, `outputStyle` is still `"Concise"`), confirming the value had gone stale from Claude Code's own write-back. Removed the literal value strings `Concise` / `light` from the out-of-scope paragraph in §1.4; reworded to "現在の outputStyle/theme の値は Claude Code 組み込みのもので" so the sentence states the *property* (built-in name, no dependency on definition files) without naming which values currently hold — consistent with the principle that concrete values belong only in the config file itself.

4. **§4.4 case 3 `record_failure` scope note** — Read `.rn/20260822-herdr4mac/design.md` §4.6's three-way severity definition directly: `record_failure` there is defined as "配る先を持っていた管理対象が、そこに届かなかった" (file-placement-only). Confirmed §4.4 case 3 in this design.md assigns `record_failure` to a plugin-install *command* failure (not a file), justified by Acceptance criteria naming plugin usability as a reproduction target — a different criterion than herdr4mac's file-arrival test. Added one sentence after case 3's existing justification stating explicitly that this `record_failure` use is not an application of herdr4mac design.md §4.6's file-placement criterion, but a separate application based on "Acceptance criteria が明示する再現対象が実現したか".

5. **§3.3 step reorder — plugin realization moved after existing OS branch** — Read `setup.sh` directly: line 2 confirms `set -euo pipefail`; `deploy()` and `warn()` are built to always return 0 (also cross-checked against herdr4mac design.md §4.6's "`deploy` は常に0を返す" mechanism) specifically to avoid `set -e` aborting the whole script on a single failure. Reordered §3.3's steps from [1 settings.json, 2 statusline.sh, 3 herdr-agent-state.sh check, 4 plugin realization, 5 existing OS branch, 6 FAILURES] to [1, 2, 3, 4 existing OS branch (iTerm2/font, WT), 5 plugin realization, 6 FAILURES]. Added a sentence to the (now) step 5 explaining the reorder's rationale: if the newly-added plugin-realization command invocation fails to guard `set -e` as strictly as the existing `deploy`/`warn` machinery does, placing it after the existing OS branch keeps that failure from taking down the existing iTerm2/WT reproduction with it. No other section referenced the old step numbers by number (checked via grep for "3.3" and "ステップ[0-9]"/"手順[0-9]" across design.md — only the §3.3 heading itself matched), so no other cross-references needed updating.

Regression check against the same 5 completion criteria used in round 1 — all still hold; see the updated Self-check/Evidence cells above for the per-criterion reasoning specific to these 5 fixes. None of the round-2 edits touched §4.2 or §4.3's core reasoning (only §4.3's `hooks.SessionStart` reference in §1.3 was reworded, not its own §4.3 content), so round 1's fixes there remain intact.

## Post-review fix pass, round 3 (3 coordinator-specified findings)

Scope: `design.md` only. Three fixes, each verified against primary sources before writing.

1. **§4.1 `permissions.additionalDirectories` — overclaim on `~` expansion corrected**. Fetched `https://code.claude.com/docs/en/settings-reference.md` and `https://code.claude.com/docs/en/permissions.md` directly (raw fetch via curl, not the summarizing WebFetch tool, to see exact text). Confirmed: (a) the official example for `permissions.additionalDirectories` in `settings-reference.md` is `{"permissions": {"additionalDirectories": ["../docs/"]}}` — a relative path, no `~` example; (b) no `~/common-configs` example exists anywhere in `permissions.md` or `settings-reference.md` — that example was fabricated in the prior text; (c) `~` expansion examples do exist elsewhere: `Read(~/Documents/*.pdf)` (permissions.md line 349) and `sandbox.filesystem.allowWrite: ["/tmp/build", "~/.kube"]` (settings-reference.md), but neither is `additionalDirectories` itself. Rewrote the table row to: state the official example is `../docs/` with no `~` example; state that `~` expansion for `additionalDirectories` specifically is **unverified** (dropped the word "確認済み"); name task #3/#4's isolated-environment measurement (`additionalDirectories: ["~/.claude/plugins/cache"]`, checking no permission prompt appears) as what will settle it; add the fallback if `~` doesn't expand (absolute path becomes a machine-specific value that differs between Windows/Mac, so repo can't own it — in that case this key is dropped from the design entirely); and add one sentence explicitly tying this permission's reasoning to the Acceptance criteria ("repo が宣言したプラグインが有効なものとして使える状態") and to §4.4's plugin-realization mechanism (without this permission, reading a realized plugin's reference files prompts every time, undermining "使える" as an experience). Added a matching bullet to §2.1's "What do we take as true?" list, in the same style as the existing WSL-unverified bullet, for consistency of the document's unverified-items listing.

2. **§1.4 / §4.4 — plugin/marketplace removal on declaration deletion declared out of scope**. Re-read steering.md's Acceptance criteria verbatim: "**dotfiles が正**...repo から消した設定はマシンからも消える." Re-read §4.4 in full: confirmed it only ever describes the addition direction (`marketplace add` → `plugin install`); no `marketplace remove` / `plugin uninstall` path exists anywhere in design.md. Added a new §1.4 bullet declaring this asymmetry out of scope, with the three reasons the coordinator specified: (1) removal-from-declaration is lower-frequency than addition for this dotfiles' usage pattern (single user, a few interactive runs per year) and can be handled by a manual `claude plugin uninstall` when it happens; (2) a leftover installed plugin does no active harm (no stale files being served, no wrong behavior) — it differs in kind from the "repo intent diverges from machine state" harm the Acceptance criteria is about; (3) covering this would require building the same three-way classification (can't-install / already-installed / failed) for the uninstall side too, which isn't justified by the "レビューは目的に錨を下ろす" rule. Added one line at the end of §4.4 ("**宣言からの削除(アンインストール・marketplace 登録解除)は対象外** — 理由は1.4。") referencing back to §1.4, per the coordinator's instruction to cross-reference from §4.4.

3. **§4.3 — herdr-agent-state.sh absence: named the unverified assumption about Claude Code itself not breaking**. Re-read steering.md's Acceptance criteria verbatim: "**dotfiles が原本を持たない設定を壊さない**...それが無いマシンでも Claude Code が壊れた状態にならず、必要なものがあるなら読み手にそれが分かる." Re-read §4.3's existing paragraph: confirmed it only guarantees that `setup.sh` itself exits 0 and emits a `warn` — it says nothing about whether Claude Code's own session survives a missing `hooks.SessionStart` target. Added a sentence explicitly naming this as an unverified assumption ("本設計はここで、フックの呼び出し失敗が Claude Code のセッション自体を壊さない…ことを前提にしているが、これは実機で未検証の仮定である"), tied it word-for-word to the Acceptance criteria phrase it covers ("壊れた状態にならず"), and pointed to steering.md task #4's "隔離した `$HOME` で実測する" step (re-read task #4's Steps list, confirmed it names "他のプログラムが所有する設定が無い環境" as one of the measured paths) as where this gets settled.

Regression check against the same 5 completion criteria — all still hold; see the updated Self-check/Evidence cells above for per-criterion reasoning specific to these 3 fixes. None of the round-3 edits touched §4.2, §4.4's severity/3-environment classification itself, or §4.3's core exclusion decision — only additive/corrective text was added to §1.3-adjacent §1.4, §2.1, §4.1's table, §4.3's breach paragraph, and §4.4's closing line.

## Post-review fix pass, round 4 (4 coordinator-specified findings)

Scope: `design.md` only. Four fixes, each verified against primary sources before writing.

1. **§4.4 — missing document name on a "§4.6" cross-reference**. Grepped design.md for every occurrence of "§4.6": confirmed 12+ other occurrences all read "herdr4mac design.md §4.6", while one occurrence in §4.4 ("§4.6 が置いた「`deploy()` は常に0を返す」という契約の意味が薄れる") named only "§4.6" — misleading, since this document (design.md) only has §4.1–§4.4 of its own, so a bare "§4.6" reads as a self-reference. Prefixed it with "herdr4mac design.md" to match the other 12+ occurrences.

2. **§4.1 `permissions.additionalDirectories` row vs §2.1 — duplicated `~`-expansion-unverified text**. Re-read both sections in full before editing: confirmed the long sentence in §4.1's table row (official doc example, `Read(~/Documents/*.pdf)` / `sandbox.filesystem.allowWrite` counterexamples, "documentation read ≠ verified on this key", task #3/#4 as the settling step) duplicated nearly verbatim the §2.1 bullet added in round 3. Compressed the §4.1 row's ending to "ただし `~` 展開自体は未検証(§2.1)" — matching the brevity of the `outputStyle`/`theme` row's existing "(2.1)" reference style — and moved the previously-§4.1-only fallback plan (if `~` doesn't expand, switching to an absolute path would make the value machine-specific and un-ownable by repo, so the key would be dropped from the design) into the §2.1 bullet, which now holds the full grounds/fallback/task-reference and is the single place this unverified item is spelled out.

3. **§4.3 — unverified item not catalogued in §2.1**. Confirmed by reading §2.1 in full before editing: the assumption newly added in round 3 ("フックの呼び出し失敗が Claude Code のセッション自体を壊さないことは実機で未検証") had no corresponding §2.1 entry, unlike §4.4's parallel unverified point (plugin-install idempotency), which §2.1 does catalogue and which §4.4 references back via "2.1で名指しした". Added a new §2.1 bullet in the same template as existing items (bold heading naming the unverified point, grounds — steering.md Acceptance criteria's "壊れた状態にならず" phrase and the fact that `setup.sh` can only guarantee its own exit 0 / `warn`, not Claude Code's own session survival — and which task settles it: steering.md task #4's isolated-`$HOME` measurement step). Shortened §4.3's own paragraph to reference it ("これは2.1で名指しした未検証の仮定である"), removing the now-duplicated grounds/task-reference prose from §4.3 itself.

4. **§1.4 reason (2) for excluding plugin/marketplace uninstall — inaccurate grounds corrected**. Verified primary sources directly before writing: fetched `https://code.claude.com/docs/en/settings-reference.md` (`sed -n '4454,4485p'` on the downloaded file) and confirmed the `enabledPlugins` section states "A plugin with no entry at any scope falls back to its `defaultEnabled` value"; fetched `https://code.claude.com/docs/en/plugins-reference.md` and confirmed `defaultEnabled`'s row states "Defaults to `true`". Read `~/.claude/plugins/cache/ccpm/rn/0.8.0/.claude-plugin/plugin.json` directly: its four keys are `name`, `description`, `version`, `author` — no `defaultEnabled` key, so it does not override the `true` default. These three facts together mean removing an `enabledPlugins` entry from the repo's declaration does not guarantee the plugin stops being enabled — it falls back to `defaultEnabled` (default `true`), so it can remain functionally enabled. Rewrote §1.4 reason (2): narrowed the "residual plugin is harmless" claim to files only ("正しくないファイルが配置され続ける"ことに限られる), stated plainly that removing the declaration alone does not guarantee disablement (citing both doc quotes and the manifest check), noted that explicit disablement requires the user to run `claude plugin disable <plugin>@<marketplace>` manually, and connected this asymmetry to why it can still stay out of scope: this dotfiles' purpose is "動くこと(同じ設定で動く)", and explicit disablement is an active user operation outside `setup.sh`'s job, consistent with steering.md's Goal framing ("Claude Code がその設定で動くところまで" is `setup.sh`'s job) and the existing usage-pattern rationale in reason (1)/(3). Reasons (1) and (3) were left unchanged; the decision itself (uninstall stays out of scope) was not changed.

Regression check against the same 5 completion criteria — all still hold; see the updated Self-check/Evidence cells above for per-criterion reasoning specific to these 4 fixes. None of the round-4 edits touched §4.2, §4.4's severity/3-environment classification, or §4.3's core exclusion decision — only a cross-reference fix (§4.4), a dedup consolidating detail into §2.1 (§4.1/§2.1), a new §2.1 catalogue entry plus a shortened §4.3 paragraph, and a factually corrected §1.4 reason (2).
