# 3 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| repo の原本だけを与えた `$HOME` で Claude Code が起動し、いまのマシンと同じ設定で動く — 使うモデル、statusLine の表示、出力スタイル、テーマが一致し、設定エラーが出ない | OK | 隔離 `$HOME`(`mktemp -d`)に `.claude/settings.json` と `.claude/scripts/statusline.sh`(実行ビット付き)のみを配置(`hooks/herdr-agent-state.sh` は意図的に置かず)。`HOME=$ISOLATED_HOME claude --version` は実機と同じ `2.1.278` を表示。`HOME=$ISOLATED_HOME claude doctor` はインストール方法・PATH・keychain に関する5件の警告のみで、settings.json のパースエラーや hooks/statusLine 関連の警告は無し。`statusline.sh` を実際の stdin 形式のサンプルJSON(model/context_window/effort/rate_limits を含む)で直接実行した結果 `C:1k/200k 5h:12% 7d:4% \| Opus5/h \| <dirname>` を jq エラー無く出力 — モデル名整形(Opus5)・effort表示(h)・コンテキスト/レート表示すべて正常。`model`("opus")・`outputStyle`("Concise")・`theme`("auto")・`statusLine.command` は repo の `claude/settings.json` と実機 `~/.claude/settings.json` を `jq -S` で比較しバイト完全一致(IDENTICAL)を確認済み。**制約**: `claude -p`/`claude config` 等セッションを実際に開始する経路は、隔離 $HOME・実機 $HOME を問わず資格情報が無いと `Not logged in` で即終了する(完全に空の `$HOME` でも同一メッセージで同一箇所で止まることを確認し、hooks/settings の内容とは無関係な認証ゲートであることを切り分け済み)。実機の資格情報を隔離環境にコピーして回避することはしていない(資格情報の取り扱いに関わるため見送り、権限システムの拒否にも従った)。**herdr-agent-state.sh 不在時の挙動**: `bash $ISOLATED_HOME/.claude/hooks/herdr-agent-state.sh session` を直接実行すると `bash: ...: No such file or directory`(exit 127)。Claude Code 公式ドキュメント(Hooks Guide/Hooks Reference)によれば `SessionStart` の hook は exit code 2 でもブロックされず("Can't be blocked")、ゼロ以外の終了コードは "non-blocking error" として扱われセッション起動自体は継続すると明記されている。よってこの隔離環境で herdr-agent-state.sh が無くても Claude Code 自体が壊れることはなく、警告のみで起動を継続すると判断した(ログイン制約により実セッションでの直接確認はできず、公式ドキュメント根拠による判断) | | |
| repo の原本に資格情報・トークン・そのマシンでしか意味を持たない値が含まれていない | OK | トークン・パスワード・APIキーの類は無し(grep で確認)。当初 `cp` でそのまま複写した結果、`permissions.additionalDirectories` に実機の展開済み絶対パス `/Users/kiyo/.claude/plugins/cache`(ユーザー名 `kiyo` を含む、他ユーザー・WSL では実在しないマシン固有値)が混入していたことが判明(コーディネーターが検出)。design.md §4.1 が明記する意図(値は `~/.claude/plugins/cache` に固定して repo に持たせる)通りに `claude/settings.json` を修正し、`~/.claude/plugins/cache`(展開前の literal 値)に置き換えた。`jq empty` で JSON として妥当なことを確認済み。**残る未検証事項**(design.md §2.1 がすでに明記): この literal `~` を Claude Code が実際に展開して読むかどうかは、資格情報が無い隔離環境では検証できなかった(`claude doctor` は settings.json のパース自体はエラー無く通ることのみ確認)。ログイン可能な環境での実測は task #4 に持ち越す | | |
| repo が持たないと決めた設定が原本に混ざっていない | 該当なし | design.md §4.1 の判定により、確認時点の13キーはすべて「秘密・マシン固有値は無し」として repo が持つと決められており、「持たない」と決めたキーは無い(`hooks.SessionStart` が指す `herdr-agent-state.sh` 自体は対象外だが、これはフックが指す別ファイルの話であり `settings.json` のキー自体は repo が持つ)。よって該当する混入チェック対象が無い | | |

## Overall Verdict

- Self-check: OK
