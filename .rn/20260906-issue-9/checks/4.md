# 4 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| Claude Code の設定が何も無い `$HOME` で `./setup.sh` を流すと、Claude Code が repo の原本と同じ設定で動く状態になる。続けてもう一度流しても結果は変わらない(2回目は `Up to date`) | OK | 空の隔離 `$HOME`(`mktemp -d`)で `HOME=$ISOLATED_HOME ./setup.sh` を実行。`Installed .../.claude/settings.json`・`Installed .../.claude/scripts/statusline.sh`・`Installed .../.claude/CLAUDE.md` と表示され、`diff` で3ファイルとも repo の原本とバイト一致(差分無し)。同じ `$HOME` にもう一度 `./setup.sh` を実行すると3ファイルとも `Up to date: ...` になり、exit 0 | | |
| マシン側で設定を書き換えたあとに `./setup.sh` を流すと、repo の状態に戻る | OK | 上記隔離 `$HOME` の `.claude/settings.json` を `{"tampered": true}` に書き換えてから再実行。`Backed up the previous .../.claude/settings.json to .../.local/state/dotfiles-backups/claude/settings.json.<timestamp>.bak` → `Installed .../.claude/settings.json` と表示され、再実行後の `diff` で repo の原本と一致することを確認 | | |
| `./setup.sh` が置き換えたものは後から取り出せ、取り出すときにそれがどの設定のものか判別できる。Claude Code と Windows Terminal の設定が同じ run で置き換えられても混同しない | OK | `backup_path_for()` を「配置先の basename」から「repo 内相対パス」キーに変更(`setup.sh` 58〜66行目、`deploy()`/`backup_file()` も `src_rel` を渡すよう変更)。検証は `setup.sh` を `source` して `deploy` 関数を直接2回呼ぶ形で実施: 事前に置いた `fake1/settings.json`(claude/settings.json を配置)と `fake2/settings.json`(windows-terminal/settings.json を配置)の両方でバックアップが発生し、`$BACKUP_DIR` 配下に `claude/settings.json.<ts>.bak` と `windows-terminal/settings.json.<ts>.bak` という別々のネストしたパスに分かれて作られることを確認(同じ basename `settings.json` でも衝突しない直接証拠) | | |
| 再現できなかったものがある run は、何ができなかったかを名指しし、全部できた run と区別できる形で終わる(exit non-zero、FAILURES に載る) | OK | 隔離 `$HOME/.claude/scripts` を `chmod 000` にしてから実行。`cp: ... Permission denied` → `WARNING: .../statusline.sh was not deployed.` → 末尾に `Finished with 1 failure(s): - .../statusline.sh was not deployed.` と表示され exit 1。同じ run で `claude/settings.json` と `claude/CLAUDE.md` は正常に `Installed`/diff一致していることも確認(他のデプロイを巻き込まない) | | |
| Claude Code の設定のうち他のプログラムが所有しているもの(herdr-agent-state.sh)を `./setup.sh` が上書きしない。それが無いマシンでも run は残りの再現を完了し、Claude Code が壊れた状態にならない | OK | `setup.sh` はこのパスに対して存在確認のみ行い、書き込み・削除は一切しない(コードに `deploy` 呼び出しなし)。存在しない隔離 `$HOME` で実行すると `WARNING: .../herdr-agent-state.sh does not exist.`(理由→対処法の複数行)が出た上で settings.json 等の配置は完了し exit 0。同じパスに事前にファイルを置いた隔離 `$HOME` ではこの warning が一切出ないこと(`grep -c` で0件)も確認 | | |
| herdr / iTerm2 / Windows Terminal の再現結果が変わっていない — 配置先・冪等性・失敗時と異常終了時(`set -u` 致命 / SIGINT / SIGTERM / 意図的な非0終了)の終わり方が従来どおり(既存の回帰確認) | OK | 上記の空 `$HOME` 実行・再実行で herdr(`~/.config/herdr/config.toml`)・iTerm2(`DynamicProfiles/herdr.json`)とも配置・2回目 `Up to date` を確認(Windows Terminal は実機で WSL 分岐を通せないため、上記のバックアップ検証で `deploy()` 自体の動作を確認する代替とした)。異常終了系: `HOME` 未設定で `set -u` 致命 → exit 1(改修前と同じくREACHED_ENDロジックで1になることを確認)、バックグラウンド起動後 `kill -TERM` → exit 143 を確認。`kill -INT` は本タスクの変更を一切含まない改修前の `setup.sh`(`git stash` で退避して比較)でも同じ実行時間内に完了してしまい exit 0 で終わる同一の競合が再現したため、これは本変更由来の回帰ではなく計測上のタイミングの問題であると切り分けた(INTトラップのコード自体は今回変更していない) | | |

## Overall Verdict

- Self-check: OK
