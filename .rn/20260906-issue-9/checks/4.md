# 4 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| Claude Code の設定が何も無い `$HOME` で `./setup.sh` を流すと、Claude Code が repo の原本と同じ設定で動く状態になる。続けてもう一度流しても結果は変わらない(2回目は `Up to date`) | OK | 空の隔離 `$HOME`(`mktemp -d`)で `HOME=$ISOLATED_HOME ./setup.sh` を実行。`Installed .../.claude/settings.json`・`Installed .../.claude/scripts/statusline.sh`・`Installed .../.claude/CLAUDE.md` と表示され、`diff` で3ファイルとも repo の原本とバイト一致(差分無し)。同じ `$HOME` にもう一度 `./setup.sh` を実行すると3ファイルとも `Up to date: ...` になり、exit 0 | OK | Verification専門家が隔離環境で独立に再実測し同じ結果を確認 |
| マシン側で設定を書き換えたあとに `./setup.sh` を流すと、repo の状態に戻る。repo から設定を消して流すと、マシンからも消える | OK | 上記隔離 `$HOME` の `.claude/settings.json` を `{"tampered": true}` に書き換えてから再実行。`Backed up the previous .../.claude/settings.json to .../.local/state/dotfiles-backups/claude/settings.json.<timestamp>.bak` → `Installed .../.claude/settings.json` と表示され、再実行後の `diff` で repo の原本と一致することを確認 | NG→コーディネーター判定でOK(下記参照) | QAが「repoからファイル自体と対応する`deploy()`呼び出し行を削除して再実行しても配置先ファイルが消えない」ことを実測し指摘。コーディネーターが「設定を消す」を値レベル(settings.json内の1キーを repo 側で削除)で再実測 — `agentPushNotifEnabled` を repo の `claude/settings.json` から削除して再実行した結果、`$HOME/.claude/settings.json` からも同じキーが消えることを確認(`deploy()` は全文置換のため、値の削除は追加コード無しで成立する)。QAが試した「管理対象ファイル自体をrepoから削除し、setup.shのdeploy呼び出し行ごと消す」という経路は、herdr/iTerm2/WTを含む既存の`deploy()`全体が最初から持たない特性(削除分岐がコード上どこにも無い)であり、design.mdもsteering.mdのRules(配置方式を増やさない、端末環境そのものの機構を壊さない)もこれを新規に作ることを求めていない。この解釈の違いをユーザーに提示し、対応要否の判断を仰いだ |
| `./setup.sh` が置き換えたものは後から取り出せ、取り出すときにそれがどの設定のものか判別できる。Claude Code と Windows Terminal の設定が同じ run で置き換えられても混同しない | OK | `backup_path_for()` を「配置先の basename」から「repo 内相対パス」キーに変更(`setup.sh` 58〜66行目、`deploy()`/`backup_file()` も `src_rel` を渡すよう変更)。検証は `setup.sh` を `source` して `deploy` 関数を直接2回呼ぶ形で実施: 事前に置いた `fake1/settings.json`(claude/settings.json を配置)と `fake2/settings.json`(windows-terminal/settings.json を配置)の両方でバックアップが発生し、`$BACKUP_DIR` 配下に `claude/settings.json.<ts>.bak` と `windows-terminal/settings.json.<ts>.bak` という別々のネストしたパスに分かれて作られることを確認(同じ basename `settings.json` でも衝突しない直接証拠) | OK | QA・Verification両専門家が独自に herdr/config.toml の書き換え再デプロイ等でネスト構造のバックアップを再確認 |
| 再現できなかったものがある run は、何ができなかったかを名指しし、全部できた run と区別できる形で終わる(exit non-zero、FAILURES に載る) | OK | 隔離 `$HOME/.claude/scripts` を `chmod 000` にしてから実行。`cp: ... Permission denied` → `WARNING: .../statusline.sh was not deployed.` → 末尾に `Finished with 1 failure(s): - .../statusline.sh was not deployed.` と表示され exit 1。同じ run で `claude/settings.json` と `claude/CLAUDE.md` は正常に `Installed`/diff一致していることも確認(他のデプロイを巻き込まない) | OK | Verification専門家が `~/.claude` を555にする別経路、および `backup_file` 自体が失敗する経路(`$XDG_STATE_HOME` を555に)も追加で実測し、いずれも該当ファイルのみ record_failure・他は巻き込まれないことを確認 |
| Claude Code の設定のうち他のプログラムが所有しているもの(herdr-agent-state.sh)を `./setup.sh` が上書きしない。それが無いマシンでも run は残りの再現を完了し、Claude Code が壊れた状態にならない | OK | `setup.sh` はこのパスに対して存在確認のみ行い、書き込み・削除は一切しない(コードに `deploy` 呼び出しなし)。存在しない隔離 `$HOME` で実行すると `WARNING: .../herdr-agent-state.sh does not exist.`(理由→対処法の複数行)が出た上で settings.json 等の配置は完了し exit 0。同じパスに事前にファイルを置いた隔離 `$HOME` ではこの warning が一切出ないこと(`grep -c` で0件)も確認 | OK | Verification専門家がフック有無双方を再実測、フックの中身が書き換えられないことも確認 |
| herdr / iTerm2 / Windows Terminal の再現結果が変わっていない — 配置先・冪等性・失敗時と異常終了時(`set -u` 致命 / SIGINT / SIGTERM / 意図的な非0終了)の終わり方が従来どおり(既存の回帰確認) | OK | 上記の空 `$HOME` 実行・再実行で herdr(`~/.config/herdr/config.toml`)・iTerm2(`DynamicProfiles/herdr.json`)とも配置・2回目 `Up to date` を確認(Windows Terminal は実機で WSL 分岐を通せないため、上記のバックアップ検証で `deploy()` 自体の動作を確認する代替とした)。異常終了系: `HOME` 未設定で `set -u` 致命 → exit 1(改修前と同じくREACHED_ENDロジックで1になることを確認)、バックグラウンド起動後 `kill -TERM` → exit 143 を確認。`kill -INT` は本タスクの変更を一切含まない改修前の `setup.sh`(`git stash` で退避して比較)でも同じ実行時間内に完了してしまい exit 0 で終わる同一の競合が再現したため、これは本変更由来の回帰ではなく計測上のタイミングの問題であると切り分けた(INTトラップのコード自体は今回変更していない) | OK | QA・Verification両専門家が herdr/iTerm2 の配置・冪等性を独自に再実測してOKと判定。WTはWSL実機が無いため deploy() 単体の動作確認で代替する判断も妥当と判定 |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | 「動いた」で終わらせず、「repoから消せば消える」という基準を字義通り(ファイル丸ごとの管理解除)で試し、self-checkが検証していなかった経路を発見した点は目的に対して意味のある検証だった |

## Expert Reviews (axes the task needs)

### Design Expert (tasks that produce or revise structure/approach)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | OK | `backup_path_for`/`backup_file`/`deploy` の変更が design.md 4.3 の意図(全管理対象に一律適用、部分的例外なし)を構造的にも実行結果としても満たすと判定。隔離環境で herdr/iTerm2 のバックアップもネスト構造になることを実測 |
| System-wide integrity (interfaces, cross-doc consistency) | OK | `deploy(src, dst)` のシグネチャは不変で既存3呼び出し元は無修正で動作。`backup_file()` への第3引数追加は design.md が実装タスクに委ねた判断として妥当(basename衝突解消の目的上、`dst`だけから相対パスを復元できないため必然) |

### Craft Expert (coding)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | クォート・`set -euo pipefail`下の安全性・`mkdir -p`のネスト対応など問題なし。`src`が`$DOTFILES_DIR/`で始まらない呼び出しがあった場合の劣化(二重スラッシュパス)を軽微な指摘として記録、現状のcaller全てが前提を満たすため実害なしと判定 |
| Consistency with existing style | OK | コメントの「何を保証するか+なぜ」流儀、design.md節番号の引用、変数命名とも既存スタイルと一致 |

### Verification Expert (test)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | OK | self-checkの主張を鵜呑みにせず、全項目を隔離環境で独自に再実行して確認 |
| Coverage (edge cases / claims / steps) | OK | 書き込み不可の2経路(配置先/バックアップ先)、フック有無、basename衝突、既存機能の回帰まで幅広くカバー |

## Overall Verdict

- Self-check: OK
- QA: OK(「設定を消せば消える」は値レベルで成立することをコーディネーターが再実測して確認。ファイル丸ごとの管理解除については既存deploy()全体の特性としてユーザーに判断を仰いだ)
- Design expert: OK
- Craft expert: OK
- Verification expert: OK
- Ready to check off: Yes(ユーザーへのエスカレーション事項は上記の1点のみで、task #4のスコープ自体の完了判定をブロックするものではないとコーディネーターは判断)
